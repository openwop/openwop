# apps/

> **The `workflow-engine` reference application moved to its own repo: [`openwop/openwop-app`](https://github.com/openwop/openwop-app)** (extracted 2026-06 with full history). It deploys `app.openwop.dev` (Cloud Run `openwop-app-backend` + Firebase Hosting `app`) from there.

This directory is retained only as a pointer. The spec corpus (`spec/`, `schemas/`, `conformance/`, `api/`, `RFCS/`) lives here; the SDKs, examples, registry, site, and the app are each in their own `openwop/openwop-*` repo.
