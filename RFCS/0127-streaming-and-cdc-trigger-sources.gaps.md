# RFC 0127 — Gap Register

Companion to `0127-streaming-and-cdc-trigger-sources.md`. Open questions, deferred decisions, and missing inputs beyond the in-template resolved questions. Each has an owner + a resolution path; a gap with no path is promoted to a Risk.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §1 | Exact schema surfaces carrying the `source` enum — `trigger-subscription.schema.json`, `trigger-event.schema.json`, and any SDK type unions (`TriggerSource` in the TS/Python/Go SDKs) must extend in lockstep. Enumerate every occurrence before the diff lands. | Schema Architect | Grep the schema + SDK trees for the closed enum; extend all in one PR. | Schema diff finalization |
| G2 | Proposal §2 | The `op` REQUIRED-in-`metadata.triggerData` rule for `change` events is not schema-expressible — `triggerData` is an opaque host-local bag per RFC 0099. Confirm the enforcement home: prose MUST + runtime `validation_error` + a conformance assertion, not a schema constraint. | Schema + Conformance Architect | Pin as prose MUST; conformance asserts `op` present on the delivered `change` event's `triggerData` at the seam. | `Active` |
| G3 | Conformance | Server-free seam for `stream`/`change` — the existing `trigger-bridge-delivery` scenario drives `ingestExternalEvent` via a host seam. Confirm the seam accepts `source:"stream"` / `source:"change"` (with `op`) without a real broker, and that the gated leg cannot soft-skip into a vacuous pass (the RFC 0100 vacuous-witness trap). | Conformance Architect | Extend the seam contract + scenario; non-vacuity bar = a real `trigger.delivery.attempted` for BOTH new sources. | Scenario authoring |
| G4 | Acceptance | Second witness — MyndHyve cannot witness honestly without operating a real broker/CDC consumer (RFC 0099 honesty rule). Architect ruling 2026-07-06: single-witness path approved (0124-G6 analog); second witness = first host operating a real streaming/CDC consumer. | Compatibility Architect | Named carry-forward at `Accepted`; INTEROP-MATRIX cells stay honest until a consumer host exists. | — (carry-forward) |
| G5 | Proposal §4 | Dedup-key guidance is SHOULD-only — is `(topic, partition, offset)` sufficient under consumer-group rebalancing and topic re-creation (offsets reset)? Confirm SHOULD is the right strength (a MUST would over-constrain hosts with better broker-native keys). | Spec Architect | Keep SHOULD; add a prose note that the key MUST be stable across redelivery of the *same* broker message, however derived. | `Active` |
