# RFC 0127 — Gap Register

Companion to `0127-streaming-and-cdc-trigger-sources.md`. Open questions, deferred decisions, and missing inputs beyond the in-template resolved questions. Each has an owner + a resolution path; a gap with no path is promoted to a Risk.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §1 | **RESOLVED 2026-07-06** — full sweep: 5 schema surfaces extended in one PR (trigger-event, trigger-subscription, trigger-subscription-registration, run-event-payloads `trigger.*` payload, capabilities `sources[]` + `ingestion.externalSources[]`). SDK types carry `source` as an open string with a descriptive comment (no closed union) — no SDK lockstep required. | Schema Architect | `carried:openwop.gap.0127.1` — | — |
| G2 | Proposal §2 | **RESOLVED 2026-07-06 — premise corrected.** `metadata.triggerData` doesn't exist on the wire; the TriggerEvent envelope carries per-source sub-objects, so `op` lives in the new `ChangeEvent` $def where REQUIRED **is** schema-expressible (`"required": ["op"]`). RFC §2 amended; conformance still asserts it at the seam (belt + braces). | Schema + Conformance Architect | `carried:openwop.gap.0127.2` — | — |
| G3 | Conformance | Server-free seam for `stream`/`change` — the existing `trigger-bridge-delivery` scenario drives `ingestExternalEvent` via a host seam. Confirm the seam accepts `source:"stream"` / `source:"change"` (with `op`) without a real broker, and that the gated leg cannot soft-skip into a vacuous pass (the RFC 0100 vacuous-witness trap). | Conformance Architect | `carried:openwop.gap.0127.3` Extend the seam contract + scenario; non-vacuity bar = a real `trigger.delivery.attempted` for BOTH new sources. | Scenario authoring |
| G4 | Acceptance | Second witness — MyndHyve cannot witness honestly without operating a real broker/CDC consumer (RFC 0099 honesty rule). Architect ruling 2026-07-06: single-witness path approved (0124-G6 analog); second witness = first host operating a real streaming/CDC consumer. **RESOLVED 2026-07-06 (see sweep row): openwop-app became that host** — advert flipped ON (rev `00413-jcm`) + steward-fired the real consumer end-to-end (accept via `trigger.delivery`→run; bad-sig + stale rejects; dedup effectively-once). | Compatibility Architect | `carried:openwop.gap.0127.4` Named carry-forward at `Accepted`; INTEROP-MATRIX cells stay honest until a consumer host exists. | **CLOSED** |
| G5 | Proposal §4 | **RESOLVED 2026-07-06** — SHOULD kept; the stability floor landed as prose in `trigger-bridge.md` §F.5: the dedup key MUST be stable across redelivery of the same broker message, however derived. | Spec Architect | `carried:openwop.gap.0127.5` — | — |

## Sweep at `Accepted` (2026-07-06)

Single-witness graduation (tier-1 reference host openwop-app rev `00411-77q`) under the bootstrap steward waiver; steward-curl-verified on the wire.

| ID | Disposition | Evidence / carry-forward home |
|---|---|---|
| G1 | **RESOLVED** | `carried:openwop.gap.0127.1` Five schema surfaces extended in lockstep (PR #835); SDK `source` is an open string (no union). |
| G2 | **RESOLVED** | `carried:openwop.gap.0127.2` `op` REQUIRED lives in the `ChangeEvent` $def (schema-enforced) + conformance op-required negative. |
| G3 | **RESOLVED** | `carried:openwop.gap.0127.3` Non-vacuity met: `trigger-stream-cdc-sources.test.ts` drove BOTH sources through `/v1/host/sample/trigger-bridge/{ingest,deliver}`, no soft-skips; steward curl confirmed. |
| G4 | **CLOSED 2026-07-06 (steward-fired real consumer, end-to-end)** | `carried:openwop.gap.0127.4` openwop-app IS now the "first host operating a real streaming/CDC consumer": the `stream`/`change` advert FLIPPED ON on rev `openwop-app-backend-00413-jcm` (`triggerBridge.sources[]` + `ingestion.externalSources[]` include `stream`/`change` — steward-curl-verified on `/.well-known/openwop`), and the steward **independently provisioned a real `core.openwop.streams` connection and fired the consumer boundary end-to-end** (connection `conn:d0a76f77-…`, source `stream`, BYOK HMAC): (1) **accept** — a valid signed push (`sha256=HMAC(secret,"${ts}.${rawBody}")`) → `202 {accepted:true,deduped:false}` → a REAL run (`694cf46f-…`, workflow `openwop-app.agents.lead-routing`, status `completed`) created via **`trigger.delivery.attempted`** (dlv-causation on `run.started`), i.e. `ingestExternalEvent` → run, **NOT** `resolveAndResume`; (2) **reject-signature** — bad signature on the REAL connection → `401`, no run (real HMAC verification, distinct from the `404` an unknown connection returns); (3) **reject-stale** — a signature VALID for a stale (`now-1h`) timestamp → `401`, no run (real ±5min replay window, isolated from the signature check by construction); (4) **dedup** — replaying the same `(topic,partition,offset)` → `202 {deduped:true}`, no second run (effectively-once, correlating the run to the steward's push identity); (5) **control** — exactly ONE run created across all legs (the two 401s + the dedup replay produced none). The steward-fired real-consumer boundary supersedes the earlier conformance-seam-only witness; MyndHyve's ruled-out status (no broker/CDC consumer) is unchanged but no longer gates — a second witness is now optional, not required. |
| G5 | **RESOLVED** | `carried:openwop.gap.0127.5` Dedup-key SHOULD + stability floor pinned in `trigger-bridge.md` §F.5. |
