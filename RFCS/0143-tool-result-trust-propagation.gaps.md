# RFC 0143 — Gap register

| ID | Section | Question / Missing input | Owner | Resolution path | Blocks |
|---|---|---|---|---|---|
| G1 | Conformance | **The completeness leg is only as complete as §4.** It asserts every ingress the threat model's §4 STRIDE table *names* has a fence + a curated floor; an ingress that exists in a host but was never added to §4 is invisible. Reduces the gap from "any missing fence" to "any ingress missing from §4" — smaller, reviewable, not zero. | Conformance Architect | Tightening needs a wire-level enumeration of ingress points, which does not exist. Left as RFC 0143 unresolved-Q1. | Nothing today; bounds the completeness claim |
| G2 | Conformance | **The host behavioral witness has two known non-discriminations, disclosed by the reference host.** (i) The fail-closed asymmetry the scenario rests on is unforgeable but does NOT observe the happy path — a host that fails closed correctly and still re-fires on a recorded node passes. (ii) Classification and the guard can mask each other, so the suite does not independently witness strategy (a) vs the reader-classification fallback. | openwop-app-1 | Named in the per-leg report at the `Active → Accepted` witness; not closable by the current seam. | The precision of the `Accepted` claim |
| G3 | Proposal | **"Structurally isolated" is defined only by reference to the RFC 0035 sandbox.** A host with a different isolation mechanism must argue equivalence; the corpus does not enumerate acceptable mechanisms. | Security Architect | Left to host argument for now — sandbox is the only shipped isolation. Enumerate if a second mechanism appears. RFC 0143 unresolved-Q2. | A non-sandbox host's carve-out claim |
| G4 | Motivation | **The 166/168 measurement is a reference-host number, not corpus-verifiable.** It motivates the MUST but the suite cannot check any specific host's default posture without that host's registry. | openwop-app-1 | Host-side; the invariant's completeness leg checks the corpus rule, not a host's tool-registry defaults. | Nothing; it is motivation, not a claim |
| G5 | Conformance | **No always-on leg witnesses monotone composition *behaviorally* — only that the rule is stated.** The meet-at-a-composition-boundary is host behavior; the corpus leg pins the prose. The behavioral witness is the host's memory-lane run (G2). | Conformance Architect | Same shape as RFC 0138 G6/G7 and RFC 0142 leg B — a host-sample seam would be needed for a server-side behavioral pin; deferred to whether a second host wires one. | A corpus-side behavioral guarantee |
