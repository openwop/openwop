# RFC 0132 — Gap Register

Companion to [`0132-anonymous-actor-authorization.md`](./0132-anonymous-actor-authorization.md). Working document (not normative). Per `RFCS/README.md` §"Companion gap & risk registers", every open row must be closed / transferred / carried forward as a named Unresolved question before a `Accepted` flip.

| ID | Section | Question / Missing Input | Owner | Resolution path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §B Proposal | `supported` shape: `const: true` (dataResidency/omit-when-unsupported) vs plain `boolean` (selfHostedRunner/purposePropagation). RFC picks `const: true`. Confirm the family truly has no meaningful `supported:false` state (it doesn't — a host with no anon surface omits the block). | Schema Architect | `carried:openwop.gap.0132.1` Maintainer confirm at `Draft → Active`; `anonymous-actor-shape.test.ts` pins the choice. | Schema diff finalization |
| G2 | §A Proposal | `owner.principalKind` adds `user`/`agent` alongside `anonymous`, incidentally resolving RFC 0048 §UQ1. Is widening the enum in *this* RFC acceptable, or should `user`/`agent` be split to a separate RFC and this one add only `anonymous`? | Spec Architect | `carried:openwop.gap.0132.2` Maintainer decision; low-risk either way (additive optional enum). Tracked as Unresolved-Q1. | `run-snapshot.schema.json` diff |
| G3 | §C.3 Proposal | BYOK-downgrade default (Unresolved-Q5): may a host attach a *tenant* credential to an in-audience, host-permitted anon egress, or is anon egress *always* credential-free? RFC proposes allow-the-in-audience-case; default credential-free. | Security Architect | `carried:openwop.gap.0132.3` Confirm before `Active`; composes RFC 0079 `egress-credential-audience-bound`. Tracked as Unresolved-Q5. | `anon-actor-egress-ssrf-guarded` invariant text |
| G4 | §B / §D | Rate-limit control (`rate-limit-session-cap`) advertises only *that* a cap exists, not the numeric parameters (Unresolved-Q4). Confirm the abuse-surface-disclosure tradeoff (opaque caps) is the maintainer's preference. | Security Architect | `carried:openwop.gap.0132.4` Maintainer confirm; keep opaque unless a client pre-flight need is demonstrated. | `writeEgressControls` semantics |
| G5 | §E / A2A | A2A-forwarded anonymous interactions (Unresolved-Q3): does a peer-forwarded, never-authenticated end-user map to `principalKind:"anonymous"`, and does the RFC 0128 purpose label ride the anon boundary? RFC keeps anonymous a *local* public-surface concept and defers the cross-host case. | Compatibility Architect | `carried:openwop.gap.0132.5` Follow-on RFC once an A2A public-entry adopter pulls. Tracked as Unresolved-Q3. | **Closed 2026-09-02 by RFC 0165 §B.6** — an A2A-forwarded, never-authenticated end user is `kind: "anonymous"` with the forwarding peer's subject as `actor`, never linked; the RFC 0128 purpose label rides the boundary unchanged; §A.5 remains in force for an anonymous actor recorded without an `actor`. |
| G6 | §A.2 | Cross-surface session continuity (Unresolved-Q2): a legitimate multi-page visitor journey vs the §A.2 no-reuse rule. A host-minted, non-PII, revocable continuity token is a de-anonymization pressure. | Security Architect | `carried:openwop.gap.0132.6` Out of scope here; own security pass if an adopter needs it. Tracked as Unresolved-Q2. | (none — carried forward) |
| G7 | §F Conformance | The five §F invariants + their tests are **specified but not landed** (RFC 0079 precedent: protocol-tier MUST-NOT lands with its public test at `Draft → Active`, not at Draft filing). At Draft, only the always-on `anonymous-actor-shape.test.ts` is committable without breaking `check-security-invariants.sh`. | Conformance Architect | `carried:openwop.gap.0132.7` Author the five behavioral scenarios + add the invariant rows in the `Draft → Active` PR (same PR, per the gate). | `Active` status |
| G8 | §D Audit | The RFC reuses `authorization.decided` (RFC 0049) rather than minting a new event, to avoid touching the `observability.md` closed-event vocabulary + a schema change. Confirm the existing event's `{principal, action, resource, allowed, reason}` shape carries an anon denial cleanly (it does: `action: "tool:<id>"`, `reason: "anon-not-granted"`). | Conformance Architect | `carried:openwop.gap.0132.8` `anonymous-actor-audit-opaque.test.ts` verifies; confirm no new `reason` enum is needed (reasons are free-form strings today). | Audit scenario finalization |
| G9 | §C.1 | Reference-host verification that the anon tool-catalog read (RFC 0078) genuinely fails **empty** (not a baseline) requires a host wiring a tool-enabled public surface. No public conformance host does yet. | Conformance Architect (+ reference host) | `carried:openwop.gap.0132.9` Deferred to reference-host implementation over openwop-app's existing gateway; behavioral scenarios soft-skip until then. | `Accepted` status |
| G10 | Compatibility | `owner.principalKind` on `run-snapshot.schema.json` — confirm the server-emitted-open vs member-declared-closure interaction (RFC 0094): the `owner` sub-object is `additionalProperties:false`, so the new property must be a *declared* member (it is), while the snapshot root stays open. | Schema Architect | `carried:openwop.gap.0132.10` Schema review at `Active`; `identity-owner-shape.test.ts` pattern already exercises `owner` closure. | Schema diff sign-off |
