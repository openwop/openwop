# RFC 0118 — Gap Register

Companion to `0118-parallel-subworkflow-fan-out-and-join.md`. Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner and a resolution path; gaps with no path are promoted to the Risk register.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|----|---------|--------------------------|-------|-----------------|--------|
| G1 | §B `onChildFailure` | Default is `'collect'`; should it be `'fail-fast'` to match single-child `core.subWorkflow` `fail-parent` default? (Mirrors UQ1.) | Spec Architect | Maintainer decision in the Draft comment window; survey the 3 motivating workloads (campaign / RFQ / batch) for which default surprises least. | `Active` status |
| G2 | §B `mode` | Is the `first` vs `race` split worth two enum values, or collapse `race` into `first` + `fail-fast`? (Mirrors UQ2.) | Spec Architect | Decision needed; if collapsed, `joinModes` capability drops `race`. | Schema diff finalization |
| G3 | §F capabilities | Exact placement of `fanOutPolicies` / `joinModes` / `maxFanOut` in `capabilities.md` — under existing `dispatch` object (assumed) vs a new sub-object. Need to read current `capabilities.md` dispatch section before finalizing the diff. | Schema Architect | Read `spec/v1/capabilities.md` + `schemas/host-capabilities.schema.json`; confirm `dispatch` object shape; align casing. | Schema/capabilities diff |
| G4 | §E events | `core.dispatch.fanOut` / `core.dispatch.join` event TYPE names — confirm they fit the `core.*` event-type naming convention and don't collide with existing `node.dispatched` / `core.workflowChain.event` names in `run-event-payloads.schema.json`. | Schema Architect | Grep `run-event-payloads.schema.json` + `asyncapi.yaml` for the `core.dispatch.*` namespace; pick non-colliding names; register message in AsyncAPI. | AsyncAPI validation (`asyncapi validate`) |
| G5 | §C `maxFanOut` | Default host ceiling value when `maxFanOut` is advertised — RFC suggests 16 by analogy to the Workflow concurrency cap; is there a host-capabilities precedent (`limits.maxNodeExecutions`) to align with? | Conformance Architect | Check `spec/v1/host-capabilities.md` limits section for an existing concurrency/breadth limit to mirror. | Reference-host implementation |
| G6 | §G replay | `mergeOrder` determinism under RFC 0063 attestation: when a child has `outputAttestation.requireApproval`, the approval suspends the parent mid-fan-out. Does an approval-gated child's terminal order in `mergeOrder` stay stable across replay if the human decides at a different wall-clock time? | Security Architect | Trace RFC 0063 §C interaction; likely add a note that `mergeOrder` is recorded at terminal-fold time (post-approval), not at child-completion time. | `Accepted` (replay scenario) |
| G7 | Conformance | Reference host: which of in-memory / SQLite / python hosts implements `fanOutSupported: true` first? RFC 0104 precedent required dual non-steward host evidence to reach `Accepted`. | Conformance Architect | Coordinate with openwop-app (motivating implementer) + one independent host; openwop-app commits to the SQLite-host parity leg. | `Accepted` status |
| G8 | §H `iterationCap` interaction | Does each parallel child count once against `iterationCap`, or the fan-out node once? (Mirrors UQ5.) | Spec Architect | Pin in prose before `Active`; proposed "one per child". | `Active` status |
| G9 | Motivation | The campaign workflow's conditional channel skip (`enabledChannels` contains X) is expressed in openwop-app via DAG edge conditions on the fork. At the wire level, conditional gating = the orchestrator selecting which `nextWorkerIds` to include. Confirm this RFC does NOT need a per-child predicate field, i.e. gating stays upstream in the `OrchestratorDecision`. | Spec Architect | Validate against RFC 0006 `next-worker` decision shape — confirm `nextWorkerIds` is freely chosen at decision time (no schema change needed here). | Scope confirmation |
