# RFC 0106 — Gap Register

Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner and a resolution path. A gap with no path is promoted to a Risk (see `.risks.md`).

**Deep-dive update (2026-06-23):** G1, G4, G5 are **RESOLVED** via a web + codebase research pass (4 agents: SDK streaming-type inspection; streaming-ASR API survey; live-stream-handle cross-protocol survey; live-audio threat-model). Resolutions folded into the RFC §B/§B.1/§F. G10 remains the gating item (author confirmation of the distillation).

| ID  | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | ------- | ------------------------ | ----- | --------------- | ------ |
| ~~G1~~ | §B Proposal | **RESOLVED → `AsyncIterable<TranscriptEvent>`.** TS SDK exposes streaming only as `AsyncGenerator` (`sdk/typescript/src/sse.ts` `streamEvents`, `client.runs.events()`); image/video/TTS = `Promise`. Native ASR SDKs are event-emitters, but consistency with the SDK's existing LLM-token streaming wins; backpressure is consumer-side buffering (browser WS has none, gRPC does) → bounded-queue host note in §B.1, not a wire signal. Event taxonomy enriched (`stability`, `committedPrefix`, `formatted`, candidate-vs-commit). | Schema Architect | DONE — §B updated. | — (resolved) |
| G2 | §A Proposal | Default values for `realtimeVoice` sub-flags are "absent ⇒ off" — but `turnDetection`/`bargeIn` with no `transcription` is nonsensical. Need a schema closure: `turnDetection`/`bargeIn` imply `transcription`. | Schema Architect | Add a JSON Schema `dependentRequired`/`if-then` closure + assert in the always-on shape scenario. Decision needed. | Schema diff finalization |
| G3 | §D Proposal | Whether `voice.*` events are emission-gated (clients ignore unknown) or flag-discoverable (`realtimeVoice.events`). Affects whether a client can pre-discover the taxonomy. | Spec Architect | Decide at `Active` (Unresolved §1). Lean emission-gated for the floor; add a discovery flag only if a consumer needs pre-session discovery. | `Active` |
| ~~G4~~ | §B / §E | **RESOLVED → a distinct `streamRef` kind (§B.1).** Unanimous cross-protocol precedent (WebRTC track+`readyState`, A2A Task/Artifact, LiveKit SID/egress, S3-key/presigned/Kafka-offset) makes live conduits a separate handle from stored bytes; overloading `mediaRef` re-introduces an `isLive` discriminator in all but name. `streamRef` = `readyState` lifecycle, no size/hash, session-bound + non-portable, `streamRef → mediaRef` finalize seam. | Spec + Schema Architect | DONE — §B.1 added. (Cross-cuts RFC 0091 Unresolved §1 + RFC 0019 — noted there as a follow-on.) | — (resolved) |
| ~~G5~~ | Security §F | **RESOLVED → §F + four `invariants.yaml` rows.** Threat pass confirmed live ingress is materially new (append-after-speech AudioJailbreak realizable only vs a continuous stream; VoiceJailbreak; DolphinAttack/inaudible; Whisper silence-hallucination; interim→final poisoning; barge-in cancellation leakage; never-committing-stream TDoS). Invariants: `voice-interim-not-durable`, `voice-transcript-untrusted`, `voice-bargein-no-partial-leak`, `voice-streamref-tenant-bound` — each with a conformance-detect recipe. | Security Architect | DONE — §F added. Coordinate with external auditor per `SECURITY/external-audit-engagement.md` at `Active` (continuous-ingress surface is new). | — (resolved; audit coordination at `Active`) |
| G6 | §E / Scale | Whether a `scale-profiles.md` voice-latency profile (first-audio-byte as an advisory target) is a separate RFC. Latency is non-normative here by design. | Compatibility Architect | Named follow-on RFC; not in 0106. Open a tracking item after 0106 `Active`. | Nothing (explicitly deferred) |
| G7 | Conformance | Reference host that will advertise `realtimeVoice` + pass gated scenarios non-vacuously is not yet identified/committed. RFC 0105 had openwop-app ADR 0086 as the named consumer; 0106 has only a *candidate* (openwop-app voice-chat over RFC 0005). | Conformance Architect | Confirm an openwop-app ADR commits to a voice-session feature pre-`Accepted`; otherwise RFC explicitly defers reference-host impl per template acceptance criterion. | `Accepted` (not `Active`) |
| G8 | §C | Streaming-synthesis chunk transport (inline base64 chunks vs `streamRef`-per-session vs one growing `mediaRef`). Inline is simplest but heavy at high chunk rates. | Schema Architect | Benchmark/decide at `Active` (Unresolved §5). | `Active` (locks §C chunk shape) |
| G9 | §D / RFC 0101 | Per-speaker turn attribution (`subject` on `voice.turn_commit`) for multi-party voice rooms. | Spec Architect | Coordinate with RFC 0101; either pin an optional `subject?` now (cheap, additive) or defer to a 0101-voice follow-on. Decide at `Active`. | Multi-party voice (not the single-speaker floor) |
| **G10** | Intake | **(GATING)** The source is a **research survey**, not a pinned design. §A–§F are this skill's distillation + the deep-dive resolutions (G1/G4/G5), not the author's committed design. Author must confirm the four Draft resolutions (`AsyncIterable` return, `streamRef` handle, enriched `TranscriptEvent`, the four §F invariants) match intent. | Spec Architect (author / David) | Author review before `Draft → Active`. | `Active` (the lock) |
</content>
