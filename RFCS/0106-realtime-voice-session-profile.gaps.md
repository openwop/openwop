# RFC 0106 — Gap Register

Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner and a resolution path. A gap with no path is promoted to a Risk (see `.risks.md`).

**Deep-dive update (2026-06-23):** G1, G4, G5 are **RESOLVED** via a web + codebase research pass (4 agents: SDK streaming-type inspection; streaming-ASR API survey; live-stream-handle cross-protocol survey; live-audio threat-model). Resolutions folded into the RFC §B/§B.1/§F.

**`Draft → Active` update (2026-06-23):** the 7-day additive comment window was waived per GOVERNANCE.md lazy consensus and the **gating G10 is RESOLVED** (author confirmed the four Draft resolutions). Because `Active` locks the wire shape, the `Active`-gated opens are all resolved in-RFC at the flip: **G2, G3, G8, G9 RESOLVED** (see RFC §"Resolved questions"), **G6** filed as a named post-`Active` follow-on tracking item (not in 0106). The only gap that survives to `Active → Accepted` is **G7** (a named reference host committing to advertise `realtimeVoice`) — that is the Accepted gate, now in active coordination with openwop-app.

| ID  | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | ------- | ------------------------ | ----- | --------------- | ------ |
| ~~G1~~ | §B Proposal | **RESOLVED → `AsyncIterable<TranscriptEvent>`.** TS SDK exposes streaming only as `AsyncGenerator` (`sdk/typescript/src/sse.ts` `streamEvents`, `client.runs.events()`); image/video/TTS = `Promise`. Native ASR SDKs are event-emitters, but consistency with the SDK's existing LLM-token streaming wins; backpressure is consumer-side buffering (browser WS has none, gRPC does) → bounded-queue host note in §B.1, not a wire signal. Event taxonomy enriched (`stability`, `committedPrefix`, `formatted`, candidate-vs-commit). | Schema Architect | DONE — §B updated. | — (resolved) |
| ~~G2~~ | §A Proposal | **RESOLVED → apply the closures.** `turnDetection`/`bargeIn` ⇒ `transcription` and `synthesis` ⇒ `speechSynthesis` are adopted as JSON Schema `dependentRequired`/`if-then` closures on `aiProviders.realtimeVoice`, asserted by the always-on shape scenario. The schema diff itself lands with the normative surface at `Active → Accepted`. | Schema Architect | DONE — decision locked at `Active`; surface sequenced to Accept. | — (resolved) |
| ~~G3~~ | §D Proposal | **RESOLVED → emission-gated.** Floor emits `voice.*` events; clients ignore unknown types. No `realtimeVoice.events` discovery flag in v1; a pre-session discovery flag is a future additive sub-flag if a consumer needs it. | Spec Architect | DONE — Unresolved §1 resolved at `Active`. | — (resolved) |
| ~~G4~~ | §B / §E | **RESOLVED → a distinct `streamRef` kind (§B.1).** Unanimous cross-protocol precedent (WebRTC track+`readyState`, A2A Task/Artifact, LiveKit SID/egress, S3-key/presigned/Kafka-offset) makes live conduits a separate handle from stored bytes; overloading `mediaRef` re-introduces an `isLive` discriminator in all but name. `streamRef` = `readyState` lifecycle, no size/hash, session-bound + non-portable, `streamRef → mediaRef` finalize seam. | Spec + Schema Architect | DONE — §B.1 added. (Cross-cuts RFC 0091 Unresolved §1 + RFC 0019 — noted there as a follow-on.) | — (resolved) |
| ~~G5~~ | Security §F | **RESOLVED → §F + four `invariants.yaml` rows.** Threat pass confirmed live ingress is materially new (append-after-speech AudioJailbreak realizable only vs a continuous stream; VoiceJailbreak; DolphinAttack/inaudible; Whisper silence-hallucination; interim→final poisoning; barge-in cancellation leakage; never-committing-stream TDoS). Invariants: `voice-interim-not-durable`, `voice-transcript-untrusted`, `voice-bargein-no-partial-leak`, `voice-streamref-tenant-bound` — each with a conformance-detect recipe. | Security Architect | DONE — §F added. Coordinate with external auditor per `SECURITY/external-audit-engagement.md` at `Active` (continuous-ingress surface is new). | — (resolved; audit coordination at `Active`) |
| G6 | §E / Scale | Whether a `scale-profiles.md` voice-latency profile (first-audio-byte as an advisory target) is a separate RFC. Latency is non-normative here by design. | Compatibility Architect | Named follow-on RFC; not in 0106. Tracking item to open post-`Active`. | Nothing (explicitly deferred) |
| **G7** | Conformance | **(now the sole open gap — the `Active → Accepted` gate.)** Reference host that will advertise `realtimeVoice` + pass gated scenarios non-vacuously is not yet *committed*. RFC 0105 had openwop-app ADR 0086 as the named consumer; 0106 has a candidate (openwop-app voice-chat over RFC 0005), now in active coordination via the `0106` crosstalk bus. | Conformance Architect | Confirm an openwop-app ADR commits to a voice-session feature pre-`Accepted`; otherwise RFC explicitly defers reference-host impl per template acceptance criterion. | `Accepted` (not `Active`) |
| ~~G8~~ | §C | **RESOLVED → inline base64 chunks (floor).** §C returns base64 chunks inline over the async stream; a `streamRef`-per-session or a growing `mediaRef` is a future additive transport option for high chunk rates, advertised separately. | Schema Architect | DONE — Unresolved §5 resolved at `Active`. | — (resolved) |
| ~~G9~~ | §D / RFC 0101 | **RESOLVED → defer; floor is single-speaker.** No `subject` field on `voice.turn_commit` in the floor; because `voice.*` payloads are server-emitted-open (RFC 0094) an optional `subject?` can be added additively in a 0101-voice follow-on with no breaking flip. | Spec Architect | DONE — Unresolved §3 resolved at `Active`. | — (resolved) |
| ~~G10~~ | Intake | **RESOLVED.** Author confirmed the four Draft resolutions (`AsyncIterable` return, `streamRef` handle, enriched `TranscriptEvent`, the four §F invariants) match intent at the `Draft → Active` waiver. | Spec Architect (author / David) | DONE — author review completed at the flip. | — (resolved) |
</content>
