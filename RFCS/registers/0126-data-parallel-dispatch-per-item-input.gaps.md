# RFC 0126 — Gap Register

Companion to `0126-data-parallel-dispatch-per-item-input.md`. Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each has an owner + a resolution path; a gap with no path is promoted to a Risk.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §1 | Merge precedence on key collision between the `inputMapping` projection and `nextWorkerInputs[i]` — override (proposed), merge-under, or reject-on-overlap. | Spec Architect | `carried:openwop.gap.0126.1` Decision + a conformance case pinning it (scenario assertion 3). | `Active` |
| G2 | Proposal §2 | Capability name: `capabilities.dispatch.perItemInput` vs `dispatch.dataParallel`. Confirm the exact JSON location — RFC 0118 introduced a `capabilities.dispatch.{fanOutSupported,joinModes,maxFanOut}` object; the new boolean slots there, distinct from the `capabilities.agents.dispatch` existence flag. | Schema Architect | `carried:openwop.gap.0126.2` Read `capabilities.schema.json` `dispatch` object added by RFC 0118; place the new boolean beside `fanOutSupported`. | Schema diff finalization |
| G3 | Proposal §1 | `nextWorkerInputs[i]` value constraints — arbitrary nested JSON (proposed) vs flat scalar map, and whether a per-item depth/size cap is needed for the bounded-work guarantee. | Security Architect | `carried:openwop.gap.0126.3` Decide against the resource-exhaustion threat (a supervisor could emit huge per-item payloads × N). Likely a documented soft cap. | `Active` |
| G4 | Conformance | Length-mismatch and unadvertised-capability failures are runtime `validation_error`s (the decision is runtime). Confirm no registration-time (`POST /v1/workflows`) signal is expected — the emitting node's typeId is not known-data-parallel at registration. | Conformance Architect | `carried:openwop.gap.0126.4` Confirm with the `dispatchLoop` / `orchestratorDispatch` scenario authors; document as runtime-only. | `Active` |
| G5 | Proposal / Replay | Interaction with `mockDispatchPlan` (RFC 0022 §Unresolved #6, `config.mockDispatchPlan` on `core.orchestrator.supervisor`) — does the mock-plan seam need a per-item-input field so conformance can drive data-parallel without a real supervisor? | Conformance Architect | `carried:openwop.gap.0126.5` Extend the mock-plan fixture shape if the harness can't otherwise emit `nextWorkerInputs`. | Scenario authoring |
| G6 | Acceptance | Python reference host — implement now or defer? in-memory + sqlite are the `Accepted` gate; python may lag. | Compatibility Architect | `carried:openwop.gap.0126.6` Decide at `Active`; if deferred, record the INTEROP-MATRIX cell as `—` with a tracking issue. | `Accepted` |
| G7 | Proposal | Does `nextWorkerInputs` interact with `askUserRouting` / `terminate` decisions? No — it is defined ONLY on `NextWorkerDecision`. Confirm the schema `oneOf` keeps it off the other two kinds. | Schema Architect | `carried:openwop.gap.0126.7` Schema review; the field lives only in the `NextWorkerDecision` branch. | Schema diff finalization |

## Sweep at `Accepted` (2026-07-04)

Register-sweep gate per `RFCS/README.md` §Process (an `Accepted` RFC MUST have every open row closed, transferred, or explicitly carried forward as a named open gap). Single-witness graduation (openwop-app PR #1278) under the bootstrap steward waiver.

| ID | Disposition | Evidence / carry-forward home |
|---|---|---|
| G1 | **RESOLVED** | `carried:openwop.gap.0126.1` Per-item overrides on key collision. Pinned in `node-packs.md §core.dispatch per-item input`, conformance assertion 2, and the openwop-app witness (test 2). |
| G2 | **RESOLVED** | `carried:openwop.gap.0126.2` Name = `capabilities.dispatch.perItemInput`, landed as a **prose** descriptor in `capabilities.md §dispatch` (prose-only `dispatch.*` family); no `capabilities.schema.json` diff — the wire-honest home for a descriptor the schema does not enumerate. |
| G3 | **CARRIED FORWARD** | `carried:openwop.gap.0126.3` No hard per-item depth/size cap; arbitrary nested JSON permitted. Bounded by existing decision-event size limits + `maxFanOut`/`maxConcurrency`/`iterationCap` (count). Named in RFC §Unresolved Q3; = risk R2. A later cap is additive. |
| G4 | **RESOLVED** | `carried:openwop.gap.0126.4` Runtime-only `validation_error` (length-mismatch + unadvertised-capability); no registration-time signal possible (node `typeId` not known-data-parallel at `POST /v1/workflows`). RFC §Unresolved Q4; witness tests 3–4. |
| G5 | **CARRIED FORWARD** | `carried:openwop.gap.0126.5` `mockDispatchPlan` per-item seam for the *server-free* conformance harness. The current single witness drives the real `core.dispatch` node via openwop-app's own test; the server-free behavioral legs use the `POST /v1/host/sample/dispatch/per-item` seam (soft-skip until wired). Extending the mock-plan fixture to emit `nextWorkerInputs` without a live host is a follow-on for the conformance suite (tracked). |
| G6 | **CARRIED FORWARD** | `carried:openwop.gap.0126.6` Python + in-memory/sqlite reference hosts deferred — not required for single-witness graduation (openwop-app is implementer #1). INTEROP-MATRIX cells stay `—` until they implement; = risk R4. |
| G7 | **RESOLVED** | `carried:openwop.gap.0126.7` `nextWorkerInputs` lives only in the `NextWorkerDecision` branch of the `orchestrator-decision.schema.json` `oneOf`; `AskUserDecision`/`TerminateDecision` (both `additionalProperties:false`) reject it. |
