# RFC 0063 — Gap Register

Companion to [`RFCS/0063-subrun-output-attestation-and-merge-gating.md`](../0063-subrun-output-attestation-and-merge-gating.md). Verdict from the reconciliation audit: **reframed** — surface the checksum via an additive `attestation` field on the existing `core.workflowChain.event { phase: "output.harvested" }` (RFC 0037) instead of a new `subRun.attested` event. The approval gate (RFC 0051), `principalScope` (RFC 0049), and checksum canonicalization (RFC 0041 JCS) compose cleanly.

| ID  | Section | Question / Missing Input                                                                                                                                                                                                                                                 | Owner              | Resolution Path               | Blocks   |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ----------------------------- | -------- |
| G1  | §B      | **Reframe applied:** checksum rides `core.workflowChain.event` `output.harvested` phase via an additive `attestation { checksum, algorithm }` field — NOT a new `subRun.attested` event. Confirm RFC 0037 author accepts the additive field on `coreWorkflowChainEvent`. | Spec Architect     | Confirm with RFC 0037.        | Active   |
| G2  | §B.1    | Checksum canonicalization MUST reuse the RFC 8785 JCS recipe pinned in `replay.md` (RFC 0041) so a cross-host child's checksum verifies.                                                                                                                                 | Schema Architect   | Confirm the recipe reference. | Active   |
| G3  | §C      | Partial fan-out approval: N children with `requireApproval` → N interrupts or one batched? Proposed one-per-child for v1.                                                                                                                                                | Spec Architect     | Decision before Active.       | Active   |
| G4  | §D      | `principalScope` references RFC 0049 scopes (no new scopes defined here) — confirm the scope-narrowing grammar aligns with RFC 0049's matching rules.                                                                                                                    | Security Architect | Confirm with RFC 0049.        | Active   |
| G5  | Impl    | `subWorkflowDispatcher.ts` harvest step populates the `output.harvested` event's `attestation` + routes through the existing suspend/interrupt path when `requireApproval`.                                                                                              | Implementer        | Reference wiring.             | Accepted |
