# RFC 0216 — Gap register

Open design gaps for RFC 0216 (a colocated companion bundle is marked, and witnesses only the harness-trust-anchor rows). Each row is keyed to the RFC and has an owner and a resolution path. Rows G1, G2 and G5 were raised against the Draft's `witnessed-colocated` disposition. They were settled when that disposition was struck at `Active` on 2026-09-26.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §C.10 | The committed companion `openwop-app-colocated-companion.json` is unmarked, `commit`-kind, and signed with a throwaway key, and the gate counts all of its rows. | Conformance Architect | `closed` 2026-09-26 — §C.10 names it by `witnessSha256` and treats it as marked, so it fails pairing and witnesses nothing. It stays committed as history. No `Accepted` RFC loses a witness (measured). | — |
| G2 | §C.9(c) | Whether a companion must be signed under a key the production host publishes, or whether a citation by digest from P is enough. | Security Architect | `closed` 2026-09-26 — with no citation from P (the disposition was struck), the only attribution left is the key. §C.9(c) requires P's published key. | — |
| G3 | §C.9(d) / Unresolved 1–2 | Discovery equivalence covers only what discovery exposes, and the normalisation set has not been measured on a real production/companion pair of one image. | Conformance Architect | `transferred:rfc-0216` — Unresolved questions 1–2. Measured when the first real pair exists (G4). A needed extension is an amendment under its own window. | Nothing in the mechanism. |
| G4 | Acceptance (deferred box) | openwop-app does not yet record its production image digest in `host.build`, and advertises no `oidc` lane at v2 in production. Both need a production deploy, which waits on the steward's `gcloud` re-auth. | Steward | `externally-gated:openwop-app-production-deploy` | The deferred end-to-end box only. |
| G5 | §A | `auth-oidc-user-bearer.test.ts` reads the issuer and is not on the list. | Conformance Architect | `closed` 2026-09-26 — it runs at major 1 only (`conformance/scenario-majors.json`) and never produces a bundle v3 row. | — |
| G6 | Alternatives 2 | Nothing binds the served digest to the served surface. `host.build` is the operator's assertion, as for every bundle. `implementation.build` was struck under the RFC 0147 §A freeze on new optional wire members. | Security Architect | `transferred:rfc-0147-freeze` — a successor RFC once the freeze lifts. Cryptographic binding (SLSA provenance, a platform attestation) is out of scope. | Nothing in this RFC. |
| G7 | §D | The premise that a served host must not trust a harness issuer was only an openwop-app ADR (0745 D4). | Spec Architect | `closed` 2026-09-26 — §D states it normatively in conformance.md §"Bundle v3". | — |
