# RFC 0216 — Gap register

Open design gaps found while authoring RFC 0216 (the `witnessed-colocated` disposition for harness-trust-anchor rows). Rows are keyed to the RFC. Each has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §Motivation 3 / Unresolved question 6 | The committed companion `evidence/v2-host-bundles/openwop-app-colocated-companion.json` is unmarked, records `host.build.kind: "commit"` rather than the image digest its matrix note cites, and is signed with a throwaway key published only in its own loopback discovery. `check-accepted-predicate` counts all of its `executed-pass` rows today, including four public-surface RFC 0200 rows it could not have witnessed through the served front. No Accepted RFC rests on it alone (measured: the predicate stays green with it removed), and RFC 0213's `Updated` field cites it as a second witness. | Conformance Architect | `transferred:rfc-0216` — an acceptance box. At `Active`, re-cut it to §C's shape (image digest, production key, `host.deployment`), or relabel it in `INTEROP-MATRIX.md` and leave it as historical. The decision is Unresolved question 6. | The `Active → Accepted` flip. |
| G2 | §C.10(e) / Unresolved question 1 | Whether a companion must be signed under a key the production host publishes (option A, this draft), or whether P's signature over the cited `companionWitnessSha256` is enough (option B, incorporation by digest). | Security Architect | `transferred:rfc-0216` — decided in the comment window, before `Active`. | `Draft → Active`. |
| G3 | §C.10(g) / Unresolved question 4 | Discovery equivalence catches only the configuration discovery exposes. Two instances of one image can differ by an environment flag that never appears in discovery. Whether to require a declared configuration diff is undecided, and the substitutable set (`oidc` lane `issuers` only) has not been measured against a real production/companion pair of documents. | Conformance Architect | `transferred:rfc-0216` — the pair is measured when the first production cut after the §D deploy exists; the diff question is decided in the comment window. | `Draft → Active` (the decision); `Accepted` (the measurement). |
| G4 | §Implementation notes | openwop-app does not yet serve `implementation.build`, and advertises no `oidc` lane at v2 in production. Both need a production deploy, and deploys currently wait on the steward's `gcloud` re-auth. | Steward | `externally-gated:openwop-app-production-deploy` — no corpus work is owed before the deploy. | The end-to-end acceptance box. |
| G5 | §Motivation 2 | `auth-oidc-user-bearer.test.ts` also reads `OPENWOP_TEST_OIDC_ISSUER_URL`, and is not on the list. | Conformance Architect | `closed` 2026-09-26 — it runs at major 1 only (`conformance/scenario-majors.json`), so it never produces a bundle v3 row, and v1 bundles have no bundle-wide `blocked` rule for this RFC to except. | — |
| G6 | §D.13 | `implementation.build` is an operator's claim on the served surface, not an attestation. A container cannot contain its own digest. Binding the served digest cryptographically (SLSA provenance, a platform attestation) would remove the residual trust §D.13 names. | Security Architect | `transferred:rfc-0216` — Unresolved question 3 records it. If wanted, a successor RFC owns it, since it needs a verifier for a third-party attestation format. | Nothing in this RFC. |
| G7 | §A.1 / Unresolved question 5 | The premise that a production host SHOULD NOT trust a harness issuer is an openwop-app ADR (0745 D4), not a corpus statement. | Spec Architect | `transferred:rfc-0216` — Unresolved question 5; decided in the comment window. | Nothing in this RFC's mechanism. |
