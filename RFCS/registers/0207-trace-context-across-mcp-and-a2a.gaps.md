# RFC 0207 — Gap register

Open design gaps found while authoring RFC 0207 (trace context across MCP and A2A; debug-bundle spans; optional `mcp.*` projection). Each row has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §Compatibility, §A | **Which carrier does MyndHyve emit today?** MyndHyve is the only host advertising `multiAgent.executionModel.version >= 3`. | Compatibility Architect | `closed` — answered 2026-09-22 by a read-only read of MyndHyve's source (steward decisions log D4): it injects W3C `traceparent` as an outbound HTTP header (`packages/workflow-engine/src/services/ExternalApiCallWrapper.ts:178`; `nodes/conformance/mcpInvoke.node.ts`). The RFC therefore keeps the HTTP header a conforming carrier, makes `params._meta` / `Message.metadata.openwop` SHOULD-also, and the rewritten scenario accepts either carrier and fails when neither is present. It stays additive; no safety-fix. | nothing |
| G2 | §C.11 | `schemas/v2/debug-bundle.schema.json` is an orphan: there is no v2 operation, no path-manifest entry and no major-2 scenario. The `production` family still declares a `debugBundle` facet with no read behind it. | Conformance Architect | `transferred:/cleanup`: delete the orphan schema, or move it under an `ext/` home if a v2 debug read is planned. The facet question goes to whoever homes `production` (a v1-dependent family). RFC 0207 adds nothing to either. | nothing |
| G3 | v2 text | **`multiAgent` is not homed.** The v2 carrier text is written in `interop.md` (the `a2a`/`mcp` home), not as `multiAgent` v2 text. Homing `multiAgent` means porting a 5,507-word v1 document. A small stub would claim the RFC 0190 budget grant without carrying the family's contract. | Spec Architect | `transferred:scripts/check-v2-normative-home.mjs` — Stays open on the RFC 0189 burn-down: `multiAgent` remains V1-DEPENDENT. The v1 edit this RFC makes to `multi-agent-execution.md` binds v2 `multiAgent` through its v1 normative home until that port happens. | nothing |
| G4 | Conformance | No v2 host drives an outbound MCP tool call or A2A message from a run against the suite's fake peers. v2-reference advertises `a2a`/`mcp` (seam-gated), but its tree has no `traceparent` handling, and no v2 fixture equivalent to `conformance-mcp-tool-roundtrip` is known to exist. | Conformance Architect | `externally-gated:v2-reference-trace-fixture` — Host work on v2-reference: a seam fixture, plus `_meta` / `metadata` injection. Until it lands, `v2-interop-trace-context.test.ts` records `blocked`, naming the missing fixture. | `Accepted` — *amended 2026-09-22:* a v1 row can never satisfy `check-accepted-predicate` rule 4, so the v2 witness is the Accepted bar (`review/arch-impl.md` R3) |
| G5 | §C | No committed host emits a non-empty debug-bundle `spans[]`: all three v1 reference hosts return `[]`. `.debug-bundle-span-trace` is therefore `inapplicable` everywhere today. | Conformance Architect | `externally-gated:host-emits-debug-spans` — Host work: the sqlite reference host, or openwop-app (which already has `middleware/traceContext.ts`), populates `spans[]` from its tracer. *Amended 2026-09-22:* §C is v1-only and non-gating (§C.11), so a v1 run on such a host is documented, un-gated evidence; the Accepted bar no longer requires it. | nothing |
| G6 | §D | Upstream `mcp.session.id` is still defined against 2025-06-18 sessions, and upstream issue `open-telemetry/semantic-conventions-genai#437` is realigning it (review slice D F3). The §D MUST NOT on emitting it for 2026-07-28 exchanges may need revisiting if upstream redefines the attribute. | Spec Architect | `externally-gated:otel-semconv-mcp-stability` — Re-read upstream when `mapping_version` bumps. `semconv_ref` pins the commit a host projected against, so a stale projection is detectable. | nothing |
| G7 | §A.3 | The inbound (host-as-MCP-server) leg needs an MCP server mount **and** OTLP export reaching the suite. No v2 host advertises `mcp.serverMount` today (architect P3), and hosts in a separate network namespace cannot call back (`REQUIRES_HOST_CALLBACK`). | Conformance Architect | `closed` — *Amended 2026-09-22:* the gating leg runs in `v2-interop-trace-context` against the v2 mount RFC 0208 builds; where OTLP cannot reach the suite it records `inapplicable` with that reason, which accounts for the declared row. Not a pass requirement of the Accepted bar. | nothing |
| G8 | Dependency | Phase 1 P1-C corrects `debug-bundle.md`'s "16-byte hex" `spanId` prose in the same bullet list that §C.9 edits. | Spec Architect | `closed` — Land RFC 0207 after P1-C, or merge `origin/main` before editing. Do not re-fix the byte count here. | merge order only |
