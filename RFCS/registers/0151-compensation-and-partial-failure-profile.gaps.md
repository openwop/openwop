# RFC 0151 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §C | Dependency-graph inclusion in profile v1 is undecided. | Spec Architect | `carried:openwop.gap.0151.1` Prototype against three multi-effect fixtures. | Active — **Sweep 2026-08-16:** **Decided for v1:** `reverse-completion` is the floor; dependency-graph ordering MAY be advertised (`compensation.md` §C `orderingModels`); no fixture prototype ran. |
| G2 | §D | ~~Exact run response carrying `compensationStatus` is unsettled.~~ **Closed 2026-08-16 (#1007):** `RunSnapshot` (`schemas/run-snapshot.schema.json`, `GET /v1/runs/{runId}`) is the sole owner. Traced: the run list has no summary schema of its own, and `debug-bundle.schema.json` + AsyncAPI `run.snapshot` reuse the snapshot by `$ref`, so one property covers every surface that returns a run; the six §D events already carry the transitions and do not need the rollup. OPTIONAL, capability-gated (non-advertising host MUST omit; advertising host MUST carry, `none` when idle), and the value is the normative fold in `spec/v1/compensation.md` §"Run rollup". Not settled by this: the value on a forked run of a partially compensated source (`compensation.md` G4). | Schema Architect | `carried:openwop.gap.0151.2` Trace run document/status/result schemas and choose one source. | ~~Active~~ Closed |
| G3 | §E | Operator substitution authorization and plan-version rules are open. | Security Architect | `carried:openwop.gap.0151.3` Threat-model and define closed override actions. | Active — **Sweep 2026-08-16:** **Carried** (`compensation.md` G2 — §E operator actions named, authorization/plan-version rules not written). **Closed 2026-08-16 (later the same day):** `compensation.md` §E — decisions bind tenant + principal + action + `planVersion`; substitution increments `planVersion` and voids prior approvals (UQ2 = no); the four override actions are a closed table with recorded outcome + rollup effect; every override audited via `authorization.decided`. Threat model `SECURITY/threat-model-compensation.md` A3/A4. Endpoints for the actions remain host-mediated (`compensation.md` G7). |
| G4 | §B | Irreversible-effect declaration vocabulary is absent. | Spec Architect | `carried:openwop.gap.0151.4` Add advisory/required declaration in child revision. | Accepted — **Sweep 2026-08-16:** **Carried** — no irreversible-effect declaration in `compensation-policy.schema.json` or the node declaration. |
| G5 | Conformance | Real-service emulator choice is unset. | Conformance Architect | `carried:openwop.gap.0151.5` Use local transactional HTTP emulator with deterministic failures. | Accepted — **Sweep 2026-08-16:** **Carried** — no host advertises `compensation.supported`; the legs are `blocked` (RFC 0148 §A); no emulator chosen. |

