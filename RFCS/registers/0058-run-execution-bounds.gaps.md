# RFC 0058 — Gap Register

Companion to [`RFCS/0058-run-execution-bounds.md`](../0058-run-execution-bounds.md). Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions.

> **Location note:** placed under `RFCS/registers/` (not `RFCS/0058-*.gaps.md`) because the protocol-status RFC counter (`scripts/generate-protocol-status.mjs`, regex `^RFCS/\d{4}-.+\.md$`) would otherwise miscount a `RFCS/0058-*.gaps.md` file as an RFC and break the README count gate. `RFCS/registers/` does not match the regex.

| ID  | Section        | Question / Missing Input                                                                                                                                                                                 | Owner                   | Resolution Path                                                                                                                 | Blocks                  |
| --- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| G1  | Proposal §A    | Soft vs. hard timeout — does `runTimeoutMs` let an in-flight node finish or hard-cancel mid-node?                                                                                                        | Spec Architect          | Decision needed; proposed hard-cancel + flush event log first. Decide before `Active`.                                          | `Active` status         |
| G2  | Proposal §A    | Iteration counting under nested orchestration (RFC 0007 sub-orchestrator) — does a child's loop count against the parent's `maxLoopIterations`?                                                          | Spec + Compatibility    | Confirm against RFC 0037 Phase 2–4; proposed per-run counter.                                                                   | `Active` status         |
| G3  | Compatibility  | Is adding `run-duration` / `loop-iterations` to the `capBreached.kind` enum a per-event-version concern under `version-negotiation.md`, or is the `wasm-*` precedent (no bump) authoritative?            | Compatibility Architect | Confirm with maintainer; proposed no bump per RFC 0008 §K precedent.                                                            | `Active` status         |
| G4  | Schema §B      | `maxRunDurationMs` minimum is set to 1000ms in the draft — is a 1s floor right, or should sub-second deadlines be allowed for cheap synchronous runs?                                                    | Schema Architect        | Decision needed; low-risk default.                                                                                              | Schema finalization     |
| G5  | Conformance    | Deterministic-clock seam reuse — the RFC reuses RFC 0052's `scheduling/tick` seam for timeout reproducibility. Confirm that seam is generic enough for a wall-clock deadline (vs. only cron ticks).      | Conformance Architect   | Verify seam shape; may need a `clock/advance` seam variant.                                                                     | Conformance scenarios   |
| G6  | Implementation | `apps/workflow-engine` executor `while(true)` drain loop has no deadline/iteration counter today — exact insertion point + cancellation-path interaction unverified at spec time.                        | Implementer             | Verify during reference wiring; `executor.ts` already emits `cap.breached{kind:'node-executions'}` so the emission site exists. | `Accepted` status       |
| G7  | Proposal §D    | `details.elapsedMs` (run_timeout) vs `details.iteration` (loop_limit_exceeded) — confirm these `details.*` keys don't collide with existing error-envelope `details` conventions in `rest-endpoints.md`. | Schema Architect        | Cross-check error-envelope schema.                                                                                              | Error-code registration |
