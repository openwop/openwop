# RFC 0067 — Risk register

| ID  | Risk                                                                                                                    | Likelihood | Impact | Score | Mitigation                                                                                                      | Owner                   | Status |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ---------- | ------ | ----- | --------------------------------------------------------------------------------------------------------------- | ----------------------- | ------ |
| R1  | Hosts advertise `authModes` inconsistently with `byok` (e.g., `apiKey` for a provider not in `byok`), confusing clients | M          | M      | Med   | §B normative consistency rules + `byok-auth-modes.test.ts` cross-field assertions                               | Conformance Architect   | Open   |
| R2  | A client treats an `oauth-*` mode as if it could pass key material on `ai.credentialRef`, leaking a token onto the wire | L          | H      | Med   | §B rule 4: `oauth-*` credentials referenced by RFC 0046 `ref`, never on `ai.credentialRef`; redaction unchanged | Security Architect      | Open   |
| R3  | Provider-id vocabulary (§C) drifts anyway because it is non-normative                                                   | M          | L      | Low   | Recommended-but-not-required by design; clients tolerate unknown ids; revisit if drift hurts interop            | Spec Architect          | Open   |
| R4  | Closed enum (`apiKey`/`oauth-pkce`/`oauth-device`/`none`) too narrow for a future auth shape                            | L          | M      | Low   | Enum grows additively via RFC; consumers MUST ignore unrecognized modes                                         | Compatibility Architect | Open   |
