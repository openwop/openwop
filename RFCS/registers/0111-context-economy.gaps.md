# RFC 0111 — Gap Register

| ID  | Section     | Question / Missing Input                                                                                  | Owner                 | Resolution Path                                                                 | Blocks            |
| --- | ----------- | -------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------- | ----------------- |
| G1 | Proposal | `tokenCounter` enum vs provider-native tokenizers — does a portable enum diverge too far from real cost? | Spec Architect | `closed` 2026-09-18 — the tree answered it: `schemas/capabilities.schema.json` ships `tokenCounter` as `o200k_base | cl100k_base | chars | host-defined`, which is this row's own stated default with its escape hatch. RFC 0113 shares the vocabulary. | Schema finalize |
| G2 | Conformance | Transcript seam: host-sample-only seam vs a normative read endpoint | Conformance Architect | `closed` 2026-09-18 — decided by construction: it shipped as a host-sample seam (`host-sample-test-seams.md`) with `context-budget-transcript-bound.test.ts`, and no normative REST surface was added. That is the answer the row proposed. | Active→Accepted |
| G3 | Proposal | Replay determinism of host summaries — must `summaryRef` artifact be byte-stable across replays? | Security/Spec | `closed` 2026-09-18 — answered normatively in three places (`run-event-payloads.schema.json` at v1 AND v2, `multi-agent-execution.md` §Context economy): replay MUST reuse the recorded `summaryRef` and MUST NOT re-summarize. The answer is "not byte-stable — reused", which is a better answer than the question's framing. | Accepted |
| G4 | Proposal | Verbatim floor unit: `keepLastTurns` (count) vs `keepLastTokens` (tokens) | Spec Architect | `closed` 2026-09-18 — decided by what shipped: `keepLastTurns` is in `capabilities.schema.json` at v1 and v2, `keepLastTokens` exists nowhere in the corpus except this gap row, and the verbatim-floor MUST is written in `multi-agent-execution.md`. | Reference host |
| G5 | Conformance | No host with a real orchestrator loop advertises `contextBudget` yet | Conformance Architect | `externally-gated:real-orchestrator-host` — retargeted 2026-09-26. It read `non-steward-host`, which misstated the tripwire: the RFC's header and GOVERNANCE's evidence tiers accept a tier-1 or tier-2 host, so a steward-affiliated host that runs **real orchestrator-loop model turns** (not a mock supervisor, §Scope) and certifies both scenarios against the live fixture `conformance-context-budget-live` closes this row. As written it said nothing a steward does could un-park the RFC, which was false. First candidate: MyndHyve (tier-2), whose supervisor is today mock-driven. (2026-09-18: first dispositioned `externally-gated:non-steward-host`; precedent RFC 0147 G20, RFC 0156 G1/G4, RFC 0170 G2.) | Accepted flip |
