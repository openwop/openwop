# RFC 0121 — Gap Register

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | Motivation / Unresolved Q1 | No confirmed provider terms-of-service permits API-shaped third-party automation under a reused consumer-subscription login, for any vendor. | Spec Architect | `externally-gated:provider-tos-clearance` 2026-09-18 — **unownable by design, and re-tokened rather than left reading as open work.** It is a legal question about third parties' contracts: no artifact in any repository answers it, no design work moves it, and this RFC's header already rules it a standing decision rather than an action item. A new tripwire name is used deliberately — `unspecified` would erase the one precise thing about this gap. This IS the un-parking tripwire — not just a gap, it is the gating condition for the flip. **Target corrected 2026-09-19:** this row named `Draft (Parked) → Active`, which the RFC has already made — its header reads `Active` (**Parked**) and its `Updated` field records 6 of 8 `Accepted` criteria met, with the remaining two hinging on this question. The register was describing a transition that had already happened, so a reader checking whether the tripwire had fired would have measured the wrong flip. | `Active (Parked) → Accepted` |
| G2 | Proposal §C / Unresolved Q2 | Whether the acquisition mechanism is direct API calls under a borrowed session (ToS-risky, cheap) or an official-client subprocess harness (ToS-safer, architecturally distinct — closer to RFC 0035 than to `ctx.callAI`) is unresolved. | Security Architect + Spec Architect | `closed` 2026-09-18 by ruling — the architectural question is independent of the legal one and is decided: if ever implemented, the mechanism is an official-client subprocess harness under a follow-up RFC extending RFC 0035; direct API calls under a borrowed session are out of scope for this RFC. The reference host already shipped consistently with that (acquisition-mechanism-only, with a drift guard asserting no login code). Recommend the subprocess-harness shape if pursued; likely needs its own follow-up RFC extending RFC 0035 rather than reusing this RFC's wire label for the mechanism itself. | Any implementation/ADR start |
| G3 | Schema §A | Whether `subscription`-mode credentials need their own dedicated schema shape beyond reusing `host.credentials` `scope: "user"`, or whether the existing RFC 0046 credential-reference shape is sufficient as-is. | Schema Architect | `closed` 2026-09-18 — answered by what shipped: no new shape was minted. `capabilities.md` §B.8 binds to `host.credentials` (RFC 0046) with `scope: "user"`, the SECURITY invariant names that shape, and the reference host implements it. The RFC 0046 credential-reference shape was sufficient. | Schema diff finalization at `Active` |
| G4 | Conformance | The existing `conformance/src/scenarios/byok-auth-modes.test.ts` scenario hard-codes the current four-item enum via `toEqual([...])`; it will fail once the schema enum gains a fifth value unless updated in the same conformance release. | Conformance Architect | `closed` 2026-09-18 — already done, and this row has been stale since conformance 1.47.0: the five-mode enum (`apiKey`, `oauth-pkce`, `oauth-device`, `none`, `subscription`) ships in `capabilities.schema.json` and `byok-auth-modes.test.ts` asserts it by name. scenario. | `Active` promotion (schema merge) |
| G5 | Security | Rate-limit/seat-abuse potential — even at correct single-user scope (§B clause 8), a host could still architect around the scope constraint by rapidly rotating which user "owns" a shared login. Not fully closed by the scope-binding MUST alone. | Security Architect | `transferred:docs/KNOWN-LIMITS.md` 2026-09-18 — ruled: §B.8's user-scope MUST plus the storage-tenant fix is the protocol's whole answer, and seat-rotation abuse is an operator and provider concern outside the wire. An audit-event MUST on a Parked RFC no host may advertise would be a requirement nothing can witness — the vacuous-requirement shape this corpus keeps refusing. Recorded as a known limit instead of a rule nobody can check. Consider whether an audit-event requirement (logging every `subscription`-mode credential resolution with the resolving user id) should be added at `Active` promotion. | `Active` promotion (may add a new audit-event clause) |
| G6 | Compatibility | The new `credential_scope_forbidden` error code's exact envelope shape (does it follow the standard OpenWOP error envelope 1:1, or does it need additional fields identifying which scope was attempted?) is sketched but not fully specified. | Schema Architect | `closed` 2026-09-18 — the code follows the canonical envelope 1:1 and needs no extra fields: the reference host returns `403 credential_scope_forbidden`, steward-witnessed, and `details` already sits at the envelope root for a host that wants `attemptedScope`. **Separate defect filed from this sweep, and CLOSED 2026-09-19:** the row read that `credential_scope_forbidden` was absent from `spec/v2/errors.json` while `capabilities.md` §B.8 makes emitting it a MUST. Re-measured: it **is** registered — `spec/v2/errors.json` carries the row at `403`, `spec/v2/core/errors.md` lists it, and `schemas/v2/error-envelope.schema.json` carries it in both the code enum and the status map. A v2 host has a registered code to emit. The claim is struck rather than left standing: a register row asserting a defect that does not exist sends the next reader to fix nothing. Specify exact error envelope shape at `Active` promotion, following the existing canonical error-code pattern (`spec/v1/errors.md` if applicable). | Schema diff finalization at `Active` |
