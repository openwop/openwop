# RFC 0163 — Gap register

Open design gaps discovered while authoring RFC 0163 (SCIM ⟷ SAML subject-linking hardening — the declarable link-key class + same-IdP trust-root MUST). Keyed to the RFC; each row has an owner and a resolution path. Gaps with no path are promoted to the risk register.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | Proposal §A.1 / UQ2 | The enum `{opaque-idp, oid, immutable-id}` is the safe-class set derived from RFC 0159 §A.1 + UQ3. Is it complete, or is a fourth class (e.g. a directory-specific immutable GUID distinct from `oid`) wanted? | Spec Architect | **CLOSED at `Active` (2026-09-01)** — the enum names classes, not attributes: `{opaque-idp, configured-immutable}`; `oid`/`immutable-id` were Entra attribute names with no witness the suite could distinguish, and an attribute allow-list is perpetually incomplete. See RFC 0163 Alternatives 6. | — |
| G2 | Proposal §B.1 / UQ3(trust-root) | Should the spec pin HOW same-IdP correspondence is established (SAML `Issuer` entityID ↔ a recorded SCIM-connection IdP entityID) as normative, or leave the representation to host config under the "same trust root" MUST? | Spec Architect | **CLOSED at `Active` (2026-09-01)** — pinned in §B.1: SAML `<saml:Issuer>` entityID (inside the signed element) ↔ the IdP entityID bound to the SCIM connection at configuration time; never inferred from a request. "Correspond" without a representation was unwitnessable. | — |
| G-fixture | Conformance §B | ~~The §B behavioral leg needs a **two-trust-root fixture**: a second synthetic IdP instance in `conformance/src/lib/saml-idp.ts` (distinct entityID) + a SCIM payload provisioned "from" the second IdP, colliding on one identifier. Does the bundled synthetic IdP support a second distinct-entityID instance today, or is a fixture extension needed?~~ **ADDRESSED** (RFC 0163 §B / G-fixture, this branch). The bundled synthetic IdP did NOT carry an entityID/Issuer; it was extended: `createSyntheticSamlIdp({ entityID })` now stamps a **signed** `<saml:Issuer>` into every assertion, exposes `.entityID`, and adds `issuerOf()`. Two instances (distinct entityID + distinct signing key) model two independent trust roots that can mint an assertion for the SAME opaque subject id while staying cryptographically + by-issuer distinguishable. `auth-subject-link-key-class.test.ts` now carries (a) a **server-free reference suite** proving the fixture (executable now — distinct entityIDs, both mint the colliding subject, distinct signed Issuers, and IdP-A rejects IdP-B's cross-root assertion `bad-signature`) and (b) a **live black-box §B leg** — a same-IdP positive control (must link, guards vacuity) paired with the §B.1 cross-IdP collision MUST NOT link — opt-in via `OPENWOP_TEST_SAML_IDP_URL` (IdP-A) + `OPENWOP_TEST_SAML_IDP_URL_B` (IdP-B) + `OPENWOP_TEST_SCIM_URL`, soft-skipping until a host wires the two-trust-root seam. No `fixtures.md` row: the synthetic IdP is a `src/lib/` code fixture (like `oidc-issuer.ts`), not a seeded workflow-JSON fixture, so it is documented in-code rather than in the workflow-fixture catalog. | Conformance Architect | ~~Read the IdP fixture API; extend + add a `two-trust-root` row to `fixtures.md`. **Phased**: declared in the scenario, shipped as a follow-on so the advertisement leg is not blocked~~ **DONE** — fixture extended + reference suite executable now; the live leg is executable when a host provides the two-IdP seam. | `auth-subject-link-key-class.test.ts` §B behavioral leg → `Accepted` (behavioral leg now witnessable once the host wires the seam) |
| G3 | Conformance §A.2 | The §A.2 "advertise only the class you honour" behavioral honesty check reuses the RFC 0159 cross-lane seam (`OPENWOP_TEST_SAML_IDP_URL`/`OPENWOP_TEST_SCIM_URL`). Confirm the RFC 0159 seam surfaces enough to assert WHICH attribute the host joined on (not just that a link formed), or is a seam extension needed to witness the key class? | Conformance Architect | Inspect the RFC 0159 seam contract; if it only reports link-formed/not, a seam field naming the join attribute is a follow-on | §A.2 witnessability (probed-pair leg) — **carried as RFC 0163 UQ5**: the seam reports link-formed/not, not the join attribute; the structural fix (issuer + key class on the emitted subject) is v2 identity work, not a seam extension |
| G4 | Reference host | openwop-app's RFC 0159 impl (ADR 0613) keys on `externalId` = class `opaque-idp`; the two host tasks (emit `subjectLinkKey`, add the §B same-IdP check on the link-formation path) are small but land AFTER `Active`. Confirm the SAML assertion issuer is available at link-formation time in the reference host's ACS/validate path. | Compatibility Architect | Reference-host implementation PR after `Active` (deferred per acceptance criteria) | `Active → Accepted` — the SAML validate seam parses the signed `<saml:Issuer>` (conformance fixture `issuerOf()` is the reference); the SCIM provision seam accepts `idpUrl` to name the SCIM lane's trust root (`host-sample-test-seams.md` updated at `Active`) |
| G5 | Conformance | The RFC 0050 `auth-saml-profile.test.ts` "synthetic-IdP reference suite" block has the same shape this RFC's fixture self-tests had before `Active`: server-free fixture assertions inside a scenario file, which record an `executed-pass` in every host bundle (certification gap G8). Moving it to `src/lib/saml-idp.test.ts` changes RFC 0050's recorded evidence surface and belongs in its own PR. | Conformance Architect | Follow-on conformance PR; not a 0163 gate | — |
