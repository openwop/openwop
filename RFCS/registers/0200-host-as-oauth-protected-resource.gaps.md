# RFC 0200 — Gap register

Open design gaps found while writing RFC 0200 (the host as an OAuth protected resource). Each row names an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §A, §B | **MyndHyve is the only committed v2 host §A/§B bind** (an `oidc` lane with Firebase and Google issuers, `evidence/v2-host-bundles/myndhyve.json`). The row is witnessed only when MyndHyve serves PRM and challenges. | Conformance Architect | `externally-gated:myndhyve-oidc-prm` — MyndHyve host work (impl §5). A second witness needs another host with an `oauth2` or `oidc` lane (G2). | `Accepted` |
| G2 | §A | **openwop-app's v2 bundle omits the `oidc` lane that its v1 discovery advertises** (`openwop-auth-oidc-user-bearer`, issuer `securetoken.google.com/openwop-dev`, verify-EF #6). Its v2 advertisement is either incomplete or deliberately narrower. | Compatibility Architect | `externally-gated:openwop-app-v2-oidc-lane` — Ask openwop-app. If the lane is real in v2, advertise it and implement §A/§B. That is the tier-1 production second witness. | Second witness |
| G3 | §A.3 / UQ1 | **`resource` (a URL) is not the `oidc` audience** (Firebase `aud` = project id). A PRM consumer that requests `resource=<url>` from Firebase cannot get a token the host accepts. PRM is correct but not *sufficient* for such hosts. | Spec Architect | `transferred:SECURITY/threat-model-auth-profiles.md` — Unresolved question 1 (an optional `auth.lanes[].audience`). Record the limit in `threat-model-auth-profiles.md` §Discovery. | Nothing |
| G4 | §B.3 | **The list of non-disclosure 404 rules is enumerated by hand.** A future RFC that adds an identical-404 surface has to be added to the no-oracle scenario, or the invariant silently stops covering it. | Conformance Architect | `transferred:conformance/src/scenarios/auth-challenge-no-oracle.test.ts` — Where they are machine-listable, drive the scenario from the OpenAPI operations whose `404` description carries "unauthorized" / "identically". Otherwise add a `check-*` gate that greps for the phrase and compares it with the scenario's route list. | Nothing today |
| G5 | §E | **Body-borne credentials and DPoP proofs are unwitnessable in general** (falsifiability table). The canary legs witness only the header subset. | Security Architect | `transferred:SECURITY/threat-model-secret-leakage.md` — Accept, and record it as a residual risk in `threat-model-secret-leakage.md`. A DPoP leg can be added once a host advertises a `sender-constrained` lane with `dpop`. | Nothing |
| G6 | §C | **Overlap with Phase 2 P2-a** (§C.3). If P2-a's wording already states the wrong-audience rejection as a MUST, §C becomes a pointer, and the "previously undefined" framing belongs to P2-a. | Compatibility Architect | `transferred:rfc-0200-spec-pr` — Read P2-a's merged text before PR-2 and edit §C accordingly. | PR-2 wording |
| G7 | §F | **`derive-v2-api.py` has no rule for per-operation `security`** on the three operations it adds itself (`/host/effect-seams`, `/runs/{runId}/compensation`, `/runs/{runId}/effects`), and the scopes are not stated in `rest-endpoints.md`. | Schema Architect | `transferred:scripts/derive-v2-api.py` — Assign them in the script: `runs:read` for the two run reads, and `manifest:read` for effect-seams (decide in PR-2). Record them in `rest-endpoints.md`. | `check-openapi-security.mjs` green |
| G8 | §F | **`deploy:*` and other extension scopes** in `rest-endpoints.md` (`POST /v1/agents/{agentId}/deployments`) are outside `auth.md` §Scopes. The gate needs a documented extension list, not a silent allowlist. | Schema Architect | `transferred:spec/v1/auth.md` — The gate reads the extension scopes from a table in `auth.md` §Scopes ("A server MAY define additional scopes…"), which PR-2 adds with the current members. | Gate |
| G9 | Budget | ≈ +152 net words. *Re-measured 2026-09-22 after the #1483 trim:* the shared base is 24,446 / 29,000 (`review/arch-impl.md` §2), not the old 1,725 headroom. | Spec Architect | `transferred:scripts/check-core-budget.mjs` — Re-run `check-core-budget.mjs` on the merged tree. | Merge only if red |
