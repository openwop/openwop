# RFC 0075 — Risk Register

| ID  | Risk                                                                                                                                      | Likelihood | Impact | Score | Mitigation                                                                                                                                                                                                                                                                                                               | Owner                 | Status            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | ----------------- |
| R1  | **`additionalProperties` MUST→SHOULD weakens cross-host shape guarantees** — a consumer assuming closed-world breaks on an open artifact. | M          | M      | Med   | The `validation` field makes strictness explicit + advertised; default `open` aligns with `COMPATIBILITY.md` §2.1 (consumers already MUST ignore unknown fields). Relaxation only permits open schemas; strict schemas still conform.                                                                                    | Spec Architect        | Mitigated         |
| R2  | **Host-native registration becomes an unverifiable `registered:true`** if a host emits it without serving the schema.                     | M          | M      | Med   | P1-3 makes serving the canonical URL a MUST for no-pack types; `registered:true` host-tier is gated on resolvability in §"Binding".                                                                                                                                                                                      | Spec Architect        | Mitigated by P1-3 |
| R3  | **Per-type `types` map drifts from the global facet vocabulary** across hosts.                                                            | L          | L      | Low   | Per-type entry vocab is pinned identical to the global object (`validated`/`validation`/`schemaVersion`/`store`/`render`/`export`); `types` absent ⇒ host-global semantics.                                                                                                                                              | Spec Architect        | Low               |
| R4  | **Renumber collision** (0075 grabbed by a parallel session before merge).                                                                 | M          | L      | Low   | Accept renumber at merge time (precedent: 0070, 0072); steward verifies next-free at PR open + merge.                                                                                                                                                                                                                    | Implementer           | Open              |
| R5  | **Store-without-render stays asserted-not-tested** if P2-1 slips.                                                                         | M          | L      | Low   | ~~tracked as a follow-on PR~~ **RESOLVED 2026-05-27** — the in-memory reference host now advertises `host.artifactTypes { store:true, render:false }` + the produce seam; `artifact-type-store-without-render` PASSES against it end-to-end (steward-verified locally). The negotiation is exercised, not just asserted. | Conformance Architect | Closed            |
