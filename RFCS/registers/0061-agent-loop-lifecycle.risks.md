# RFC 0061 — Risk Register

Companion to [`RFCS/0061-agent-loop-lifecycle.md`](../0061-agent-loop-lifecycle.md). Likelihood × Impact (H/M/L).

| ID  | Risk                                                                                                                                                                                                            | Likelihood | Impact | Score | Mitigation                                                                                                                                                                                                                                 | Owner                   | Status |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- | ------ |
| R1  | RFC 0058 ships (already pushed) with the wrong `agents.loop.supported` gate; if 0058 is merged before the G1 correction, the `maxLoopIterations` key references a capability no cohort RFC creates → dead gate. | H          | M      | High  | Land the G1 correction in the same branch before 0058 merges; the branch is in review (not merged), so the fix is pre-merge.                                                                                                               | Spec Architect          | Open   |
| R2  | Adding `iteration` to `runOrchestrator.decided` (additive) is harmless, but a host on `version >= 5` that _omits_ it would silently break the `maxLoopIterations` `observed` value.                             | M          | M      | Med   | `agent-loop-version5-shape.test.ts` asserts `iteration` present + integer ≥ 1 when `version >= 5`; conformance gates the behavior.                                                                                                         | Conformance Architect   | Open   |
| R3  | Workspace-snapshot-per-iteration (§C.2) depends on RFC 0059 (Draft). If 0059 stalls, `version: 5` ships with only the memory-snapshot + counter + resume halves, and the workspace input is dormant.            | M          | L      | Low   | §C.2 is explicitly conditional on `host.workspace.supported`; a `version: 5` host without 0059 is conformant with no workspace input. No hard coupling.                                                                                    | Spec Architect          | Open   |
| R4  | Stateful resume (§D) interacts with RFC 0039 cross-child memory concurrency + RFC 0041 replay; a naive host could reset the iteration counter on resume, diverging from replay.                                 | L          | H      | Med   | §D MUST: counter does not reset/skip on resume; snapshot lineage intact. `agent-loop-stateful-resume.test.ts` + composes with RFC 0041 replay suite.                                                                                       | Spec + Replay reviewer  | Open   |
| R5  | `version: 5` requires implementing all of 1..4 first (ladder rule). A host wanting only the loop bound but not full multi-agent (confidence, cross-host, replay-nondeterminism) cannot advertise v5 cheaply.    | M          | L      | Low   | The `maxLoopIterations` _bound_ (RFC 0058) gates on `executionModel.supported` (v>=1), so a v1 host can bound iterations without v5. v5 only adds the workspace-input + counter-field + stateful-resume formalization. Document the split. | Compatibility Architect | Open   |
| R6  | Cohort/doc drift: README + CHANGELOG still describe the superseded `agents.loop`/`agent.loop.iterated` surface (G2).                                                                                            | M          | L      | Low   | One-line CHANGELOG/README edits land with the reframe.                                                                                                                                                                                     | Spec Architect          | Open   |
