# RFC 0199 — Gap register

Open design gaps found while writing RFC 0199 (the host as an OAuth client, and the `credential` interrupt). Each row names an owner and a resolution path. Until a row closes, it is recorded here and not assumed away.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §A, §C | **No host witnesses §A or §C today.** MyndHyve is the only `oauth` host (tier-2; nine providers, `evidence/v2-host-bundles/myndhyve.json`). The existing `oauth-authorization-code-roundtrip` seam soft-skips on every committed host. v2-reference advertises no `oauth`. | Conformance Architect | `externally-gated:oauth-seam-host` — v2-reference adds a synthetic provider, `oauth.credentialInterrupt` and the `authorize-start` / `expire-refresh` seams (openwop-examples PR). MyndHyve wires the same seams on a staging revision. | `Accepted` |
| G2 | §A.3, §C.3 | **The same-Subject check depends on how a host authenticates a browser callback.** The suite plays the user agent with a bearer, while a production callback carries a session cookie. A host that checks the bearer but not the cookie passes the scenario and fails in production. | Security Architect | `transferred:SECURITY/threat-model-auth-profiles.md` — Record the limit in `threat-model-auth-profiles.md` as a residual risk. The seam contract says the host MUST apply the same identity check on the seam callback as on its production callback. That condition cannot be observed, so it is recorded here and not claimed. | Nothing; it narrows what a pass means |
| G3 | §D.2 | **No v2 host has an MCP server mount** (`review/arch-P3.md` ground rules; v2-reference is seam-gated with no `serverMount`). Today §D.2 can be exercised only on openwop-app's v1 mount (`/v1/host/openwop-app/mcp`), and a major-1 row can never satisfy `check-accepted-predicate` rule 4. | Conformance Architect | `transferred:rfc-0208`: the v2 mount lands with RFC 0208's v2-reference work (shared with RFC 0198). *Amended 2026-09-22:* `.mcp-url-mode` and `.form-mode-no-secret` are minted by legs in RFC 0208's `v2-mcp-mount-map` scenario; the v1 legs are non-gating. The v2 §D.2 rules are amended rows in RFC 0208's `spec/v2/interop-map.json` (`mcp.mrtr`), in force from the day both merge. | v2 witness for `.mcp-url-mode` |
| G9 | §D.1, §D.2 | **Collision with RFC 0208's registry.** RFC 0208 (filed the same day) moves the v2 mappings into `spec/v2/interop-map.json`. Its first draft gave form mode to every waiting interrupt in `mcp.mrtr` and said "no forward source" for `TASK_STATE_AUTH_REQUIRED`, and its totality check keyed `a2a.taskState` on `RunSnapshot.status` alone, while a `credential` interrupt is `waiting-input` yet maps to `AUTH_REQUIRED`. | Schema Architect | `closed` — resolved in the filing PR's architect pass: RFC 0208 defers every credential and authorization-required row to this RFC (RFC 0208 §A "Rows another RFC owns", its gap G11). RFC 0208 ships form mode limited to flat, non-secret schemas and keys `a2a.taskState` by `(runStatus, interruptKind?)`, one default row per status. This RFC's spec PR, ordered after RFC 0208's, adds the `(waiting-input, credential) → auth-required` override row, §D.2(a)–(c) on the `mcp.mrtr` row, and the reverse note. | Nothing |
| G4 | §B | **The census of published `reach.mcp` connection packs** (§Compatibility C3). If a published pack lacks `provider.auth.issuer`, a schema conditional would reject a document that validates today. | Compatibility Architect | `closed` — Run 2026-09-22 over `openwop-registry` `registry/{v1,v2}/packs`: non-empty. `core.openwop.connections.{github,jira,notion}` (v1 1.0.0, v2 1.0.1) are `reach.mcp` + `oauth2` with no `issuer`. §E.2 therefore adds no conditional; Unresolved question 3's host-side rule is adopted (`review/arch-impl.md` R1). | — |
| G5 | §C.6 | **The author-facing "no interrupt solicits a secret" rule is unwitnessable in general**, because whether a string field is a password is semantic. Only the `writeOnly` / `format: "password"` subset is checkable, and only at an MCP bridge. | Spec Architect | `closed` — Accept as unwitnessable (falsifiability table). A later RFC could add a lint over published workflow and pack manifests for field names in the `connection-packs.md` clause 2 blocklist. | Nothing |
| G6 | §D.2(b) | **The client-without-URL-mode answer shape** (Unresolved question 1). MCP 2026-07-28 reserved `-32042`, which leaves no upstream error for "URL mode required". | Spec Architect | `transferred:rfc-0199-spec-pr` — Decide before `Accepted`. The options are `isError` with a structured content convention, or keeping plain `isError`. | `Accepted` |
| G7 | §A.4 | **An issuer-less provider on a single-provider host.** RFC 9700 §4.4.2 waives mix-up defences for a client that talks to one authorization server. §A.4's distinct-redirect rule is then trivially met, which is correct, but the scenario cannot tell such a host from one that shares a redirect URI. | Conformance Architect | `closed` — The scenario's distinct-redirect leg needs two issuer-less providers. It records `inapplicable` on a host that advertises fewer than two. | Nothing |
| G8 | Budget | **Shared core headroom.** ≈ +162 net words. *Re-measured 2026-09-22 after the #1483 trim:* 24,446 / 29,000, shared with every RFC in the 0197–0209 batch (`review/arch-impl.md` §2; the old 27,275 base is stale). | Spec Architect | `transferred:scripts/check-core-budget.mjs` — Merge order in `review/arch-P3.md` §Recommended filing order. Re-run `check-core-budget.mjs` on the merged tree (CLAUDE.md §4). | Merge only if the budget is red |
| G10 | §E.2 | **Six published packs lack `issuer`** (G4). Under the host-side rule a conforming host refuses to authorize them over MCP reach until they declare it. | Registry steward | `externally-gated:openwop-registry` — republish `core.openwop.connections.{github,jira,notion}` as patch versions carrying each provider's authorization-server `issuer` (read from its RFC 8414 metadata). openwop-registry merge authority is a steward ask. | Hosts authorizing those packs over MCP reach |
