# RFC 0167 — Risk register

| ID | Risk | Likelihood | Impact | Score | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | Eleven children drift on shared vocabulary (an axis or alias named two ways) | M | H | High | Axes and aliases are enumerated once in §E and in data (`alias-detectors.json`, `migrations.json`); a child that needs a new one adds a row, and `check-migrations.mjs` / `check-alias-coverage.mjs` refuse prose-only additions | Spec Architect | `open` |
| R2 | The sole-steward waiver is read as "no review": a child lands a wrong wire shape that an external reviewer would have caught | M | H | High | Every child carries an `## Adversarial review` section recording a `/architect` pass with findings and dispositions; the umbrella was itself reviewed before filing (five blocking findings absorbed: header negotiation default, invariant timing, program-RFC amendment, umbrella Active in PR A, waiver literal) | Steward | `mitigated` — the review is recorded, not skipped; an external audit (RFC 0156) remains the real control |
| R3 | A codemod's negative control passes vacuously (a sabotage that silently fails to apply) | L | H | Medium | `check-codemods.mjs` sabotages its own comparator on every run and fails when the sabotage is not seen (EVIDENCE-DISCIPLINE §6); a refused-input fixture proves the codemod throws rather than guesses | Conformance Architect | `mitigated` |
| R4 | The C.4 rename set changes after codemods are written, orphaning them | M | M | Medium | The event-type codemod is generated from `event-codemap.json` overrides, not hand-written per event; a codemap edit regenerates it | Spec Architect | `open` |
| R5 | Phase 2 ends with children Active and no host able to implement them (the "unimplemented MUST" pattern the charter names as v1's core defect) | M | H | High | §G.2 refuses the cut until an example host passes the 2.0.0 floor from the front door and both matrix hosts produce a non-vacuous v2 bundle (Phase 4); Active is a status, Accepted is evidence | Steward | `open` |
