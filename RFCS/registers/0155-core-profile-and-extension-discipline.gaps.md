# RFC 0155 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §D | Extension budget lacks empirical calibration. | Governance Maintainer | `carried:openwop.gap.0155.1` Inventory current cohort and review maintainer capacity. | Active — **Sweep 2026-08-16:** **Carried** — budget uncalibrated; measured cohort available via `extensions.json` `coverage`. |
| G2 | §C | Current extension registry backfill is not complete. **2026-08-16: measured and gated** — derived `coverage` block in `extensions.json` (`generate-extension-registry-coverage.mjs`, `--check` in `openwop:check`): 5 covered / 81 uncovered / 4 core of 90 families. The candidate ledger the resolution path asks for now exists as `coverage.uncovered[]`; the per-RFC review of each remains. **Closed 2026-08-16 (later the same day):** every family reviewed and recorded — **72 covered / 0 uncovered / 4 core / 14 metadata of 90**. The generator gained a `metadataFields` bucket (keys that describe the document/host — versions, identity, suite hints — each with a stated rationale, and the registry test forbids a metadata key that carries `supported`, which is how `production` ended up a record, not metadata). Six pre-RFC v1 base advertisements carry `owningRfc: null` + `owningDoc`. Two records state a suite gap honestly (`nosql`, `promptLibrary`: no scenario reads the family → `minimumSuiteVersion: null`). | Spec Architect | `carried:openwop.gap.0155.2` Generate candidate ledger and review every RFC/capability. | Closed |
| G3 | §C | Tier-3 exception for low-risk schema-only extensions is undecided. | Governance Maintainer | `carried:openwop.gap.0155.3` Define narrow evidence alternative or require Tier-3 universally. | Active — **Sweep 2026-08-16:** **Carried** — undecided. |
| G4 | §E | Badge wording/design is not specified. | Docs Maintainer | `carried:openwop.gap.0155.4` Produce accessibility-safe text-first badge variants. | Accepted — **Sweep 2026-08-16:** **Carried** — badge design is a site concern; profile-name distinction (`openwop-discovery-core` vs `openwop-core-standard`) is normative in `profiles.md`. |
| G5 | §B | Manifest packaging location is unsettled. | Release Maintainer | `carried:openwop.gap.0155.5` Include in corpus and conformance package with drift check. | Active — **Sweep 2026-08-16:** **Closed** — `core-standard-manifest.json` lives in `spec/v1` with a `--check` gate in `openwop:check` and an in-suite parity leg (`core-manifest-and-extension-registry.test.ts`); it is not shipped in the conformance tarball (prose dir), and the operation-path manifest follows the same packaging (#1019). |
| G6 | §A | **2026-08-16: §A landed** (`profiles.md` rename/alias + claim vocabulary, `profiles.ts`, `profile-discovery-core-alias.test.ts`; §B manifest and §C registry landed 2026-08-12 with generators + gates). Original text: **§A is ready; §§B–D are not, and they gate the RFC as a whole.** §A's motivating defect is now evidence-backed (the published bundle claims `openwop-core` while failing six `interrupt-*` scenarios that `openwop-core-standard` would have rejected it for; RFC 0148 G6 gave `openwop-core` an explicit `discoveryOnly` marker recording that it has no runtime floor by design), and the alias rule is additive with no wire break. But §B's `core-standard-manifest.json` and §C's `extensions.json` **have no schema, no generator, and no conformance coverage**, so RFC 0147 §A.2's Schema and Conformance reviews cannot be completed against artifacts that do not exist. | Spec + Schema Architects | `carried:openwop.gap.0155.6` Either split §A to `Active` on its own evidence, or build the §B/§C artifacts far enough to review. Do NOT advance the whole RFC on §A's strength. | `Draft → Active` |

