# RFC 0163 — Risk register

Risks identified while authoring RFC 0163 (SCIM ⟷ SAML subject-linking hardening). Scored Likelihood × Impact (H/M/L). Critical/High rows carry a named mitigation owner.

| ID | Risk | Likelihood | Impact | Score | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | A host **lies** in discovery — advertises a safe class (`opaque-idp`) while secretly joining on a mutable key (email) for pairs the suite never probes → the account-takeover join (RFC 0159 R1) survives under a now-"witnessed" banner | L | H | Med | The residual is strictly SMALLER than RFC 0159's: the lie is a §A.2 violation catchable on ANY probed pair (not an unstated fallback). §A.1/§A.3 make the honest expression of a mutable key impossible; §A.2 makes the dishonest one a behavioral failure. Surviving sliver = "lies in discovery AND is never probed on the lying pair" — recorded in §Conformance Falsifiability note | Conformance Architect | Open (residual documented; strictly reduced vs RFC 0159) |
| R2 | The §B two-trust-root behavioral leg is **phased** (fixture is follow-on) → `subjectLinkKey` and the same-IdP MUST ship, but the cross-IdP collision is not yet witnessed by a bundled fixture → an unsafe cross-IdP host passes the suite until the fixture lands | M | M | Med | Advertisement leg (§A) lands executable now; §B leg is DECLARED + soft-skips `blocked` (RFC 0148 §A — unobservable, not unmet) until the two-IdP fixture is engineered (G-fixture); mirrors RFC 0159's phased behavioral legs; gate `Active→Accepted` on it | Conformance Architect | Open (accepted phasing; G-fixture owns the fixture) |
| R3 | A pre-existing RFC 0159 `subjectLinking:true` host that has NOT added `subjectLinkKey` becomes non-conforming against the new suite version → surprise regression for an early adopter | L | L | Low | `subjectLinkKey` is schema-OPTIONAL (no schema-validation break); non-conformance arises only against the suite version shipping the §A.1 scenario — a `COMPATIBILITY.md` §2.3 suite-version tightening, not a spec relaxation, and it is a one-line discovery addition; CHANGELOG + RFC §Compatibility name it | Compatibility Architect | Open (intended, documented tightening) |
| R4 | Enum growth later (a fourth safe class) is rejected by validators pinned to `{opaque-idp, oid, immutable-id}` if a host emits the new value before consumers upgrade | L | L | Low | Standard forward-compat cost of any allow-list enum; adding a member is itself `additive` (UQ2 / G1); clients SHOULD tolerate an unknown value on the SAME leniency `profiles[]` uses ("clients SHOULD tolerate unknown profile ids") | Schema Architect | Open (documented; UQ2) |
| R5 | `subjectLinkKey` is added under `additionalProperties: true` `auth`, so a typo'd or out-of-enum value is NOT a schema error (only a conformance-scenario failure) → a host self-certifying against the bare schema thinks it passes | M | L | Low | The §A.1 obligation lives in prose + the conformance scenario, exactly as RFC 0159's `subjectLinking` cross-profile obligation does; the `enum` DOES constrain the value when the field is present (an out-of-enum value fails validation of the property itself), so the only schema-invisible case is total absence, which the scenario catches | Schema Architect | Open (parity with RFC 0159 design) |
| R6 | Third-party hosts adopt `subjectLinkKey` on a suite older than the one shipping `auth-subject-link-key-class.test.ts` → INTEROP-MATRIX drift / a claimed field with no leg run | M | L | Low | Scenario capability-gated on `auth.subjectLinking`; RFC §Conformance reminds; matrix cell noted in the same PR that lands the scenario; no current reference host advertises `subjectLinking` | Conformance Architect | Open |
