# RFC 0201 — Gap register

Open design gaps found while authoring RFC 0201 (Standard Webhooks as an opt-in companion scheme). Each row has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | v2 text | **Core word budget.** The v2 half adds about 238 words to `spec/v2/core/webhooks.md` and `headers.md` and homes no family (`webhooks` is already homed in `spec/v2/core/webhooks.md`, and none of the listed v1-dependent families is on this RFC's path). That is under the +300 hard ceiling and over the +0 target. It draws on the ~1,725-word headroom that every Phase 3 RFC shares (architect P3-H5). | Spec Architect | `transferred:scripts/check-core-budget.mjs` — Budget is allocated up front by the Phase 3 coordinator. If the headroom is gone when this lands, move the §Rotation paragraph (≈50 words) into the `rotateWebhookSecret` OpenAPI description and keep a one-line pointer. Do **not** move the obligation into `spec/v2/ext/`: RFC 0190 §B counts an ext home that a core family cites, and a co-pointer cannot carry the MUST. | `check-core-budget` at merge time |
| G2 | §D | Standard Webhooks 1.0.0 defines **no** endpoint-verification step. The handshake here is OpenWOP's own design, justified by the flooding argument in §Motivation. The only external precedent is the MCP Triggers & Events draft, which RFC 0147 §A.10 forbids citing as alignment. | Security Architect | `transferred:docs/WAIVER-RETROSPECTIVE-REGISTER.md` — The §B review (RFC 0156) owes an external read of §D in particular. If Standard Webhooks later standardises a handshake, a follow-up RFC adopts its shape as a second verification mode. | nothing now; the §B review |
| G3 | §B.6 | Requiring a **client-supplied** `whsec_` secret for opted-in subscriptions differs from v1's host-minted, returned-once secret. It is the only way the endpoint can authenticate the synchronous challenge. Hosts that mint secrets from a KMS may object. | Spec Architect | `externally-gated:host-report` — Alternative on record: asynchronous verification with a host-minted secret (§Alternatives). Revisit if a host reports the client-supplied rule as a blocker during `Active`. | nothing |
| G4 | §C.10 | "Identical across a host restart" can only be witnessed through the RFC 0158 test hook, and no production host mounts it (openwop-app records `inapplicable`). | Conformance Architect | `closed` — Accept a seam-gated row. Tier-1 (v2-reference) can witness it. | the restart row only |
| G5 | v1 OpenAPI drift (pre-existing) | `api/openapi.yaml` `registerWebhook` omits the `tenantId` body field that `spec/v1/webhooks.md` §Register requires, and its `201` omits `secret`/`secretFingerprint`, which the prose returns. RFC 0201's v1 body edit touches the same schema. | Schema Architect | `transferred:phase-2-parity-correction` — a Phase 2 correction PR (prose/OpenAPI parity). RFC 0201's v1 edit adds only `signatureAlgorithms` and does not attempt the parity fix. | nothing; the two land separately |
| G6 | §E | The post-overlap retirement leg cannot run in a bounded suite when `overlapSeconds` is large. It records `partial-witness:`, not `executed-pass`. | Conformance Architect | `closed` — Hosts that want a full witness can advertise a test-tenant `overlapSeconds: 60`. Recorded so no one cites the row as fully witnessed. | nothing |
| G7 | Process | The architect pass (P3) recommended that Phase 3 **not** repeat the RFC 0147 §A.6 override. The authoring rules for this batch instruct `Draft → Active` under the override. | Steward | `closed` — decided 2026-09-22 (steward decisions log D5): the §A.6 steward override is kept, as David's instruction waives every window. Acceptance is provisional with the RFC 0156 §B review owed, as for RFCs 0194–0196. The RFC records the override verbatim. | nothing |
