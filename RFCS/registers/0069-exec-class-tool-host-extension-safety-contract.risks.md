# RFC 0069 — Risk register

| ID  | Risk                                                                                                                                              | Likelihood | Impact | Score | Mitigation                                                                                                                                                                                         | Owner                   | Status                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | -------------------------- |
| R1  | An independent implementer ships a protocol-tier `core.exec` RCE primitive, reading the protocol's silence as permission                          | M          | H      | High  | This RFC: normative MUST-NOT in `host-extensions.md` + `exec-must-not-be-protocol-tier` protocol-tier invariant + always-on structural conformance scenario that fails if the corpus ever adds one | Security Architect      | Mitigated by this RFC      |
| R2  | The structural conformance assertion false-positives on a benign `core.*` name containing an exec-shaped substring (e.g. `multi-agent-execution`) | M          | L      | Low   | Closed exact denylist of exec-class identifiers as whole namespaced segments, not substring match; positive control for `execution`                                                                | Conformance Architect   | Open                       |
| R3  | A host that privately shipped `core.exec` (already a `core.*` reservation violation) must rename to `x-host-<vendor>-exec`                        | L          | L      | Low   | Codifies an existing exclusion; the rename is correcting an already-non-conformant state, no embargo needed                                                                                        | Compatibility Architect | Open                       |
| R4  | Host-extension exec ships without the SHOULD safety controls (the protocol can't enforce a surface it doesn't define)                             | M          | H      | High  | §A documents the required controls (sandbox/allowlist/approval/audit); residual risk owned by the host per the carve-out; Unresolved #3 considers a MUST on shell-interpolation                    | Security Architect      | Open (host-owned residual) |
