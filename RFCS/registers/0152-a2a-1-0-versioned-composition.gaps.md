# RFC 0152 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | Compatibility | A2A 0.3 adopter inventory is missing. | Interop Maintainer | `carried:openwop.gap.0152.1` Survey matrix, public discovery, and downstream issues. | Active — **Sweep 2026-08-16:** **Measured** — one dual-era advertiser (openwop-app `['0.3','1.0']`); no reference host advertises `a2a.protocolVersions`; the suite peer serves both eras 0.3-first. Legacy `a2a-0.3` named + time-bounded in `a2a-integration.md`. |
| G2 | §D | ~~Field-by-field A2A 1.0 translation table is not authored.~~ **Closed 2026-08-16:** `spec/v1/a2a-integration.md` §"A2A 1.0 versioned composition" D.1–D.7, generated by reading `a2a.proto` at tag `v1.0.0` (not hand-copied types) and reviewed against the 0.3 body. | Spec Architect | `closed` Generate from pinned upstream schemas and review manually. | ~~Active~~ Closed |
| G3 | Conformance | Official real-peer SDK/implementation is not selected. | Conformance Architect | `externally-gated:unspecified` Evaluate official SDKs for reproducible CI. | Active — **Sweep 2026-08-16:** **Carried, externally gated** — no official real peer selected; suite peer pinned to `a2a.proto@v1.0.0` (`a2a-1-0-agent-card.test.ts`). |
| G4 | §C | ~~Mandatory A2A interface floor is undecided.~~ **Closed 2026-08-16:** JSON-RPC at 1.0 is the floor for `a2a-1.0`; HTTP+JSON / gRPC optional. Reasoning: continuity with the 0.3 composition and its interop evidence, the suite's fake peer, and no forced gRPC dependency. | Interop Maintainer | `closed` Test HTTP/JSON floor and optional streaming interfaces. | ~~Active~~ Closed |
| G5 | §E | Delegated identity composes with RFC 0154, still Draft. | Security Architect | `closed` Cross-reference stable identity fields before Accepted. | Accepted — **Sweep 2026-08-16:** **Closed** — RFC 0154 `Accepted` 2026-08-12; `a2a-integration.md` §E cross-references it (identity ≠ authorization; peer principal → boundary reauthorization). |
| G6 | §C | **S18 Q1 (2026-08-16, decided):** which card does a header-less `GET agentCardUrl` return? Decided: the §B receiver rule applies — 0.3-shaped card header-less while `a2a-0.3-legacy` is advertised, 1.0 card on `A2A-Version: 1.0` (a 1.0 client MUST send it), preferred-version card header-less once 0.3 is dropped. Suite leg fixed to send the header (1.122.0); the fake peer already complied. | Spec Architect | `carried:openwop.gap.0152.6` `a2a-integration.md` §C. | Closed |
| G7 | Conformance | **S18 Q2 (2026-08-16, decided):** the `conformance-a2a-task-roundtrip` fixture named `core.a2a.invoke`, a node no host shipped and the corpus never defined. Renamed to conformance-reserved `core.conformance.a2a-invoke`; a host consuming A2A MUST map it to its bridge node, others MUST NOT advertise the fixture. | Conformance Architect | `carried:openwop.gap.0152.7` `node-packs.md` §Reserved typeIds, `fixtures.md`. | Closed |
| G8 | §E | Interop threat-model document (downgrade, card/runtime drift, cross-tenant lookup, artifact leakage) — threats stated in §E; no `SECURITY/threat-model-interop.md` exists (row copied from `spec/v1/a2a-integration.md` §Open spec gaps under RFC 0174 §C register-drift rule). | Security Architect | `carried:openwop.gap.0175.4` — written by the v2 C.8 child (RFC 0175 §F.1) | RFC 0175 `Accepted` |
| G9 | §Future work | Native `auth-required` interrupt kind (drift #3; RFC 0100 UQ4) — never emitted forward. | Spec Architect | `closed` — decided by RFC 0175 §E.2: not in v2.0; the enum member stays for reverse-direction fidelity; a future additive RFC may add the kind | — |
