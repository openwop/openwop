# RFC 0143 — Risk register

Scored Likelihood × Impact (H/M/L).

| ID | Risk | L | I | Score | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|
| R1 | **The completeness leg is read as proving "no unfenced ingress exists"** when it proves "no ingress *in §4* is unfenced." A host adds a new content path to the prompt, never updates §4, and a green suite reads as full coverage. | M | **H** | **High** | The gap is stated in the RFC (unresolved-Q1), the leg's docstring, and gap G1. The leg reduces the surface to "any ingress missing from §4" — reviewable at threat-model review time — but does not eliminate it. | Conformance Architect | Open (disclosed, not closable server-free) |
| R2 | **The isolation carve-out is abused** — a host declares a reader "structurally isolated" that can in fact reach model context or egress, and drops the trust tag legitimately-looking. | M | **H** | **High** | The carve-out is narrow by construction: structural isolation (runtime-enforced) only, never conventional; a reader that can reach model context or egress is explicitly not isolated. But the corpus cannot verify a host's isolation claim server-free — same class as any host-behavioral MUST. | Security Architect | Open (bounded by definition, host-trust) |
| R3 | **The rule is read as requiring value-granular taint** despite saying the opposite, and a host defers adoption believing it needs plumbing it lacks. | L | M | Low | The RFC states value-granular taint is NOT required in the Summary, Proposal, §2a prose, and Alternatives; the two coarse strategies are named as conformant with the meet as the floor. This exact misread was the trap the reference host's answer steered around. | Spec Architect | **Closed** — over-stated in four places |
| R4 | **The meet rule is softened in a later edit** (`MUST be the meet` → `SHOULD`) and the invariant quietly weakens to advisory. | L | **H** | Med | Prose-pinned by the conformance leg, sabotage-verified: softening `MUST be the meet` reds exactly that leg. | Conformance Architect | **Closed** — regression-guarded |
| R5 | **A point-invariant is renamed or removed and the general rule silently loses an instance.** | M | M | Med | The completeness floor asserts each named ingress retains its fencing invariant; renaming one without updating the floor reds the leg. Sabotage-verified (renaming `form-content-pack-string-trust-boundary` reds exactly the floor leg). | Conformance Architect | **Closed** — regression-guarded |
| R6 | **The 166/168 motivating number is challenged** as unverifiable and the MUST reads as unmotivated. | L | L | Low | Framed explicitly as a reference-host measurement (gap G4), not a corpus claim; the invariant stands on the laundering-path argument independent of the number. | Spec Architect | Accepted (motivation, not evidence) |
