# RFC 0128 — Risk Register

Companion to `0128-purpose-propagation-permitted-use-labels.md`. Likelihood × Impact (H/M/L). Critical/High risks carry a named mitigation owner.

| ID | Risk | Likelihood | Impact | Score | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|
| R1 | **Compliance-theater reading** — operators/regulators read `purposePropagation: supported` as "the receiver *enforces* purpose limits" when the wire only promises label *carriage* (internal use is SHOULD/declared-intent, deliberately un-gated §4). Over-claiming here damages the corpus's falsifiability-honesty posture. | M | H | **High** | §4's explicit demarcation + `compliance.md`'s protocol-vs-operator line; the capability description in `capabilities.md` must say "propagates labels", never "enforces purposes". Review wording at schema/spec landing. | Spec Architect | Open |
| R2 | **Silent label stripping by a non-advertising intermediary** — a conformant host that does NOT advertise the capability may legally drop the label mid-chain; the original sender cannot detect the loss downstream. | M | M | **Med** | Prose SHOULD: a sender with a purpose constraint checks the receiver's `purposePropagation` advert before a labelled sync (mirrors the RFC 0083 capability-check pattern). Absence of the advert = the honest signal. | Spec Architect | Open |
| R3 | **Purpose-vocabulary fragmentation** — opaque strings mean `analytics` on host A and host B may not denote the same use; cross-org semantics drift with no wire-level detection. | H | L | **Med** | Deliberate design (jurisdiction/product-specific purposes; a frozen enum would be worse). Non-normative reference vocabulary in the RFC; revisit only if real interop pain emerges — a registry is additive later. | Spec Architect | Open |
| R4 | **Widen-detection evasion via data transformation** — a receiver re-keys/derives the data so the "same data" linkage is lost, then emits with a wider label; conformance catches only correlated-identifier flows, not laundering. | M | M | **Med** | Accept as a named residual: the conformance promise is per-observable-hop on traceable records; laundering is an operator-layer/audit concern (same line as internal use, §4). Do NOT claim wire-level lineage. | Security Architect | Open |
| R5 | **Author-is-witness (MITIGATED — VERIFIED: steward authored the seam-gated scenarios (1.53.1) + independently curl-verified all four legs on the wire, rev 00411-77q)** — original: — the RFC author (openwop-app) would be a graduation witness; if MyndHyve opts out (gap G4), the sole one. | M | M | **Med** | Mandatory per architect ruling 2026-07-06: steward authors the gated scenarios + independently drives and curl-verifies the witness; tier-2 attempt precedes any single-witness fallback. | Steward | Open |
