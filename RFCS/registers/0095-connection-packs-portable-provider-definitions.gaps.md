# RFC 0095 — Gap Register

Open questions, deferred decisions, and "learn-from-implementation" items beyond the in-template Unresolved questions. Each gap has an owner and a resolution path; a gap with no path is promoted to a Risk.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §A `scopeModel` | Do `groups`/`coarse`/`capabilities` capture every real consent model, or do Stripe Connect (coarse `read_only`/`read_write`) and Notion (capabilities, not scopes) need richer per-model fields so a host renders consent correctly? | Schema Architect | `carried:openwop.gap.0095.1` Prototype the 3 Tier-1 quirk providers (Stripe, Notion, GitHub) against the schema during reference-host impl; amend before Active. | Active status |
| G2 | Proposal §A `instanceUrlTemplate` | Where does the resolved `{account}` live — in the connection pack (no, it's per-user) or the RFC 0046 per-connection record? Confirm the split for Snowflake/NetSuite/ServiceNow. | Spec Architect | `carried:openwop.gap.0095.2` Decision needed; coordinate with RFC 0046 author (same). | Schema finalization for per-account-host providers |
| G3 | Proposal §B.6 | Precedence when a built-in and an installed pack both define `provider.id`. Current rule: installed wins iff `version >=` built-in, else `connection_provider_conflict`. Is version-compare across two different *sources* well-defined? | Compatibility Architect | `carried:openwop.gap.0095.3` Spec a deterministic source-precedence rule (e.g. installed-always-wins with a diagnostic) before Active. | Active status |
| G4 | Proposal §A `reach.mcp.server.url` | MCP endpoints are volatile (ecosystem moves monthly; old monorepo deprecated). Should the registry health-check + re-verify MCP URLs, and should the host re-validate at install? | Conformance Architect | `carried:openwop.gap.0095.4` Add a registry re-verification facet (non-normative) + an install-time SHOULD; defer hard enforcement to a follow-up. | None (Draft-acceptable) |
| G5 | Security §B.2 | The "looks like a secret" rejection (§B.2) needs a concrete predicate — a banned-key list (`clientSecret`/`apiKey`/`token`/`password`/`privateKey`) vs a value-shape heuristic. A heuristic risks false positives/negatives. | Security Architect | `carried:openwop.gap.0095.5` Define a normative banned-property-name list in the schema (not a value heuristic); the invariant test enumerates them. | Invariant + scenario `connection-pack-no-credential-material` |
| G6 | Conformance | Which reference host (in-memory / sqlite / python) implements §B.6 first? The openwop-app reference already has the in-code registry (ADR 0024); the three example hosts do not. | Conformance Architect | `carried:openwop.gap.0095.6` Decide whether `Accepted` requires an *example* host or accepts the openwop-app reference impl as the evidence host. | Accepted status |
| G7 | Catalog seeding | Ship Tier-1 `core.openwop.connections.*` packs in THIS RFC or a follow-up "core connection catalog" RFC? (Unresolved Q5.) | Spec Architect | `carried:openwop.gap.0095.7` Recommend follow-up RFC once the manifest is Accepted — keeps this RFC to the *mechanism*, not the *content*. | None (scoping decision) |
| G8 | §C capability | Should `packs.connectionsSupported` be a standalone flag or fold into an existing `packs.*` capability block? Confirm the `capabilities.schema.json` shape has a `packs` object to extend. | Schema Architect | `carried:openwop.gap.0095.8` Read `capabilities.schema.json` `packs`/`prompts.packsSupported` shape; mirror exactly. | Schema diff finalization |
| G9 | Wire | Does `auth.md` already have an "Open spec gap" entry for OAuth provider-definition portability that this RFC should close by name? | Spec Architect | `carried:openwop.gap.0095.9` Grep `auth.md` for the open-gap table; cite + close it in the spec PR. | Spec text merge |
