# RFC 0120 — Gap Register

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §B.9 | SHOULD vs MUST for `apiHosts` when a provider is consumed by an RFC 0045 action / `reach: openapi`. MUST catches the footgun at install but breaks legitimate metadata-only / MCP-read packs with no REST API host. | Spec Architect | `closed` **RESOLVED 2026-06-29 (Active)** — conditional MUST (spec items 13 + §A `allOf`): REQUIRED when `reach: openapi` (manifest schema) AND at the RFC 0045 binding site (runtime); SHOULD otherwise. Converged openwop-app-1 + myndhyve-1 input; architect re-keyed the predicate to `reach.openapi`. | (resolved) |
| G2 | Proposal §B.7 | Port support — draft forbids a port (443 only). Do any target providers expose a non-443 API port? | Schema Architect | `carried:openwop.gap.0120.2` Survey the catalog (meta/google/tiktok/slack/… are all 443); keep host-only unless a real case appears. | Schema `pattern` finalization. |
| G3 | Proposal §B.6 | eTLD+1 (registrable-domain suffix) vs exact-host matching. | Spec + Security Architect | `closed` **RESOLVED 2026-06-29 (Active)** — eTLD+1 floor + dot-anchored suffix containment (item 10): host enforces `==`/`"."+entry`, pack MAY tighten to an exact host; no-substring-escape explicit. Converged openwop-app-1 + myndhyve-1 input. | (resolved) |
| G4 | Security | Relationship between `apiHosts` and RFC 0079 `connectionUse` provenance `audiences` — auto-populate or independent layers? | Security Architect | `closed` **RESOLVED 2026-06-29** — independent, AND-composed (normative §B item 11): egress MUST satisfy both; `apiHosts` match never bypasses/widens the RFC 0079 `egress-credential-audience-bound` MUST; no auto-population. | (resolved) |
| G5 | Conformance | The host-behavioral egress-allow-list scenario needs a reference host that pins credential egress to `apiHosts` (the in-memory/sqlite example hosts may not implement connector egress). | Conformance Architect | `carried:openwop.gap.0120.5` **WITNESS CONFIRMED 2026-06-29** — openwop-app (`brokeredEgress.ts`+`connectorInvoker.ts`, ADR 0167) committed as the egress behavioral-leg witness, to implement at Accepted; myndhyve-1 honest opt-out (no brokered connector egress). Manifest-validity leg is server-free + host-independent. Behavioral leg single-witnessed at Accepted (recorded honestly). | (carried to Accepted) |
| G6 | Implementation | Reference-host migration — do the `bigquery` / `microsoft-graph` dedicated built-ins fold back into packs once `apiHosts` is declarable, or stay as narrow read-only identities? | (impl, non-normative) | `carried:openwop.gap.0120.6` openwop-app maintainer decision post-merge; out of RFC scope. | Nothing (non-normative). |
| G7 | Schema | Old hosts reject a pack carrying `apiHosts` via `additionalProperties:false` until they bundle the new schema. Is the suite-version gating (vs a spec-relaxation) the right framing? | Compatibility Architect | `carried:openwop.gap.0120.7` Documented under Compatibility as a suite-version requirement; confirm no host advertises `packsSupported` AND pins an older manifest schema in a way that breaks. | INTEROP-MATRIX note. |
