# RFC 0147 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


Working document (not normative). Every open row must be closed, transferred to a child RFC or durable tracker, or carried forward explicitly before RFC 0147 can move to `Accepted`.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | Workstream 1 | Inventory of public conformance bundles and the exact assertions that executed versus returned early is not yet available. | Conformance Architect | Build a runner that emits assertion witnesses; replay archived results where possible; publish invalidation/reissue manifest. | SR-1 `Active`; RFC 0147 `Accepted` — **Sweep 2026-08-16:** **Closed.** The runner emits per-requirement dispositions with `assertionCount` (RFC 0148 §A ledger, suite `1.114.0`, #1018); the historic bundle is inventoried and invalidated and all four reference hosts reissued as bundle v2 (`docs/CERTIFICATION-BUNDLE-INVENTORY.md` rows 1–5, `openwop-examples#14`). No archived result could be replayed — the pre-ledger runner recorded no witnesses — so the reissue is a fresh measurement, stated as such. |
| G2 | Workstream 1 | The machine-readable certification schema does not yet distinguish executed, skipped, inapplicable, and failed requirements. | Schema Architect | SR-1 schema diff and migration; add negative bundle fixtures. | SR-1 `Active` — **Sweep 2026-08-16:** **Closed.** `certification-bundle-v2.schema.json` (RFC 0148 §C) carries the five dispositions + required `blocked` total; negative vectors in `certification-bundle-v2.test.ts` and `certification-bundle-non-vacuous.test.ts` (#1019). |
| G3 | Workstream 2 | Whether the OpenAPI base-path correction is purely editorial for every generator has not been demonstrated. | Schema Architect | Resolve all server/path combinations with at least two generators and all SDK operation tables. | SR-2 classification — **Sweep 2026-08-16:** **Closed** (RFC 0149 UQ1, 2026-08-11 — no generator or SDK had consumed the doubled path; the reference SDKs issued `/v1/*` against a bare base). `operation-path-manifest.json` + `openapi-asyncapi-sdk-parity.test.ts` (#1019) now hold every operation table to the resolved path. |
| G4 | Workstream 2 | A v1-compatible mechanism for catching canonical discovery typos while retaining open server-emitted documents is undecided. | Compatibility Architect | Compare authoring-lint, reserved-name denylist, profile manifests, and v2 closure; decide in SR-2. | SR-2 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0149 G2 / UQ2** (typo heuristic threshold unchosen). Wrapper lint (`capability-example-root-layout`) and shadow invariant (`discovery-canonical-family-no-shadow`) landed; runtime documents stay open. |
| G5 | Workstream 2 | Full list of stale lifecycle/status contradictions is not yet machine-generated. | Spec Architect | Add lifecycle lint, run across all RFC/spec documents, publish exceptions. | SR-2 `Accepted` — **Sweep 2026-08-16:** **Closed for RFCs ≥ 0147** (`rfc-lifecycle-coherence.test.ts`, annotated-vs-bare); deliberately not retroactive (`docs/RFC-LIFECYCLE-COHERENCE.md`, 200 bare items pre-0147). Carried as a scope decision, not a defect. |
| G6 | Workstream 3 | Deployed histories and idempotency records using the existing recipes have not been inventoried. | Compatibility Architect | Survey Tier-1/Tier-2 hosts; choose versioned dual-read or v2-only interpretation based on evidence. | SR-3 `Active` — **Sweep 2026-08-16:** **Closed** (RFC 0150 G1, `docs/EFFECT-IDENTITY-V1-INVENTORY.md`): no host implements the Layer-2 recipe; zero persisted v1 keys. |
| G7 | Workstream 3 | Provider-specific outcome-affecting fields beyond the proposed semantic digest floor are not cataloged. | Spec Architect | Build provider-adapter input matrix; define extension digest namespace and vectors. | SR-3 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0150 G3** (provider semantic-option registry). Digest v2 recipe + golden vectors landed (`replay.md`, `conformance/vectors/`); the provider adapter matrix has not. |
| G8 | Workstream 3 | One split-brain strategy and effect-ownership mechanism has not been selected. | Spec + Security Architects | Compare lexicographic owner, quorum/lease owner, fencing token, and effect-level idempotency; threat-model partitions. | SR-3 `Active` — **Sweep 2026-08-16:** **Decided in RFC 0150 §D** (record reconciliation separated from effect authorization; `crossRegion: fenced-effects` named) — **but unwitnessed**: RFC 0150 G8/G9/G10 record that no black-box witness exists and no host fences (S8 waits for a fencing host). |
| G9 | Workstream 4 | Mandatory compensation ordering floor is undecided. | Spec Architect | Prototype reverse-completion and dependency-graph models against payment/message/inventory fixtures. | SR-4 `Active` — **Sweep 2026-08-16:** **Closed.** `compensation.md`: `reverse-completion` is the mandatory ordering floor; dependency-graph ordering MAY be advertised (RFC 0151 G1 carries the graph model's inclusion). |
| G10 | Workstream 4 | Terminal-state additions may require schema widening and client migration. | Schema + Compatibility Architects | Draft state/event schema diff; determine additive unknown-state behavior or v2 need. | SR-4 `Active` — **Sweep 2026-08-16:** **Closed** (RFC 0151 UQ3 / G2, #1007): `compensationStatus` is an OPTIONAL closed enum on `RunSnapshot`; `RunStatus` untouched — additive, no client migration. |
| G11 | Workstream 5 | Exact A2A 0.3 legacy-support window and observed adopter population are unknown. | Interop Maintainer | Survey advertised hosts; propose dates and negotiation behavior in SR-5. | SR-5 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0152 G1.** Measured today: no reference host advertises `a2a.protocolVersions`; openwop-app advertises `['0.3','1.0']`; the suite peer defaults 0.3-first. Legacy `a2a-0.3` is named + time-bounded in `a2a-integration.md`; the adopter population is one dual-era host. |
| G12 | Workstream 5 | Exact MCP 2025-06-18 legacy-support window and callback-to-MRTR migration are unresolved. | Interop Maintainer | Build a dual-profile prototype against official/current peers. | SR-6 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0153 G1.** Same shape: openwop-app advertises both MCP revisions; no reference host advertises `mcp.protocolVersions`; MRTR mapping landed (`mcp-integration.md` §C), callback→MRTR migration prose present, no real peer in CI. |
| G13 | Workstream 6 | Technology-neutral workload-identity vocabulary has not been selected. | Security Architect | Map SPIFFE, mTLS SAN, cloud workload identity, OAuth token exchange, and sender constraint into one optional profile. | SR-7 `Active` — **Sweep 2026-08-16:** **Closed as a vocabulary** — `schemas/workload-identity.schema.json` + `auth.md` §Workload identity (SPIFFE / mTLS SAN / cloud identity / token-exchange / sender-constraint mapped into one optional profile). Carried → RFC 0154 G1 for the mandatory proof format. |
| G14 | Workstream 6 | OpenTelemetry GenAI attributes may remain experimental or change. | Observability Architect | Version-gate mappings and define fallback to stable `openwop.*`; do not make experimental fields core. | SR-7 `Active` — **Sweep 2026-08-16:** **Closed by decision** (RFC 0154 G3): no stable upstream GenAI convention exists; mapping v0 is experimental/optional/ref-labelled and projects no identity field (`observability.md`). |
| G15 | Workstream 6 | Current release and pack attestation coverage versus SLSA expectations is not fully inventoried. | Supply-chain Maintainer | Produce artifact-by-artifact provenance matrix and close missing build attestations. | SR-7 `Accepted` — **Sweep 2026-08-16:** **Carried → RFC 0154 G4** — provenance matrix spans `openwop-sdks` / `openwop-registry`; not inventoried in this repo. |
| G16 | Workstream 7 | Whether `openwop-core` can be additively aliased or needs v2 renaming is unresolved. | Compatibility Architect | Consumer survey; add alias/deprecation plan in SR-8 and removal only in v2. | SR-8 `Active` — **Sweep 2026-08-16:** **Closed** (RFC 0155 §A, suite `1.110.0`): additive alias — `openwop-discovery-core` canonical, `openwop-core` deprecated for all of v1, both-or-neither; removal only in v2; `--certify` v2 keeps the alias out of `claimedProfiles` (#1018). |
| G17 | Workstream 7 | Extension budget and Stable-graduation evidence threshold are not defined. | Governance Maintainer | Propose quantitative budget plus Tier-3 or dual-host evidence rule. | SR-8 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0155 G1/G3** (budget uncalibrated; Tier-3 exception undecided). Registry coverage is measured (5 covered / 81 uncovered / 4 core). |
| G18 | Workstream 8 | No two independent maintainer candidates have accepted nomination. | Lead Maintainer | Send public recruitment, record candidates and affiliations, use normal promotion process. | RFC 0147 `Accepted` — **Sweep 2026-08-16:** **Externally gated** — `MAINTAINERS.md` lists one maintainer; RFC 0156 G1. R14 remains Critical/Open. |
| G19 | Workstream 8 | External security-audit vendor, budget, scope, and start date remain unset. | Lead Maintainer | Execute the existing engagement plan; publish non-sensitive scope and timeline. | RFC 0147 `Accepted` — **Sweep 2026-08-16:** **Externally gated** — engagement unscheduled; `SECURITY/external-audit-findings.json` committed empty; RFC 0156 G2. |
| G20 | Workstream 8 | No Tier-3 host has committed to the corrected suite. | Ecosystem Maintainer | Recruit from durable-workflow/agent platforms; fund or support adapter without controlling implementation. | RFC 0147 `Accepted` — **Sweep 2026-08-16:** **Externally gated** — no Tier-3 host; `INTEROP-MATRIX.md` header now states it; RFC 0156 G4. |
| G21 | Workstream 8 | The high-risk RFC cohort requiring retrospective review is not enumerated. | Governance Maintainer | Select by identity/auth/isolation/replay/effects/certification tags; publish ratification queue. | SR-9 `Active` — **Sweep 2026-08-16:** **Carried → RFC 0156 G3.** `docs/RFC-0147-SELF-AUDIT.md` §A.6 names 0148/0150/0152/0153/0154 as high-risk-under-waiver; the ratification queue is not published. |
| G22 | Workstream 9 | The A-grade release gate is not machine-readable. | Release Maintainer | Define a generated assurance manifest sourced from suite, audit, interop, governance, and known-limits evidence. | RFC 0147 `Accepted` — **Sweep 2026-08-16:** **Carried.** Pieces exist (`docs/PROTOCOL-STATUS.md` generated; `extensions.json` coverage; bundle v2; audit-findings gate) but no single generated assurance manifest ties them together; RFC 0156 §E. |
| G23 | Program | The child RFCs do not yet have assigned RFC numbers or named owners beyond role ownership. | Lead Maintainer | Assign on authoring using next-free numbering; record a DRI and target date in the risk register. | Each child `Draft` — **Sweep 2026-08-16:** **Closed** — RFCs 0148–0157 numbered and authored; role owners stand in for DRIs (single maintainer). |
| G24 | Program | The audit input is in the sibling app repository rather than the protocol corpus. | Spec Architect | Copy a stable assessment summary or link a public immutable revision before RFC 0147 becomes Active. | RFC 0147 `Active` — **Sweep 2026-08-16:** **Closed** — the assessment summary is linked from RFC 0147 §Motivation and the self-audit is in-repo (`docs/RFC-0147-SELF-AUDIT.md`). |
