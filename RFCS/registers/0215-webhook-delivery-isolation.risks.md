# RFC 0215 — Risk register

| ID | Risk | Likelihood | Impact | Score | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | The floor of 8 fails openwop-app, whose claim batch is 5, at its first cut on the new suite minor. That would turn a fixed defect back into a red row. | M | M | Medium | Measured at `Active` (2026-09-25), from the host's own session: openwop-app fails §A for three reasons, not one — a batch barrier (the tick awaits the whole claimed batch), in-flight concurrency of min(CLAIM_BATCH, pool − 1) = 3 in production, and a 10 s delivery timeout. It is replacing the loop with a non-blocking dispatcher, a lane per subscription and an in-flight cap of at least 32 decoupled from the DB pool. A host that fails it is starving subscribers, which is the defect. | Steward | `accepted` — the row failing is the rule working; the floor is decided (§Resolved questions 1) and the host fix is in flight. |
| R2 | §A's leg flakes on a slow host. The healthy delivery misses `RETRY_WAIT_FLOOR_MS` because of load, not head-of-line blocking. | M | M | Medium | The leg fails only on a *positive* observation: the healthy attempt arrived after the host abandoned a held attempt. A healthy attempt that never arrives, while no held attempt was abandoned, records `blocked` with `noDeliveryCause`, as the control leg does. | Conformance Architect | `mitigated` — by the pass/fail/blocked split in §Conformance. |
| R3 | A public front (tunnel) closes a held request before the host does, so the suite misreads the front's timeout as the host abandoning the attempt. | L | M | Low | A cloudflared front times out at ~100 s, beyond the 20 s window, and the scenario records which side closed the connection. | Conformance Architect | `mitigated` — by the window being shorter than the front's timeout. |
| R4 | §B's 5 s grace is too short for a host whose in-flight attempt was started before the `204` but reaches the receiver later. | L | L | Low | The receiver keys on request *arrival*, and an attempt started before the `204` arrives within network transit. The spec states no number (§Resolved questions 3); 5 s is the scenario's instrument setting and can change in a suite patch. | Spec Architect | `mitigated` — no number is normative. |
