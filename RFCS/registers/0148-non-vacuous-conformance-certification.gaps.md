# RFC 0148 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §C | Exact canonical witness transcript is undecided. | Conformance Architect | `carried:openwop.gap.0148.1` Prototype JSON canonical transcript and signed event forms. | Active — **Sweep 2026-08-16:** **Carried.** The witness is `assertionCount` per requirement (S6); no canonical transcript or signed event form exists. `witnessSha256` is reserved in the v2 schema and unused. |
| G2 | §D | ~~Historic public bundle inventory is incomplete.~~ **CLOSED 2026-08-11** — swept all six ecosystem repos; exactly one v1 bundle exists (in-memory reference host, suite 1.18.1). `INTEROP-MATRIX.md` cites no bundle as evidence. Inventory published at `docs/CERTIFICATION-BUNDLE-INVENTORY.md`; the one bundle is marked `invalidated`. | Release Maintainer | `closed` Done. | — |
| G6 | §C | ~~Floor sets exist for `openwop-core-standard` only, so `verifyBundleProfile()` returns `floorProven: true` for any other claimed profile.~~ **CLOSED 2026-08-12** — undefined floor is now unprovable (`floorUnspecified`); floors for `openwop-stream-sse` / `openwop-stream-poll` / `openwop-node-packs` **transcribed from `profiles.md` prose**, and `openwop-core` / `openwop-fixtures` marked `discoveryOnly` so an empty floor is a decision on record. Implementation of existing normative text, so no safety-fix window. `certification-floor-enforcement.test.ts`, sabotage-verified. | Conformance + Spec Architects | `closed` Done. | — |
| G7 | §C | `openwop-replay-fork` has a **discovery-conditional** floor (`profiles.md`: scenarios "pass on whichever mode the host advertises") that a flat `required[]` cannot express, so it stays unprovable. Same for `openwop-interrupts`, `openwop-secrets`, `openwop-provider-policy`, `openwop-memory`, `openwop-trigger-bridge`, whose prose names no settled floor. Any host claiming these cannot be certified until the model or the prose catches up. | Spec + Conformance Architects | `carried:openwop.gap.0148.7` Extend `ProfileFloor` with a discovery-predicate-conditional form, or settle each profile's floor in `profiles.md`. | Active — **Sweep 2026-08-16:** **~~Carried~~ CLOSED 2026-08-16 (later the same day, suite `1.120.0`).** `ProfileFloor.conditional` (`{ path, includes, required }[]`, evaluated against the captured discovery document by `requirementsFor(profile, document)`, `--certify`, `verifyBundleV2`, and the v1 verifier; unevaluable without a document → `null`, never `[]`) expresses `openwop-replay-fork`'s mode-conditional floor, and the five prose-less profiles received floors transcribed into `profiles.md` ("Floor (RFC 0148 §C)" per section): `openwop-interrupts`, `openwop-secrets`, `openwop-provider-policy`, `openwop-memory`, `openwop-trigger-bridge`. Every catalog profile now has a floor; *unprovable* remains only for names outside the catalog. |
| G3 | §C | Stable `requirementId` registry and alias policy do not exist. **Measured 2026-08-11** (`docs/REQUIREMENT-REGISTRY-FEASIBILITY.md`): 1,544 `driver.describe()` sites, 87.8% generatable from two string literals. Two blockers the gap did not anticipate — 168 sites interpolate the requirement text at run time, so **no text-derived ID can be stable for them by construction**; and citations use **six** addressing conventions for the same documents, so a text-derived ID encodes an authoring accident until addressing is normalized. | Spec Architect | `carried:openwop.gap.0148.3` Generate + accept hand-authored entries; normalize citation addressing FIRST (overlaps RFC 0149 §D); alias file makes reworded IDs a reviewable act. | Active — **Sweep 2026-08-16:** **Partially closed.** `requirement-registry.ts` gives stable ids per floor file (`openwop.floor.<file>`), per prefix (`openwop.floor.any.<prefix>`), and per file (`openwop.scenario.<file>`) — the granularity the ledger records at. Per-`driver.describe()` ids remain unbuilt for the reasons measured 2026-08-11. **Sweep 2026-09-02 (suite 1.153.0): CLOSED at test granularity** — ids derive from `it()` titles (`openwop.it.<file>.<slug>`, 1,927 of 1,950 stable; 23 interpolated titles map by file+line), generated into `conformance/requirements.json` with `requirement-aliases.json` gated by `generate-requirement-registry.mjs --check`; per-`it` rows are recorded and emitted in bundle v2. Per-`driver.describe()` granularity is not pursued: the title is the stable unit and the registry carries the citations. |
| G4 | §E | Signing boundary between RFC 0148 and RFC 0154 is unsettled. | Security Architect | `carried:openwop.gap.0148.4` 0148 owns digest; 0154 decides attestations/signatures. | Accepted — **Sweep 2026-08-16:** **Carried.** 0148 owns the digest (`scenarioManifestSha256`, `targetConfigurationSha256`); RFC 0154 G4 has not selected the attestation predicate; no bundle is signed. |
| G5 | Conformance | Vitest reporter API support for assertion-level custom events needs validation. | Conformance Architect | `carried:openwop.gap.0148.5` Spike reporter; fall back to explicit ledger calls with CI completeness check. | Active — **Sweep 2026-08-16:** **Closed.** No reporter API needed: `setup.ts` `afterEach` reads `expect.getState().assertionCalls` and `afterAll` records the file (S6, #1018); the CI completeness check is `--certify`'s rejection of any silent requirement. |

