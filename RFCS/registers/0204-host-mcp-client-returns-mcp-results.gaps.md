# RFC 0204 — Gap register

Open design gaps discovered while authoring RFC 0204 (v2 `ctx.mcp` contract, v1 `raw`, `toolCatalog` v2 home, MCP annotation mapping). Lands as `RFCS/registers/0204-host-mcp-client-returns-mcp-results.gaps.md`. No row is `open`.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §Normative text | Net core cost is **+245** (445 added, one family homed), inside the +300 ceiling but not self-funding. `mcp` is already homed, so the `ctx.mcp` section grants nothing; homing `httpClient` would not help, because its v1 contract (six facets, `safeFetch`, the egress policy) costs more to restate than the 200-word grant it earns. | Spec Architect | `transferred:scripts/check-core-budget.mjs` — accepted as is. The orchestrator's running total across the Phase 3 RFCs is the control; re-measure on the merged tree. | Nothing while the cap holds. |
| G2 | §A.7 | V2 names the method `callTool`, while the v1 name `invokeTool` is what pack authors know. A host keeping `invokeTool` for ported packs is permitted (MAY) but not specified. | Compatibility Architect | `transferred:../openwop-sdks/sdk/typescript` — the SDK types `ctx.mcp` by major and ships a `{ result, isError }` shim over `CallToolResult` for ported packs; no protocol text needed. | Nothing. |
| G3 | §D.10 | The restated `toolCatalog` MUSTs are witnessed only at major 1 (`tool-catalog-projection`, `tool-catalog-compact-projection`, `tool-descriptor-shape` in `conformance/scenario-majors.json`). | Conformance Architect | `transferred:conformance/scripts/generate-scenario-majors.mjs` — add the three files to `BOTH_MAJORS` after removing `/v1/` literal paths, in this RFC's suite minor. | `Accepted` for the §D.10 row only. |
| G4 | §A | No v2 host exposes `ctx.mcp` to packs through a fixture: v2-reference advertises `mcp` seam-gated with no pack-facing client; MyndHyve advertises `toolCatalog` but not `mcp` in v2; openwop-app neither. | Conformance Architect | `externally-gated:host-work` — v2-reference wiring `mcp.client` and the `conformance-mcp-client` fixture is the shortest path. | `Accepted`. |
| G5 | §D.12 | `annotations-derived` is vacuous on a host that emits no `annotations`, and the row says so (`inapplicable`). Nothing requires a host to emit them. | Spec Architect | `transferred:spec/v2/core/tool-catalog.md` — leave optional; revisit if an MCP server-mount projection of the catalog is specified (then the mount needs them, and the row gains a mandatory leg there). | Nothing; the acceptance box requires a non-vacuous bundle row. |
| G6 | §D.11 | "Not classified" is host-internal: the suite can only drive it with a tool the host cannot have seen before (the fake server's `conformance_readonly_claim`). A host that pre-classifies suite tools by name could pass without the default being real. | Security Architect | `transferred:conformance/src/scenarios/v2-tool-catalog-annotations.test.ts` — the fake server randomises the tool name per run (`conformance_readonly_claim_<nonce>`), which a pre-classification cannot anticipate. | Nothing. |
| G7 | §C | `fetchMCP` stays defined in v1 with its own result convention; the note supersedes it for new packs but nothing retires it. | Compatibility Architect | `transferred:spec/v2/ext/dataIntegration/README.md` — its organization decides; v1 retires at end-of-support. | Nothing. |
