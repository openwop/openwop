# RFC 0071 — Gap Register

Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner and a resolution path. Gaps with no path are promoted to the Risk register.

> **Architect-pass resolutions (2026-05-26):** **G1 RESOLVED** — in-tarball `schemaRef` source of truth + optional host-served `{HostBase}/schemas/artifacts/{id}` mirror (byte-identical). **G2 RESOLVED** — single `host.artifactTypes` object with `store`/`render`/`export[]` facets (not dotted; `export` needs a list). **G3 RESOLVED** — integer `schemaVersion` riding `capabilities.schemaVersions{}`; pack stays SemVer. **G4 RESOLVED** — distinct `kind: "card"`; a card MAY reference a prompt-pack template (composition). **G5 RESOLVED** — `exportFormats` reserved-core vocabulary + `vendor.*`/`x-` extension. **Escape-hatch RESOLVED** — unregistered types are a permanent first-class tier. **Still open:** G7 (R1 bounded-schema-compilation invariant — gates `Active`), G8 (R2 card-input trust boundary — gates Phase 2 `Active`), G9 (portable card field subset), G10/G11/G12 (impl-time), G6 (no reference host — defer per acceptance clause).

| ID  | Section                      | Question / Missing Input                                                                                                                                                                                                                                            | Owner                   | Resolution Path                                                                                                                 | Blocks                                                            |
| --- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| G1  | Proposal §Phase1             | Schema home: in-tarball `schemaRef` (drafted) vs. host-served `{HostBase}/schemas/artifacts/{id}` reference (Unresolved Q1). Affects whether artifact schemas are independently verifiable/signed.                                                                  | Spec Architect          | Maintainer decision in comment window; prototype both against `pack-registry-publish` flow                                      | Final `$id` strategy in `artifact-type-pack-manifest.schema.json` |
| G2  | Proposal §host.artifactTypes | Capability shape: single capability w/ `store\|render\|export` sub-flags (drafted) vs. three dotted capabilities `host.artifactTypes.{store,render,export}` (Unresolved Q2).                                                                                        | Spec Architect          | Align with `host.canvas.create`/`.crossInvoke` precedent; decide before §host.artifactTypes prose lands in host-capabilities.md | INTEROP-MATRIX columns; capability-gate flag names                |
| G3  | Proposal §Phase1             | Versioning axis for `artifactTypeId`: integer `schemaVersion` (drafted, envelope `schemaVersions` style) vs. semver (Unresolved Q5). Interacts with discovery `schemaVersions{}`.                                                                                   | Schema Architect        | Decision; if integer, add to `schemaVersions` advertisement contract in capabilities.md                                         | capabilities.schema.json delta                                    |
| G4  | Proposal §Phase2             | `card` as new `kind` vs. extension of existing `prompt` kind (Unresolved Q4 / Alternative 4).                                                                                                                                                                       | Spec Architect          | Compare `prompt-pack-manifest.schema.json` fields against card needs; maintainer call                                           | Whether `chat-card-pack-manifest.schema.json` is a new file       |
| G5  | Proposal §Phase1             | `exportFormats` controlled vocabulary vs. free list (Unresolved Q6).                                                                                                                                                                                                | Conformance Architect   | Draft a minimal registry table OR leave as host-advisory hints                                                                  | §host.artifactTypes `export[]` semantics                          |
| G6  | Conformance                  | No reference host implements `host.artifactTypes` today (agent-runtime gap — `docs/OPENWOP-AGENT-RUNTIME-HANDOFF.md`). Behavior scenarios sit at `host-pending`.                                                                                                    | Conformance Architect   | Build store-side into in-memory reference host OR defer per acceptance escape clause                                            | `Accepted` milestone (not `Active`)                               |
| G7  | Security                     | Distributed artifact JSON Schemas are attacker-controllable input the engine compiles (Ajv). Need schema-bomb / unbounded-`$ref` / ReDoS-in-`pattern` review.                                                                                                       | Security Architect      | Extend `threat-model-node-packs.md`; bound schema size + `$ref` depth at registry `PUT`; new invariant + public test            | `Active` status (see R1)                                          |
| G8  | Security                     | Card prompt templates with `{{placeholder}}` substitution from workflow inputs are a prompt-injection surface. Untrusted input flows into `systemPrompt`/`template`.                                                                                                | Security Architect      | Require `contentTrust` propagation per `ai-envelope.md`; document card inputs as `untrusted` unless host asserts otherwise      | Phase 2 `Active` (see R2)                                         |
| G9  | Proposal §Phase2 inputs      | The portable `inputs[].type` closed enum (`text\|longtext\|number\|boolean\|select\|artifact-ref`) is a best-guess minimal portable subset of MyndHyve's 11 `CardFieldType`s. Needs adopter validation.                                                             | Schema Architect        | Confirm with MyndHyve which field types are genuinely host-agnostic; widen/narrow enum                                          | `chat-card-pack-manifest.schema.json`                             |
| G10 | Compatibility                | `artifact.created.registered` default `true` assumes "unregistered behaves as today." Confirm no existing consumer infers schema-validity from event presence.                                                                                                      | Compatibility Architect | Audit demo app + MyndHyve `artifact.created` consumers                                                                          | Schema delta sign-off                                             |
| G11 | Process                      | README corpus counts (`N prose specs`, `N JSON Schemas`, `Draft (N)`) must sync or `openwop:check` step 7 fails. Two new spec docs + two schemas + one RFC.                                                                                                         | Implementer             | Run `npm run protocol:status`; update README counts in the impl PR                                                              | Green gate at implementation time                                 |
| G12 | Phase2 binding               | Relationship between a card's `outputArtifactType` and `WorkflowNode.cardType`/`artifactType` when a node both _runs_ a card and _produces_ its artifact — annotated with one, the other, or both?                                                                  | Spec Architect          | Worked example in `chat-card-packs.md`; align with RFC 0065 `outputRole`                                                        | Phase 2 prose clarity                                             |
| G13 | Numbering                    | RFC renumbered 0067 → 0070 → **0071** — 0067–0069 were taken when the worktree was cut (stale local base), then 0070 was claimed mid-session by a parallel session (`agent-manifest-runtime`). Registers relocated to `RFCS/registers/` per origin/main convention. | Implementer             | Resolved during implementation                                                                                                  | — (closed)                                                        |
