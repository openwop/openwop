# RFC 0150 — Gap Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §E | ~~Deployed v1 key/history inventory is absent.~~ **CLOSED 2026-08-12** — surveyed; **no host implements the Layer-2 recipe**, so no persisted v1 keys exist (reference hosts: zero `invocationId` occurrences; openwop-app: a spec-citing comment only). Migration cost is zero *while that holds*. Published at `docs/EFFECT-IDENTITY-V1-INVENTORY.md`. | Compatibility Architect | `closed` Done. | — |
| G10 | §D | `multi-region-stale-owner-no-effect` has **no black-box witness**. `multi-region-effect-vocabulary.test.ts` verifies the schema and prose no longer advertise a posture the corpus contradicts — real evidence about the *specification*, and none about any engine. Proving a host actually acquires a fencing token before issuing an effect needs a partition simulator PLUS an observable effect sink; the existing `POST /v1/host/sample/test/multi-region/simulate-partition` seam covers record convergence only, which is precisely the record-vs-effect conflation §D exists to end. | Conformance + Operations Architects | `carried:openwop.gap.0150.10` Extend the simulator seam with a counting effect sink, so a fenced host demonstrates one effect where a reconciled-records host demonstrates two. Do NOT promote the corpus gate's tier to imply host behavior. | Active — **Sweep 2026-08-16:** **Carried, unchanged** — no host advertises `crossRegion: fenced-effects`; S8 waits for one. |
| G9 | §B | `logical-effect-id-retry-stable` has **no black-box witness**, for the same reason as G8 but a different mechanism: whether an engine actually reuses the identity across an internal retry is observed by the *provider*, not the caller — the injected `Idempotency-Key` never crosses the seam a conformance driver can see. `effect-identity-composition.test.ts` therefore witnesses the **normative composition** (corpus-structural tier), which is real evidence that the spec no longer specifies the defect, and is *not* evidence that any host implements it correctly. Labelled rather than glossed, per RFC 0148 §A. | Conformance Architect | `carried:openwop.gap.0150.9` Either a host-sample seam exposing the injected key for a synthetic provider, or a provider-side echo fixture. Do NOT promote the corpus gate's tier to imply host behavior. | Active — **Sweep 2026-08-16:** **Carried, unchanged** — provider-side observation only; corpus-structural witness is what exists. |
| G7 | §B | ~~Does §B admit a business-identifier derivation or require the `logicalInvocationOrdinal` form?~~ **CLOSED 2026-08-12 — neither; it is a scope question.** `idempotency.md` §"Why this exists" scopes Layer 2 to node executors, so three of the reporting host's four entry points (HTTP route, connect admin, seeder) are **outside Layer 2 entirely** — not conforming evidence, not divergence, not its subject matter. The fourth (`feature.commerce.nodes.refund-order`) is a real divergence and a correct one: `runId` is in the preimage, so a §B-derived id could not dedupe the in-run refund against the out-of-run one. v1.4 requires a business identity **in addition** for effects reachable outside any run. Original G7 text retained below for the record. | Spec Architect | `closed` Done. | — |
| G7-orig | §B | The only production provider-idempotency path (openwop-app → Stripe refunds) derives keys from stable business ids (`refund:${orderId}`) with **no `attempt` component** — it independently implements §B's principle and ignored the spec's formula, because following the spec would have produced duplicate refunds. Adoption evidence for §B, but it embodies the principle rather than the `logicalInvocationOrdinal` wire shape. | Spec Architect | Decide whether §B's contract admits a business-identifier derivation or requires the ordinal form; record openwop-app as §B implementation evidence either way. | Active |
| G2 | §A | Canonical endpoint ID alias rules are undecided. | Spec Architect | `carried:openwop.gap.0150.2` Derive from OpenAPI operationId plus extension namespace. | Active — **Sweep 2026-08-16:** **Carried** — canonical `operationId`s are now published (`operation-path-manifest.json`, #1019), which is the derivation base the resolution path names; the alias rule itself is unwritten. |
| G8 | §A/§F | The new `idempotency-store-no-host-generated-keys` MUST NOT has **no black-box witness**: "shares no keyspace" is a storage-layout property no wire probe observes. Its behavioral projection — a caller-supplied key MUST NOT affect an operation the caller did not initiate — requires a host-sample seam (a nameable internal lock whose suppression is observable). Without one the invariant is reference-impl tier, which weakens it precisely where §A.5 wants execution evidence. | Conformance + Security Architects | `carried:openwop.gap.0150.8` Define a host-sample seam per `host-sample-test-seams.md`, or accept reference-impl tier and record it in `INTEROP-MATRIX.md`. Do NOT ship a scenario that passes without executing. | `Active` — **Sweep 2026-08-16:** **Carried, unchanged** — no host-sample seam; invariant stays reference-impl tier. |
| G3 | §C | Provider semantic-option registry is incomplete. | Provider Maintainer | `carried:openwop.gap.0150.3` Build adapter matrix and namespaced fallback rules. | Active — **Sweep 2026-08-16:** **Carried** — no provider adapter matrix. |
| G4 | §D | Provider idempotency qualification criteria need a closed test. | Security Architect | `carried:openwop.gap.0150.4` Define minimum duplicate-suppression contract and adversarial probe. | Active — **Sweep 2026-08-16:** **Carried** — no closed qualification test. |
| G5 | §E | v1 cache retention/deletion policy is unset. | Operations Architect | `carried:openwop.gap.0150.5` Align with run retention and rollback window. | Accepted — **Sweep 2026-08-16:** **Carried** — retention unset. |
| G6 | Security | CVE-class triage for cross-tenant and duplicate-effect impact is pending. | Security Maintainer | `carried:openwop.gap.0150.6` Private advisory review under SECURITY.md. | Public detail / Active — **Sweep 2026-08-16:** **Carried** — no advisory filed; `SECURITY.md` §6.2 index absent (no resolved advisory). |

