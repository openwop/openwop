# RFC 0169 — Gap register

Open design gaps discovered while authoring RFC 0169 (v2 discovery and capabilities; RFC 0167 child C.2). Keyed to the RFC; each row has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | Unresolved Q1 | The `extensions.<org>.<name>` org form (short vs reverse-DNS). | Spec Architect | `closed` — decided: the short org form. `spec/v2/declaration.json` registers it as `extensionsKeyPattern` (`^[a-z][a-z0-9]*(-[a-z0-9]+)*\.[a-z][a-z0-9]*(-[a-z0-9]+)*$`, i.e. `<org>.<name>`) with `reservedOrgs` `["openwop", "vendor"]`; `check-declaration.mjs` validates the file against `spec/v2/declaration.schema.json` on every run | RFC 0169 `Accepted` |
| G2 | §B.3 | `host-capabilities.md:2186` says a host MUST NOT advertise `host.workspace`; `agent-workspace.md:27` and `capabilities.schema.json:2979` make advertising it the gate for an Accepted surface (RFC 0059). A v1.x prose contradiction, out of this RFC's v2 scope. | Spec Architect | `carried:openwop.gap.spec.agent-workspace.r6` — a v1.x prose contradiction, out of scope for v2 and unchanged by the cut: `spec/v2/core/` never restates the reserved-slot line and the declaration file anchors `workspace` as an ordinary family, so no v2 surface carries it. The v1.x retraction of `host-capabilities.md:2186` citing RFC 0059 stays editorial and is tracked with the v1 workspace surface's own open row | — |
| G3 | §B.3 | `imageGeneration` / `videoGeneration` are prose-only under `aiProviders` (RFC 0105 G5); the gap's premise (`additionalProperties: false` on `aiProviders`) has drifted — the block is open at `capabilities.schema.json:1536`. Declared facets or deleted? | Spec Architect | `closed` — decided: declared facets, not deleted. `spec/v2/facets/aiProviders.schema.json` declares `imageGeneration` and `videoGeneration` as boolean facets and `spec/v2/declaration.json` lists both in `families[aiProviders].facets` with `facetsFrom` bound to that file, so the prose-only premise no longer holds; `check-declaration.mjs` fails a facet path the declaration cannot explain | RFC 0177 `Active` |
| G4 | §C.5 | The adoption-axis triage of RFCs 0112–0116 depends on Phase 4 bundle evidence that does not exist yet. | Conformance Architect | `externally-gated:phase-4-host-bundles` — the adoption-axis triage of RFCs 0112-0116 needs Phase 4 host bundles that do not exist at the v2.0.0-rc.1 cut; nothing in the corpus can produce them. Until one lands, `spec/v2/declaration.json` carries `adoption: none` on every affected family (`restTransport`, `a2uiSurface`, `memory`, `toolCatalog`, `aiProviders`) and `check-declaration.mjs` refuses `externally-gated` at `stable` | RFC 0169 `Accepted` |
| G5 | §C.1 | `agent-platform-aggregate-evidence.test.ts:37` reads a root `profiles[]` that no schema declares (the RFC 0085 annex claim). The v2 suite reads the generated profile registry; the v1 scenario keeps reading the field until the v1 suite is archived. | Conformance Architect | `carried:openwop.gap.spec.agent-platform-profile.r1` — the v1 scenario keeps reading the v1 field until the v1 suite is retired at v1 end-of-support (`spec/v2/core/overview.md` §"v1 end-of-support"); the field is tolerated by the open v1 root only. The v2 read is `conformance/src/scenarios/v2-profiles-derived-only.test.ts`, which reads the generated registry and fails a root `profiles[]`. Tracked with the RFC 0085 annex row that owns the claim | RFC 0168 `Active` |
