# RFC 0089 — Gap Register

Companion to `0089-conformance-certification-bundle.md`. Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner + resolution path.

| ID  | Section             | Question / Missing Input                                                                                                                                                                                        | Owner                 | Resolution Path                                                                                                                                                                                          | Blocks                                                          |
| --- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| G1 | Proposal §B(2) | The binding rule needs each profile's **required floor scenario set** mechanically. `core-standard-profile.md` §C lists them in prose; `profiles.md` predicates derive membership but not floor-scenario lists. | Spec Architect | `carried:openwop.gap.0089.1` Decide: extract floor-sets into a machine-readable manifest (possibly a small prerequisite RFC) the verifier reads, vs. encode them in `conformance/src/lib/profiles.ts`. Resolve during comment window. | `Active` (the binding rule is unverifiable without it) |
| G2 | Proposal §A | Scenario-ID format — is the suite's `file + assertion path` identifier stable across minor suite versions so `results.passed` diffs are meaningful? | Conformance Architect | `carried:openwop.gap.0089.2` Audit the vitest scenario-ID surface; if unstable, define a normalized taxonomy first. | Schema finalization (the `results.*` item shape) |
| G3 | Proposal §C | Does vitest's JSON reporter expose per-scenario terminal state + a stable ID in a form the generator can consume without internal APIs? | Conformance Architect | `carried:openwop.gap.0089.3` Spike `vitest --reporter=json` against the existing harness; confirm fields. | Generator implementation |
| G4 | Security | Should bundles be Ed25519-signed (registry trust-anchor recipe) or is reproducibility sufficient? | Security Architect | `carried:openwop.gap.0089.4` Decide signed-vs-unsigned for v1; if signed, reuse `node-packs.md` signing recipe (Unresolved Q3). | `Accepted` (changes the schema if a `signature` block is added) |
| G5 | Security | `host.commit` is self-reported; a third-party self-published bundle is unverifiable without independent re-execution. | Security Architect | `carried:openwop.gap.0089.5` Spec language: bundles are authoritative only when generated by an independent verifier (steward / CI), not the host-under-test. | Spec text |
| G6 | Conformance / hosts | Which reference host generates the first real bundle (Postgres vs SQLite), and does it require the optional `certificationBundleUrl` to be wired? | Conformance Architect | `carried:openwop.gap.0089.6` Pick Postgres (production claimant) for the first bundle at `Accepted`; `certificationBundleUrl` optional. | `Accepted` |
| G7 | Publication | Where do reference-host bundles live (alongside `conformance.md`, under `public/`, or in the registry)? Affects `certificationBundleUrl`. | Spec Architect | `carried:openwop.gap.0089.7` Decide publication surface; default = committed next to `conformance-full.md` + linked from `INTEROP-MATRIX.md`. | `certificationBundleUrl` semantics |
| G8 | Compatibility | Does the optional `conformance.certificationBundleUrl` need an OpenAPI/discovery-schema example + a `capabilities.md` prose row? | Schema Architect | `carried:openwop.gap.0089.8` Add example + prose at implementation; trivial. | Schema PR completeness |
