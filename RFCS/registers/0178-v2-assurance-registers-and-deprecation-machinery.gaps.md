# RFC 0178 — Gap register

Open design gaps discovered while authoring RFC 0178 (v2 assurance registers and deprecation machinery; RFC 0167 child C.11). Keyed to the RFC; each row has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
| --- | --- | --- | --- | --- | --- |
| G1 | §B.1 | 558 of 558 `gaps.json` rows were `witness: unclassified` and `requirementId: null` at filing. This PR classifies the RFC 0167 family's rows; the rest is the RFC 0166 G2 ratchet. | Security Architect | `carried:openwop.gap.0166.2` — all 57 v2-era rows (RFC 0167 and later) carry a witness class, none `unclassified`, and `generate-gaps.mjs --check` gates them; the v1 backlog stays under the RFC 0166 G2 ratchet at 545 unclassified of 801 and falls as registers are swept. `requirementId` binding is a separate leg and is still null on every v2 row | RFC 0178 `Accepted` |
| G2 | §C.1 | Nine of 164 RFCs carry a falsifiability table; the parser is a gate for the RFC 0167 family and advisory elsewhere. | Conformance Architect | `closed` — `scripts/check-falsifiability.mjs` is a hard gate for the RFC 0167 family in `scripts/openwop-check.sh` (step 4): a family member with no `### Falsifiability` table, a verdict outside the closed set, an unexplained `unwitnessable`, or a named scenario file that does not exist all fail the run. 12 family tables parse today; v1 RFCs stay advisory and are not re-adjudicated | RFC 0178 `Accepted` |
| G3 | §B.3 | `check-gap-contradictions.mjs` resolves scenario files and OpenAPI paths only; a gap naming any other artifact is reported unchecked. | Conformance Architect | `carried:openwop.gap.0166.4` — `check-gap-contradictions.mjs` prints its unchecked count on every run (139 of 155 open gaps name no resolvable artifact today) and gains resolver classes as the audit finds them; scenario files and OpenAPI paths are the two it resolves. Tracked with the RFC 0166 row for register prose the machinery cannot bind | — |
| G4 | Unresolved Q1 | Whether AsyncAPI 3.x carries a standard `deprecated` on channels and messages. | Spec Architect | `closed` — decided: `x-openwop-remove-in` is the portable carrier, written beside `deprecated: true` by `scripts/generate-deprecation-annotations.mjs` from `spec/v1/deprecations.json`; `--check` runs in the gate (`scripts/openwop-check.sh` stage 10) and AsyncAPI's own `deprecated` keyword is not relied on | RFC 0178 `Accepted` |
