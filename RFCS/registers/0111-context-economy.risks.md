# RFC 0111 — Risk Register

Score = Likelihood × Impact (H/M/L).

| ID  | Risk                                                                                                          | Likelihood | Impact | Score | Mitigation                                                                                          | Owner                 | Status |
| --- | ----------------------------------------------------------------------------------------------------------- | ---------- | ------ | ----- | -------------------------------------------------------------------------------------------------- | --------------------- | ------ |
| R1  | Host-summarized turns are nondeterministic → replay (RFC 0041) diverges, producing a different transcript    | M          | H      | High  | **Normative (architect 2026-06-26):** summarization governed exactly like an RFC 0041 LLM envelope — on `mode:replay` the host MUST reuse the recorded `summaryRef` artifact at seq ≥ fromSeq and MUST NOT re-summarize; summary artifact persisted/readable as-of the index per the MAE-3 analogy, else refuse the fork. `context-summarization-replay.test.ts` verifies. | Spec/Security         | Mitigated |
| R2  | `tokenCounter` mismatch between host's real provider tokenizer and the advertised enum → budget is "honest but wrong" | M    | M      | Med   | Document that the bound is in the *advertised* unit; conformance measures against that unit, not the provider's | Conformance Architect | Open   |
| R3  | Summarization silently drops task-critical context → agent quality regression blamed on the protocol          | M          | M      | Med   | Normative: active turn never summarized; `keepLastTurns` verbatim floor; non-normative note to pair with a verifier turn (RFC 0090) | Spec Architect        | Open   |
| R4  | Hosts advertise `contextBudget` but ship suite version without the new scenarios → INTEROP-MATRIX drift        | L          | M      | Low   | Gate scenarios on the capability flag; suite-version requirement, not spec requirement              | Conformance Architect | Open   |
| R5  | Seam returns a self-reported number a host can fabricate → vacuous pass                                        | M          | M      | Med   | Scenario must drive enough turns to exceed an unbounded transcript and cross-check `eventIds` count against the run log | Conformance Architect | Open   |
