# RFC 0147 — Risk Register

> **Acceptance sweep 2026-08-16 (RFC 0147 criterion 12, S11).** Every row below carries a `Sweep 2026-08-16` disposition against the evidence on `main` at that date — closed / carried (with the pointer) / externally gated. Rows without one were already struck through as closed. This is a sweep, not a rewrite: original text is preserved.


Working document (not normative). Likelihood and impact use H/M/L. High/Critical risks require a named owner and target date before RFC 0147 may become `Active`.

| ID | Risk | Likelihood | Impact | Score | Mitigation | Owner | Target / Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | Existing certification bundles materially overstate behavior because required paths returned early. | H | H | **Critical** | Ship SR-1 first; inventory, invalidate, and reissue affected bundles; publish exact scope. | Conformance Architect | Target: before next certification publication; Open — **Sweep 2026-08-16:** **Realised and remediated in scope:** the one historic bundle overstated (Bundle 1 invalidated); ledger + rejection now prevent recurrence (#1018); reference hosts reissued as v2 (`openwop-examples#14`) with `openwop-node-packs` REJECTED on all four — the exact scope is published. Status → **Mitigated; monitor** (zero-assertion passes still 107–143 per host). |
| R2 | Correcting activity identity or replay digests silently changes interpretation of stored histories. | H | H | **Critical** | Version recipes, pin per run/event, dual-read only where proven safe, migration tooling and adversarial vectors. | Compatibility Architect | Target: SR-3 `Active`; Open — **Sweep 2026-08-16:** **Mitigated by evidence:** no stored histories use the retired recipe (RFC 0150 G1); digest v2 is versioned (`recipe: openwop-semantic-request-v2`) with golden vectors. Open only for the Python/Go vector consumers (`openwop-sdks`). |
| R3 | Split-brain hosts duplicate payments/messages before a losing run is cancelled. | M | H | **Critical** | Fencing/effect ownership plus effect-level idempotency; partition tests; add compensation as defense, not primary prevention. | Security Architect | Target: SR-3 `Accepted`; Open — **Sweep 2026-08-16:** **Open — unwitnessed.** §D separates ownership from reconciliation on paper; no host fences and no black-box witness exists (RFC 0150 G8–G10). Compensation profile exists as defense (RFC 0151). |
| R4 | The remediation program expands the corpus further without improving implementability. | H | M | **High** | Freeze optional growth, enforce child decomposition, stable-core manifest, extension budget, independent evidence. | Lead Maintainer | Target: RFC 0147 `Active`; Open — **Sweep 2026-08-16:** **Mitigated:** §A freeze in force (no new optional wire capability landed since 0147; 0157 is a §A-compliant errata); core-standard manifest + extension registry + coverage gate landed. Extension budget itself still uncalibrated (RFC 0155 G1). |
| R5 | A single umbrella RFC obscures incompatible compatibility classes. | M | H | **High** | No wire change lands directly from 0147; every workstream requires a child RFC and separate classification. | Compatibility Architect | Target: each child intake; Mitigated by design |
| R6 | The OpenAPI correction breaks a client that worked around `/v1/v1`. | M | M | **Medium** | Survey generated clients, document canonical endpoint, temporarily detect/warn on legacy URL in reference hosts if necessary. | SDK Maintainer | Target: SR-2 `Accepted`; Open — **Sweep 2026-08-16:** **Closed** — no client had worked around `/v1/v1` (RFC 0149 UQ1). |
| R7 | Tightening discovery schema closure breaks legal v1 extensions. | M | H | **High** | Keep runtime server-emitted shapes open in v1; catch canonical typos in authoring/certification lint; reserve strict closure for v2. | Schema Architect | Target: SR-2 `Active`; Open — **Sweep 2026-08-16:** **Mitigated by design** — runtime schemas stay open; only authoring/certification lints landed (`capability-example-root-layout`, `discovery-canonical-family-no-shadow`, `normative-example-extraction`). |
| R8 | Compensation is marketed as rollback or atomicity. | M | H | **High** | Normatively call it best-effort durable compensation; expose partial/manual states; add claims test. | Spec Architect | Target: SR-4 `Active`; Open — **Sweep 2026-08-16:** **Mitigated in prose** — `compensation.md` names best-effort durable compensation, `partial`/`manual` states, no rollback inferred; the claims-test (RFC 0156 §F) is not built. |
| R9 | A compensating action causes a second harmful effect or leaks authority. | M | H | **Critical** | Separate idempotency, delegated identity, least privilege, approvals, egress policy, audit, and DLQ; external audit review. | Security Architect | Target: SR-4 `Accepted`; Open — **Sweep 2026-08-16:** **Open — unwitnessed.** Prose separates identity/approval/egress/audit/DLQ (`compensation.md` §E carried per its G2); no host advertises `compensation.supported`; external audit unscheduled. **Later 2026-08-16:** §E landed (approval on the plan entry, RFC 0049 binding to tenant/principal/action/`planVersion`, four audited override actions), `SECURITY/threat-model-compensation.md` A1–A9, all four §G invariants registered against `compensation-behavior` / `compensation-recovery` (§21 recovery extension: retry-stable key, 404/403-audited/200 operator triple, replay ≡ source), UQ4 `irreversibleEffect`. Still no host advertises, so still unwitnessed — status unchanged, evidence path complete. |
| R10 | A2A/MCP upstream protocols change again during implementation. | M | M | **Medium** | Explicit versioned profiles, compatibility matrix, upstream-version pins, scheduled refresh SLA, no generic support flag. | Interop Maintainer | Target: SR-5/SR-6 `Active`; Open — **Sweep 2026-08-16:** **Mitigated:** exact versioned profiles (`a2a-1.0`, `mcp-2026-07-28`) with upstream pins (`a2a.proto@v1.0.0`, MCP 2026-07-28) and dual-era suite peers; refresh SLA not written. |
| R11 | Dual legacy/current interop profiles create downgrade attacks. | M | H | **High** | Authenticated negotiation, minimum-version policy, audit downgrade, fail closed when policy forbids legacy. | Security Architect | Target: SR-5/SR-6 `Accepted`; Open — **Sweep 2026-08-16:** **Mitigated in suite:** `a2a-version-no-silent-downgrade` / `mcp-version-no-silent-downgrade` invariants + negotiation witnesses; host-side legs `blocked` on every current host (no invoke seams). |
| R12 | Workload provenance is mistaken for authorization and creates a confused deputy. | M | H | **Critical** | State identity ≠ authorization; re-evaluate tenant/audience/policy at every boundary; add negative delegated-chain tests. | Security Architect | Target: SR-7 `Accepted`; Open — **Sweep 2026-08-16:** **Mitigated in prose + suite:** identity ≠ authorization stated (`auth.md`, `a2a-integration.md` §E, `mcp-integration.md` §E); `delegation-tenant-audience-bound` / `delegation-chain-bounded` invariants + `a2a-peer-authority` / `mcp-extension-opacity` legs; **no advertiser** — behavioural rows `blocked`. |
| R13 | Experimental OTel GenAI conventions churn and destabilize the core taxonomy. | M | M | **Medium** | Keep stable `openwop.*` canonical; make external mappings versioned and optional. | Observability Architect | Target: SR-7 `Active`; Open — **Sweep 2026-08-16:** **Mitigated by decision** — RFC 0154 G3 (mapping v0 experimental/optional; `openwop.*` canonical). |
| R14 | Governance requirements cannot be satisfied because no independent maintainers volunteer. | H | H | **Critical** | Do not waive the gate; pause standards claims and optional RFC growth; fund outreach and publish the capacity constraint. | Lead Maintainer | Target: before RFC 0147 `Accepted`; Open — **Sweep 2026-08-16:** **Open, Critical, externally gated** — one maintainer as of 2026-08-16; standards claims stay qualified (RFC 0147 §A phrase ban); no waiver. |
| R15 | Security-audit funding or vendor availability delays the program. | M | H | **High** | Set budget/authority now, solicit multiple vendors, stage design review then implementation retest, keep claims qualified. | Lead Maintainer | Target: contract before SR-3 `Accepted`; Open — **Sweep 2026-08-16:** **Open, externally gated** — no vendor, no start date (`SECURITY.md` §9). |
| R16 | A sponsored Tier-3 host is perceived as steward-controlled. | M | M | **Medium** | Independent repository, maintainers, release control, deployment, and public evidence; disclose funding/support. | Governance Maintainer | Target: first Tier-3 result; Open — **Sweep 2026-08-16:** **Open** — no Tier-3 host exists to be perceived either way. |
| R17 | Retrospective review reopens many accepted RFCs and destabilizes v1. | M | H | **High** | Risk-rank cohorts; corrections use safety-fix/additive rules; non-safety breaks queue for v2; publish decision log. | Governance Maintainer | Target: SR-9 `Accepted`; Open — **Sweep 2026-08-16:** **Open** — retrospective review not started (RFC 0156 G3). |
| R18 | Publicly documenting critical defects creates exploitation risk before fixes ship. | M | H | **High** | Triage through GitHub Security Advisories; embargo exploit detail when CVE-class; keep public RFC at design/mitigation level. | Security Maintainer | Target: immediate triage; Open — **Sweep 2026-08-16:** **Mitigated** — no CVE-class detail was published; program defects were spec/verifier defects, not exploitable host behaviour; advisory ledger empty. |
| R19 | “A-grade” becomes a one-time badge rather than a maintained evidence state. | H | M | **High** | Machine-readable evidence expiry, scheduled standards refresh, annual audit/retest, current Tier-3 suite reports. | Release Maintainer | Target: Workstream 9; Open — **Sweep 2026-08-16:** **Open** — evidence expiry undecided (RFC 0156 G5); bundles carry `generatedAt` + suite version, nothing expires them. |
| R20 | Reference-host success is again mistaken for independent protocol proof. | H | M | **High** | Evidence-tier labels are mandatory; Tier-1/Tier-2 cannot satisfy Tier-3 gates. | Governance Maintainer | Target: immediate claims policy; Open — **Sweep 2026-08-16:** **Mitigated in text:** `GOVERNANCE.md` tier taxonomy + `INTEROP-MATRIX.md` evidence vocabulary (2026-08-16); tier-1/2 evidence labelled as such throughout. |
