# RFC 0128 — Gap Register

Companion to `0128-purpose-propagation-permitted-use-labels.md`. Open questions, deferred decisions, and missing inputs beyond the in-template Open questions. Each has an owner + a resolution path; a gap with no path is promoted to a Risk.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Open Q1 | **RESOLVED 2026-07-06** — no provenance hint; bare purpose set (provenance is subject-linkable; audit trail lives in the host-local governance decision log). Reference-implementer review concurred; folded into RFC §Resolved Q1. | Spec Architect + reference implementer | — | — |
| G2 | Open Q2 | **RESOLVED 2026-07-06** — mint `capabilities.purposePropagation`; steward + reference implementer independently confirmed no existing consent/governance family to ride. Folded into RFC §Resolved Q2. | Schema Architect | — | — |
| G2a | Proposal §3(a) | **RESOLVED 2026-07-06** — reference-implementer review (Criterion 2): the re-emit MUST was structurally unsatisfiable for non-OpenWOP sinks (reverse-ETL ESP/webhook has no OpenWOP envelope). §3 re-scoped: MUST on OpenWOP-envelope hops only; non-OpenWOP destination = field-mapping SHOULD (untestable leg, §4 falsifiability logic). | Spec Architect | — | — |
| G3 | Conformance | Two-hop observability harness — the `purpose-propagation-onward` scenario needs the suite to play hops A (labelled sender) and C (onward receiver) around the host at B. Confirm the seam shape: an inbound labelled ingest + an observable egress (destination-sync or A2A forward) the suite can capture. | Conformance Architect | Define the seam contract with the reference host before scenario authoring; non-vacuity = the label observed at C, not just accepted at B. | Scenario authoring |
| G4 | Acceptance | Tier-2 witness — architect ruling 2026-07-06: MyndHyve MUST be attempted first (it already carries both label surfaces: A2A from the 0100 arc, trigger-event metadata from 0083/0099); an honest opt-out downgrades to single-witness + this row carried forward as the named gap. | Steward | Coordinate the MyndHyve arm; record attempt-or-opt-out in the sweep. | `Accepted` |
| G5 | Proposal §3 | **RESOLVED 2026-07-06** — derived outputs combining differently-labelled records SHOULD carry the intersection of contributing labels (the only never-widening composition); prose clause landed in §3's narrow/widen bullet. | Spec Architect | — | — |
| G6 | Conformance | Observing the `[]` fail-closed rule — asserting "does NOT forward" is a negative assertion; the scenario needs a bounded observation window + a positive control (an unlabelled twin record that DOES arrive) so the non-arrival is evidence, not a timeout artifact. | Conformance Architect | Pattern the scenario on the existing fail-closed legs (0120 off-allow-list, 0126 unadvertised-capability). | Scenario authoring |
