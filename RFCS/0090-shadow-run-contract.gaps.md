# RFC 0090 — Gap Register

Companion to `0090-shadow-run-contract.md`. Open questions, deferred decisions, and missing inputs beyond the in-template Unresolved questions. Each gap has an owner + resolution path.

> **RECONCILED 2026-06-06.** The RFC was returned to `Draft` and re-scoped to compose RFC 0081 (`live-shadow` eval + `EvalSummary`) + RFC 0082 (promotion gate) + RFC 0054 (diff) instead of inventing a parallel `capabilities.shadow` / `shadow.*` / `ShadowComparison` surface. Gaps G1–G8 below were written against that removed surface and are **superseded** by the single open question (Unresolved Q1): *is there any net-new wire surface, or does this RFC fully fold into RFC 0081 §D + RFC 0082 §E?* They are retained only for history.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | Proposal §B | External-baseline ingestion shape — `outcome` supplied at run creation only, or also a post-hoc callback (push the legacy result after the agent run)? No pilot has exercised the callback path. | Spec Architect | Decide during comment window; default to at-creation `outcome` for v1, add callback as a follow-up if the invoice-exception pilot needs it. | `Active` (changes the `shadow.baseline` schema if callback is added) |
| G2 | Proposal §D/§E | Canonical read surface: `RunSnapshot.shadow`, the `GET /v1/runs/{id}/shadow` endpoint, or both — and which is authoritative if they ever disagree? | Schema Architect | Pick one authoritative source (lean: endpoint authoritative, snapshot a cached mirror) before `Active`. | Schema + OpenAPI finalization |
| G3 | Proposal | `overrideRate` definition with no human in the shadow loop. Is "override" only meaningful when the baseline is a HITL decision, or is it derived from a policy boundary (e.g. amount > threshold)? | Spec Architect | Define against the invoice-exception pilot's actual override signal; document the formula in `shadow-run.md`. | `Active` (the metric is unspecified without it) |
| G4 | Proposal | Comparison strategy for non-deterministic / free-text NL outputs (`exact`/`keyed` only cover structured outputs). | Conformance Architect | Defer a `semantic`/`fuzzy` strategy to a separate pilot with NL outputs; keep v1 to structured. | A non-structured-output pilot |
| G5 | Security | Digest recipe for `shadow.diverged` — SHA-256 over what canonicalization (JCS? sorted-key JSON)? Must match cross-host so two hosts agree a value diverged. | Security Architect | Reuse the canonicalization the replay LLM-cache-key recipe uses (`replay.md §B`) for consistency; pin it in `shadow-run.md`. | Conformance scenario `shadow-comparison-content-free` |
| G6 | Conformance / hosts | Which reference host implements shadow first (Postgres vs in-memory), and does it implement `replay` baseline (needs working replay/fork) or only `external`? | Conformance Architect | Defer reference-host impl until the demo pilot stabilizes shapes; first impl likely the host with the strongest replay support. | `Accepted` |
| G7 | Compatibility | Interplay with RFC 0041 §C (replay determinism under non-deterministic models) when the baseline leg replays a workflow whose nodes call non-deterministic models. | Compatibility Architect | Cross-reference RFC 0041; the baseline leg inherits its determinism caveats. Resolve when a replay-baseline pilot lands. | `replay`-mode baseline guarantees |
| G8 | Proposal §B | HITL inside the shadow leg — do the agentic run's interrupts pause/annotate the comparison, or is shadow strictly non-interactive? | Spec Architect | Decide against the pilot; default to non-interactive for v1. | Spec text on the agentic leg's lifecycle |
