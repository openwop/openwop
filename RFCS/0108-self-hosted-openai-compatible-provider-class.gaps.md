# RFC 0108 — Gap register

Open questions, deferred decisions, and missing inputs beyond the in-RFC Unresolved questions. Each gap has an owner and a resolution path.

| ID | Section | Question / Missing Input | Owner | Resolution Path | Blocks |
|---|---|---|---|---|---|
| G1 | §A / Schema | Ship the minimal `selfHosted[]` subset, or the richer `providerClasses` map (`managed-cloud`/`byok-cloud`/`byok-self-hosted` + `capabilitySource`)? (RFC Unresolved Q1.) | Schema Architect | Decision in comment window; recommend flag-now (parity with `byok[]`), promote to a map additively if a host needs the managed-vs-byok-cloud wire distinction. | Schema diff freeze → Active |
| G2 | §B | Is RFC 0031 `modelCapabilities.advertised[]` host-scoped enough to express a *per-self-hosted-endpoint* capability set, or do multiple compat endpoints with differing capabilities need per-id capability advertisement? | Spec Architect | Audit RFC 0031 §E granularity against ADR 0121's per-connection declared caps; if insufficient, file a follow-on (per-provider `modelCapabilities`), NOT a blocker for this RFC's advertisement field. | Active (capability-source clarity) |
| G3 | §A.2 | Exact `OPENWOP_REQUIRE_BEHAVIOR` check for the honesty rule: "reachable" = a successful handshake (a `/v1/models` or tiny completion) at advertise time, or merely "a Connection is configured"? | Conformance Architect | Pin in the `aiproviders-selfhosted-honesty.test.ts` design; recommend "configured AND a cheap liveness check at advertise time," gated so offline endpoints soft-skip rather than hard-fail the suite. | Active → Accepted (honesty scenario) |
| G4 | §D / Security | Is `medium` the right severity for endpoint-location disclosure, or does an internal-network endpoint (vs a public one) warrant `high`? | Security Architect | Confirm against `threat-model-secret-leakage.md` scoring; recommend `medium` (topology/recon, not key material) with a note that operators of internal endpoints SHOULD treat it as higher locally. | Active (invariant text freeze) |
| G5 | §E | The honesty scenario needs the host's endpoint location out-of-band to assert non-disclosure — what test-seam delivers it without itself disclosing the URL on the wire? | Conformance Architect | Reuse the per-host conformance-config seam (test supplies the expected URL fragment locally; the suite asserts its *absence* from wire payloads). Spec the seam in `fixtures.md`. | Accepted (behavioral scenario) |
| G6 | INTEROP-MATRIX | Which existing advertised host (beyond openwop-app) can serve as the dual-witness for the class, or is the bootstrap steward waiver the path? | Compatibility Architect | Survey INTEROP-MATRIX hosts for any compat-endpoint support; if none, take the bootstrap-phase steward waiver (RFC 0031/0046/0095 precedent) with openwop-app as the reference host. | Accepted (third-party gate) |
| G7 | §A.3 | Should the spec recommend a canonical opaque-id convention (e.g. `compat` / `compat:<label>`) to discourage URL-encoding, or leave the label fully host-chosen? | Spec Architect | Add a non-normative SHOULD example in `capabilities.md` (`compat:<label>`); keep it advisory so hosts with existing ids aren't broken. | Active (spec prose) |
