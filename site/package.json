{
  "name": "@openwop/openwop-site",
  "version": "1.1.0",
  "description": "Static-site generator for the OpenWOP public docs and conformance leaderboard. Outputs to dist/ with zero runtime dependencies.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "serve": "node src/serve.mjs",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20"
  }
}
