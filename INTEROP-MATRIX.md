# openwop Interop Matrix

> **Last updated:** 2026-05-24 — **RFC 0039 Half B fully closed end-to-end**: MyndHyve commit `560cfc89` (Cloud Run revision `workflow-runtime-00362-yoz` now serving 100% on `api.myndhyve.ai`) lands the `replay_memory_snapshot_unavailable` 422 wire-route surface that had been the long-standing parallel-session blocker. Three coordinated pieces: (1) engine wiring in `runExecutor.ts` selects `MyndHyveMemoryResolver.forFork(forkedFrom.runId)` for replay-mode dispatches; (2) route pre-flight `checkReplayMemorySnapshotPreflight` at `POST /v1/runs/{runId}:fork` uses the same `forFork(sourceRunId)` construction so the gate truthfully predicts dispatch behavior (no probe-vs-dispatch dishonesty); (3) envelope locked with `details.{fromSeq, sourceRunId, reason}` where `reason ∈ {retention_expired, event_log_unavailable}`. Status of RFC 0039 unchanged — already Accepted. MyndHyve confirms `x-host-myndhyve-memory-written` stays as the canonical vendor-extension event-type literal (no canonicalization RFC requested). Prior 2026-05-23 — **late update: RFC 0034 Active → Accepted on MyndHyve OTel test-seam adoption** (revision `workflow-runtime-00196-7mm`, commit `60b569de` wires `registerHostSampleRoutes()` — see Honest Correction note in the multi-agent section). RFC 0028 Tier-1 + RFC 0029 Tier-1 also landed but stay Active (paths-to-Accepted require Tier-2 `packsSupported: true` / `mutableLibrary: true` / `agentBindings: true` advertisements not yet shipped). **Earlier 2026-05-23 batch:** RFC 0027 Active → Accepted + RFC 0039 Half B host implementation landed on MyndHyve prod; MyndHyve `x-host-myndhyve-memory-written` vendor-extension event type locked per commit `21fb2387` (forward-reference row added below). MyndHyve commit `5385548c` lands the prompt-compose seam end-to-end with SR-1 + `<UNTRUSTED>` wrapping; `prompts.observability: 'full'` now backed by real emission (verified live on `https://api.myndhyve.ai/.well-known/openwop`). MyndHyve commit `a51f7bbd` lands Half B host primitive (now fully closed via `560cfc89` route wiring above). Prior 2026-05-22: 3 RFCs Active → Accepted (RFC 0037 Phase 1 + RFC 0039 Half A + RFC 0044) from a single Cloud Run conformance run — MyndHyve workflow-runtime revision `workflow-runtime-00353-rab` against `@openwop/openwop-conformance@1.5.0` (2 passed / 2 fixture-gated skips / 0 failed). RFC 0044's `x-host-<host>-<kind>` vendor-extension pattern demonstrated end-to-end — host with entrenched vendor semantics passes conformance without cross-cutting rename. Prior 2026-05-22 RFC 0037 Phase 1 promoted on version: 1 advertisement signal (revision `workflow-runtime-00352-geh` against `@openwop/openwop-conformance@1.4.0`). Prior 2026-05-21 RFC 0030/0031/0032/0033 envelope LLM-contract-hardening track promoted Active → Accepted under bootstrap-phase steward waiver; cross-host validation closed by MyndHyve workflow-runtime adoption + reference workflow-engine emission. Prior 2026-05-15 gap-closure batch closes CF-5/6/8/10/11/12 + OPS-1/2/5 + PACK-1/2/3/4/5 + SDK-3/5 + SEC-3/6 + STD-3/5/6 + DOC-4/5 + MA-1/6/7 alongside Postgres host test-suite wiring fix; prior Phase I.3 + I.4 close-out — OAuth2-CC + OIDC user-bearer JWT validators — remains in effect)
> **Conformance suite:** `@openwop/openwop-conformance`

This matrix records public, reproducible compatibility evidence for openwop-compatible hosts. A row is a claim plus evidence: the host advertises a profile set, runs the conformance suite, and links to a result that another implementer can inspect.

> **Submit your host.** Run `@openwop/openwop-conformance` against your implementation, publish the result alongside your repo, and open a PR adding a row below. See [`conformance/README.md`](https://github.com/openwop/openwop/blob/main/conformance/README.md) for the suite, and the [Independent-implementation tripwire](https://github.com/openwop/openwop/blob/main/ROADMAP.md) tracker for hosts in flight.

> **How to read "Not claimed":** the reference hosts in `examples/hosts/` are minimal demos of the wire shape, not production-grade servers. A "Not claimed" profile means *this reference host doesn't wire it up* — not that the protocol surface is incomplete. Every profile listed in `spec/v1/profiles.md` has a normative RFC, capability schema, conformance scenario, and behavior gate; an adopter wiring the profile in their own host can validate end-to-end against `@openwop/openwop-conformance` by setting the scenario's documented env vars (e.g., `OPENWOP_TEST_OIDC_ISSUER_URL`, `OPENWOP_TEST_MTLS=1`). Profile coverage on the reference hosts is a product-of-scope decision, not a protocol-readiness signal.

## Hosts

| Host | Use case | Repo / Path | Compatibility profile claim | Scale claim | Production profile claim | Conformance link |
|---|---|---|---|---|---|---|
| **In-memory** (reference example) | Local development / fastest boot / no persistence | `examples/hosts/in-memory/` | `openwop-core` · `openwop-stream-sse` · `openwop-stream-poll`. **RFC 0013 Phase 3 — `workflowChainPacks` (2026-05-18):** host advertises `capabilities.workflowChainPacks.supported: true` and serves the vendor-prefixed `POST /v1/host/sample/workflow-chain:expand` endpoint that resolves a chain pack from a filesystem-mounted registry mirror, verifies the optional Ed25519 signature, and runs the spec-authoritative 9-step expansion algorithm (steps 1–9 of `workflow-chain-packs.md §"Expansion semantics (normative)"`). 6-case live-host conformance scenario `workflow-chain-host-expansion.test.ts` passes under `OPENWOP_REQUIRE_BEHAVIOR=true`. Closes the last gate on RFC 0013 → `Accepted`. | `minimal` | Not claimed | `examples/hosts/in-memory/conformance.md` |
| **SQLite** (reference example) | Single-machine durability / process-restart-safe walkthrough | `examples/hosts/sqlite/` | `openwop-core` · `openwop-stream-sse` · `openwop-stream-poll` · `openwop-audit-log-integrity` · `openwop-interrupt-quorum` · `openwop-interrupt-auth-required` · `openwop-interrupt-external-event` · `openwop-interrupt-cascade-cancel` · `openwop-auth-api-key-rotation` · `openwop-discovery-auth-scoped`. **Conformance posture (updated 2026-05-12 after Phase D close-out, suite v1.1.0):** `openwop-auth-api-key-rotation` is **verified end-to-end** (two-key overlap + canary-redaction + grace-window advertisement, exercised via `OPENWOP_SECONDARY_API_KEY` against the host's constant-time dual-candidate `checkAuth`). `openwop-discovery-auth-scoped` is **verified end-to-end** (same-endpoint mode; `OPENWOP_TENANT2_API_KEY` wires a synthetic second principal whose discovery view OMITS `orchestrator` + `dispatch` — strict subset of primary's view per `capabilities-change-detection.md` §"Scoped capability views" line 69; 3-subtest behavior probe in `discovery.test.ts` passes under `OPENWOP_REQUIRE_BEHAVIOR=true` + `OPENWOP_TEST_UNAUTHORIZED_API_KEY=<tenant2-key>`). RFC 0006 + RFC 0007 (`core.orchestrator.supervisor` + `core.dispatch` with causationId propagation per §E) verified end-to-end via `dispatchLoop.test.ts`. The host explicitly does NOT claim `openwop-production`, `openwop-auth-oauth2-client-credentials`, `openwop-auth-oidc-user-bearer`, or `openwop-auth-mtls` — those profiles require backpressure/retention enforcement, JWT validation, or TLS termination that the reference HTTP listener does not implement (per the honesty principle: advertise only what behavior exists). | `minimal` | Not claimed | `examples/hosts/sqlite/conformance.md` |
| **Python in-memory** (reference example) | Cross-language portability proof / Python 3.11 stdlib-only port of the TypeScript in-memory host. **Conformance close-out (2026-05-12, suite v1.1.0):** 700/788 = **100% of applicable tests pass; zero failures** against `@openwop/openwop-conformance@1.1.0` — retained for historical context; see the v1.4.0 re-measurement in the pass-rate table below (`docs/CONFORMANCE-RUNS-2026-05.md` for the per-failure-topic taxonomy). From the 53-failure post-Phase-C-round-2 baseline, every controllable failure closed via: canonical `RunEventDoc` shape on event emission (closes 3 version-negotiation); `?streamMode=` + `?bufferMs=` validation + SSE `Connection: close` (closes 12 stream-modes); pause/resume reshaped to 202 + `pausedAt`/`resumedAt` + 409 with `conflict` + idempotent re-pause + `core.delay` drain-on-pause semantics (closes 5 pause-resume); `IdempotencyCache` per-key locks across the create-run get→create→put (closes the highConcurrency race); webhook codes restored to spec-de-facto (`webhook_url_rejected` for SSRF, `subscription_not_found` for unknown DELETE); discovery `fixtures[]` filtered to executable + enforcement-blocklisted set (24+ FAIL→SKIP); `GET /v1/workflows/{id}` + `/v1/packs/*` non-OpenWOP-shaped 404 catch-all. Phase C round 2 surfaces from earlier this day remain: pause/resume + bulk-cancel + `capability_required` refusal + webhooks (HMAC-SHA256({ts}.{rawBody})) — all stdlib-only. Honestly does NOT claim `openwop-audit-log-integrity` (stdlib lacks Ed25519). | `examples/hosts/python/` | `openwop-core` · `openwop-stream-sse` · `openwop-stream-poll` | `minimal` | Not claimed | `examples/hosts/python/conformance.md` |
| **Postgres** (reference example) | Multi-process durability path + first host advertising `production-profile.md`. **Phase H launch-blocker batch (2026-05-12)** added six surfaces required for myndhyve.ai production: BYOK secret resolver + `aiProviders` with 4-mode policy enforcement (`disabled`/`optional`/`required`/`restricted`) for `core.llm.chat`/`core.llm.completion`, MCP client (`core.mcp.toolCall` over HTTP/JSON-RPC with `trustBoundary: "untrusted"` per threat-model-prompt-injection.md §UNTRUSTED), HTTP client (`core.http.request` with SSRF guard + 1 MiB response cap), recursion-limit enforcement (`cap.breached` event + terminal `failed` when configurable.recursionLimit exceeded), per-workflow configurableSchema validation, and the SR-1 + MCP-1 redaction invariants (host-side hashing of secrets/tool-args; raw cleartext never on event/variable/log surfaces). Wire-surface parity with SQLite (orchestrator + dispatch + bulk-cancel + capability_required refusal + causationId propagation + canonical `core.subWorkflow` output shape; existing surface: audit + 4 interrupt profiles + webhooks + SSE + observability + debug-bundle + pause/resume) plus production-shape guarantees (session-level advisory-lock claim acquisition + orphan recovery, 503/Retry-After backpressure, 7-day event retention sweeper, structured terminal logs). | `examples/hosts/postgres/` | `openwop-core` · `openwop-stream-poll` · `openwop-stream-sse` · `openwop-audit-log-integrity` · `openwop-interrupt-quorum` · `openwop-interrupt-auth-required` · `openwop-interrupt-external-event` · `openwop-interrupt-cascade-cancel` · `openwop-production` · `openwop-auth-oauth2-client-credentials` (conditional on `OPENWOP_OAUTH2_ISSUER_URL` + `OPENWOP_OAUTH2_AUDIENCE`) · `openwop-auth-oidc-user-bearer` (conditional on `OPENWOP_OIDC_ISSUER_URL` + `OPENWOP_OIDC_AUDIENCE`) · `openwop-auth-api-key-rotation` (conditional on `OPENWOP_SECONDARY_API_KEY`) · `openwop-discovery-auth-scoped` (conditional on `OPENWOP_TENANT2_API_KEY`). **Capability advertisements added in Phase H:** `capabilities.secrets` (host-managed, tenant scope), `capabilities.aiProviders` (anthropic/openai/gemini supported + byok + 4-mode policies), `capabilities.mcpClient` (http+jsonrpc transport, trustBoundary: untrusted), `capabilities.httpClient` (6 methods, ssrfGuard: true, maxResponseBodyBytes: 1 MiB). Two new protocol-tier SECURITY invariants land alongside: `mcp-toolcall-payload-redaction` + `http-client-ssrf-guard`. **Phase I enterprise-blocker MVP (2026-05-12)** added four more: `capabilities.memory` (RFC 0004 MemoryAdapter — read-side `list`/`get` with TTL + CTI-1 cross-tenant isolation enforced via tenant_id filtering on every query; host-internal `writeMemoryEntry` enforces 65 KiB content cap + SR-1 placeholder substitution before persistence); `capabilities.agents` (Phase 1-6 — profile `wop-agents-full`, modelClasses, orchestrator + dispatch flags, reasoning verbosity governance per `RunOptions.configurable.reasoningVerbosity` precedence: run → host → fallback "summary"); `capabilities.auth.rotation` (when `OPENWOP_SECONDARY_API_KEY` set — two-key overlap with constant-time dual-candidate `checkAuth` + canary-redaction in 401 envelope; `openwop-auth-api-key-rotation` profile claim conditional on env); `capabilities.discovery.authScoped` (when `OPENWOP_TENANT2_API_KEY` set — same-endpoint narrowed view; tenant2 principal sees STRICT subset of primary's view per `capabilities-change-detection.md` §"Scoped capability views" line 69 — `orchestrator` + `dispatch` blocks omitted; `openwop-discovery-auth-scoped` profile claim conditional on env). Three new protocol-tier SECURITY invariants: `agent-memory-cti-1`, `agent-memory-sr-1-redaction`, `auth-key-rotation-no-canary-echo`. **Phase I.3 + I.4 close-out (2026-05-12):** `openwop-auth-oauth2-client-credentials` + `openwop-auth-oidc-user-bearer` profile claims now **verified end-to-end on Postgres** via `examples/hosts/postgres/src/jwt-validator.ts` (~250 LOC; uses `node:crypto` for RS256/ES256 signature verification + JWKS fetch with 10-minute cache and re-fetch on `kid` miss for key-rotation grace). Operators configure via `OPENWOP_OAUTH2_ISSUER_URL` + `OPENWOP_OAUTH2_AUDIENCE` (and/or `OPENWOP_OIDC_ISSUER_URL` + `OPENWOP_OIDC_AUDIENCE`); the host advertises each profile claim only when the corresponding env vars resolve. JWT bearers with three dot-separated segments route through the validator first; static-API-key bearers still authenticate via the constant-time dual-candidate fallback. 10-path smoke (`test/oauth2-oidc.test.ts`) covers: discovery advertisement shape, static-API-key fallback, positive JWT, malformed JWT, wrong-issuer, wrong-audience, expired, unknown-kid, `alg: "none"` rejection, and canary-redaction per `auth.md` §"No credential echo". **Phase I.7 mTLS termination (2026-05-13):** Postgres host now claims `openwop-auth-mtls` end-to-end when `OPENWOP_MTLS_CERT_PATH` + `OPENWOP_MTLS_KEY_PATH` (+ optional `OPENWOP_MTLS_CA_PATH`) are set. The HTTP listener switches to `node:https.createServer({ requestCert: true, rejectUnauthorized: OPENWOP_MTLS_REQUIRED !== 'false' })`; discovery emits `capabilities.auth.mtls.{supported, required, subjectMapping: 'cn'}` only when configured (honesty principle). Verified by `test/mtls.test.ts` (advertisement shape + valid-cert 201 + no-cert TLS handshake rejection). **Phase I.2 reasoning-event wiring closure (2026-05-13):** the `core.llm.*` executor now emits `agent.reasoned` (verbosity-gated per `RunOptions.configurable.reasoningVerbosity`) + `agent.decided` (confidence ∈ [0,1]); `core.mcp.toolCall` emits `agent.toolCalled` + `agent.toolReturned` paired via shared `callId`. SR-1 + MCP-1 preserved: only SHA-256 digests + lengths appear on payloads — never raw args/results. Verified by `test/reasoning-event-emission.test.ts`. **RFC 0012 (Memory Compaction Profile, `Accepted` 2026-05-15 under the bootstrap-phase waiver) reference impl — landed 2026-05-14:** when `OPENWOP_MEMORY_COMPACTION=true`, Postgres advertises `capabilities.memory.compaction.{supported, trigger: 'host-managed', maxInputEntries: 1000, maxOutputBytes: 65536}` + emits canonical `memory.compacted` events per `run-event-payloads.schema.json` §`memoryCompacted`. SR-1 carry-forward (RFC 0012 §D) enforced via `applyCompactionRedaction` re-substituting `[BYOK:...]` form-leaks + non-canonical `<REDACTED:...>` markers with `[REDACTED:carry-forward-<n>]` BEFORE the derived entry persists. Test seam at `POST /v1/test/memory/{seed,compact}` (gated on `OPENWOP_TEST_TRIGGER_COMPACTION=true`) lets the 3 conformance scenarios (`memory-compaction-{event-emitted,sr1-carry-forward,provenance-tag}.test.ts`) drive the host-managed scheduler synchronously. Host smoke: `test/memory-compaction.test.ts` (7 paths). **PACK-1/PACK-2 host-side pack consumer (2026-05-15):** `examples/hosts/postgres/src/pack-consumer.ts` runs the canonical install-time security checks per `node-packs.md` §"Dependency resolution + lockfile" + §"Signing recipe": (1) lockfile structural parse against `pack-lockfile.schema.json`, (2) SRI integrity of fetched tarball vs. lockfile pin, (3) version-drift detection between lockfile pin and registry-served manifest, (4) Ed25519 signature verification against PEM public key over tarball bytes. Every failure mode is a typed `PackConsumerError` with a canonical `code` (`pack_integrity_mismatch`, `pack_signature_invalid`, `pack_version_mismatch`, `pack_lockfile_invalid`, `pack_signature_unverifiable`, `pack_manifest_invalid`); host fails closed in every case. 9-path host smoke (`test/pack-consumer.test.ts`) verifies positive + 7 fail-closed + sig-absent-permitted + `loadLockfile` roundtrip against the in-tree `core.openwop.examples@1.0.0` pack (signed with `openwop-registry-root`). **RFC 0022 (Active 2026-05-18) reference impl — landed 2026-05-18:** Postgres advertises `capabilities.agents.dispatchMapping: true` + `capabilities.subWorkflow.inputMapping: true`. `core.dispatch` executor now loops over `nextWorkerIds[]` sequentially (`fanOutPolicy: 'sequential'`), projecting parent variables into each child's `inputs_json` via `effectiveInputMapping = perWorkerInputMappings[workerId] ?? inputMapping ?? {}` before invocation, and harvesting child variables back into the parent variable bag via the symmetric `effectiveOutputMapping` on terminal `completed` (failed/cancelled children skip the mapping per §A; the shared parent variable bag is the cross-worker handoff channel per §D). `core.subWorkflow` executor seeds the child run's `variables_json` two-pass: `variables[].defaultValue` first, then `inputMapping` projections override matching keys (§B). Host smoke: HVMAP-2 (`subworkflow-input-mapping.test.ts`) verifies the child-run-variable override end-to-end against the parent + child fixture pair; HVMAP-1a/1b/1c remain `it.todo()` pending a supervisor-mock extension that lets fixtures drive `OrchestratorDecision` sequences (the existing reference supervisor emits a single hard-coded decision). **RFC 0024 (Active 2026-05-18) streaming reasoning — landed 2026-05-19 (commit `08ac7bc`):** Postgres advertises `capabilities.agents.reasoning.streaming: true`. `core.conformance.mock-agent` executor honors `mockReasoning.streamChunks` by emitting one `agent.reasoning.delta` per chunk (sequence 0..N-1) followed by exactly one closing `agent.reasoned` whose `reasoning` field carries the concatenation — verbosity-gated per `RunOptions.configurable.reasoningVerbosity` ('off' suppresses both deltas and closing). Host smoke (`test/mock-agent.test.ts` third block) verifies the contract end-to-end against pglite: N=3 deltas with monotonic sequence, single closing event, consistent agentId across the block, and the ordering invariant (all deltas precede the closing event). `core.llm.*` BYOK thinking surfaces (Anthropic extended thinking, Gemini 2.5 `thoughts`) remain on the workflow-engine sample lane only — the Postgres host's reasoning emission is mock-agent-scope on this advertisement. | `minimal` (operational-readiness claim is independent of throughput — see `production-profile.md:74`; single-`pg.Client` design serializes writes) | Claimed (since 2026-05-11) — see `examples/hosts/postgres/conformance-full.md`; mechanically verified via `capabilities.production.supported: true` advertisement + `production-backpressure.test.ts` + `production-retention-expiry.test.ts` passing under `OPENWOP_REQUIRE_BEHAVIOR=true` per RFC 0009 | `examples/hosts/postgres/README.md` + `conformance-full.md` + in-process `test/{lifecycle,audit-tamper,pause-resume,interrupts,webhooks,sse,review-fixes,claim,backpressure,byok-roundtrip,byok-cross-provider,ai-policy,mcp-client,http-client,auth-rotation-scoped,memory-adapter,agent-events,oauth2-oidc,mtls,reasoning-event-emission,memory-compaction,pack-consumer,audit-checkpoint-export,multi-region-idempotency,multi-region-partition,cross-engine-append}.test.ts` (26 files, all wired into `npm test` as of 2026-05-15 — the prior `npm test` script ran only the first 9 wire-surface files; the 17 gap-closure additions now run on every push via `.github/workflows/postgres-host-conformance.yml`) via pglite |

### External conformance suite — pass rates (2026-05-23, suite v1.5.0 + Phase 4 close-out)

Latest `npx vitest run` against each running reference host. **Re-measured 2026-05-23 against suite v1.5.0 (215 test files / 1595 tests)** — total grew 1564 → 1595 (+31 tests) as the 2026-05-22 → 2026-05-23 Phase 4 close-out landed 5 new behavioral scenarios totaling ~32 new assertions: `secret-leakage-otel-attribute`, `experimental-tier-shape`, `multi-region-idempotency-behavior`, `cross-engine-append-behavior`, `sandbox-mvp-behavior`. The 4 standard reference hosts soft-skip cleanly on the new test seams (they don't advertise the matching capabilities). The workflow-engine reference, with all opt-in seams enabled, exercises 22 behavioral assertions across the 5 new scenarios. See `docs/CONFORMANCE-RUNS-2026-05-23.md` for per-host failure-topic taxonomy. Prior 2026-05-22 v1.5.0 snapshot retained in git history.

| Host | Passed | Failed | Skipped | Todo | Total | Pass rate (default) |
|---|---:|---:|---:|---:|---:|---:|
| Postgres reference | **1477** | 6 (capability gaps in RFC 0022 dispatch mapping + cost-attribution seam + RFC 0031 model-capability-insufficient executor path; not regressions — these are new v1.2–v1.5.0 scenarios the host has not yet wired) | 98 | 14 | 1595 | **92.6%** total; ~99.6% of non-skipped (measured 2026-05-23 against suite v1.5.0 via pglite + `OPENWOP_WEBHOOK_ALLOW_PRIVATE=true`; the +29 skip delta vs 2026-05-22 reflects the 5 new Phase-4-close-out scenarios soft-skipping for the host that doesn't yet advertise the matching seams) |
| SQLite reference | **1490** | 7 (RFC 0022 inputMapping/outputMapping not implemented in SQLite host + cost-attribution seam absent + RFC 0031 model-capability-insufficient executor path absent — all capability-gaps, not regressions) | 84 | 14 | 1595 | **93.4%** total |
| In-memory reference | **1449** | 48 (predominantly version-negotiation event-sequence ordering + run-event canonical shape + scenarios outside the claimed `openwop-core` profile set — the minimal-host floor) | 84 | 14 | 1595 | **90.8%** total |
| Python reference | **1391** | 60 (multi-agent execution model Phase 2/3/4 + envelope reliability + envelope completion contract + envelope variant discriminator — Python host is the cross-language portability proof and intentionally lags on advanced capability adoption per the Python pyproject scope) | 130 | 14 | 1595 | **87.2%** total; **100% of applicable** when scoped to the host's claimed `openwop-core` + `openwop-stream-poll` + `openwop-stream-sse` profile set (the floor for cross-language parity) |
| **Workflow-engine reference** (exhaustive-mode, post prompt-templates bundled-path bugfix: 2026-05-23) | **1525** | 2 (auth-anonymous-allow under NODE_ENV=development + multi-agent confidence-escalation timing flake — both documented in `docs/KNOWN-LIMITS.md` §"Behavior tests too coarse to fully prove an invariant") | 56 | 14 | 1597 | **95.5%** total (re-measured 2026-05-23 with `OPENWOP_RATELIMIT_DISABLED=true` + all Phase 4 + sandbox + multi-region + cross-engine + secret-leakage seams enabled). **Prior snapshots (same-day delta):** 1291 / 129 / 161 / 14 / 1595 = 80.9% (pre-`d09d99c`, crash-polluted); 1521 / 6 / 56 / 14 / 1597 = 95.4% (post-`d09d99c`, but before the `promptStore.ts` + `promptCompose.ts` bundled-path follow-up landed). The same bundled-path pattern that caused `envelopeAcceptor.ts` to crash also caused `promptStore.ts`'s `FIXTURES_DIR` to resolve to `apps/conformance-fixtures/prompt-templates` (doesn't exist) under the bundled tree → host-built-in templates never loaded → `getTemplate('conformance.prompt.writer-system')` returned undefined → `prompt.composed` event silently never emitted → 4 conformance assertions failed. Both bugs share the shared `_repoPath.ts:locateRepoDir()` helper introduced 2026-05-23. Closes 22 behavioral assertions across the 5 new Phase 4 scenarios end-to-end (multi-region 6 + cross-engine 4 + sandbox 9 + secret-leakage 3). |

**Note on v1.5.0 re-measurement (2026-05-22, same-day).** The pass rates above reflect a fresh run against `@openwop/openwop-conformance@1.5.0` (210 scenario files / 1564 tests — same-day publish after v1.4.0, +6 tests from RFC 0044's vendor-kind routing branch splitting `multi-agent-confidence-escalation.test.ts` into discrete sub-blocks). Per-host failure-topic taxonomy lives in `docs/CONFORMANCE-RUNS-2026-05.md`. Commentary in the paragraphs below preserves the v1.1.0-era narrative for historical context — the rate-pp numbers it cites are not directly comparable to the v1.5.0 measurements, since the suite scenario count grew from ~850 to 1564 (with the +6 v1.4.0 → v1.5.0 delta described above).

SQLite's 9 pre-Phase-A failures (`core.dispatch`, `channel-ttl` pruning, conversation-capability refusal, subworkflow `outputMapping`, identity-fixture variable echo, events/poll forward-compat tolerance, and runtime-capabilities `capability_not_provided`) are all closed; the host now passes 100% of applicable default-mode scenarios. Postgres's 1-2 failures are unchanged from 2026-05-11: `webhook-signed-delivery` (flake; passes in isolation) and `pause/resume running→paused→terminal` (fixture-coupled — 30s default delay vs 10s post-resume timeout). None of the failures on either host block the production-profile MUSTs.

Python reference's 53 baseline failures (the 2026-05-12 first-measurement against the post-Phase-C-round-2 host) **closed in full** via a focused follow-up batch on 2026-05-12. Highlights: (1) `RunEvent.to_dict()` rewritten to emit the canonical 6-field `RunEventDoc` shape (`eventId/runId/type/payload/timestamp/sequence`) per `schemas/run-event.schema.json`, closing the 3 version-negotiation failures; (2) `?streamMode=` query validation (closed enum + comma-separated subsets, `values` exclusive) + `?bufferMs=` range validation + `Connection: close` on SSE so terminal events drop the socket — closes all 12 stream-modes failures; (3) pause/resume reshaped to 202 + `pausedAt`/`resumedAt` per `rest-endpoints.md`, 409 + `error: "conflict"` + `details.runStatus`, idempotent re-pause, `core.delay` drain-on-pause semantics so wall-clock stays bounded — closes 5 pause-resume edge cases; (4) `IdempotencyCache` gained per-key locks held across `_handle_create_run`'s get→create→put — closes the highConcurrency parallel-runs race; (5) webhook error-code catalog restored to the conformance-expected codes (`webhook_url_rejected` for SSRF, `subscription_not_found` for unknown DELETE); (6) discovery `fixtures[]` filtered to those whose every node typeId is in `{core.noop, core.delay}` AND not in the enforcement-fixture blocklist (cap-breach, configurable-schema — enforcement contracts the host honestly does NOT implement) — converts 24+ FAILs to honest SKIPs; (7) `GET /v1/workflows/{id}` route + `/v1/packs/*` catch-all returns non-OpenWOP-shaped 404 so `pack-registry.test.ts`'s registry-presence probe identifies "no registry mounted" and skips. Final: **700 passed / 0 failed / 58 skipped / 30 todo (788 total)**.

**Strict-mode posture (`OPENWOP_REQUIRE_BEHAVIOR=true`).** SQLite runs at 669 pass / 10 strict-fail / 32 skip / 30 todo. The 10 strict-fails are intentional honesty opt-outs — the host explicitly does NOT claim `openwop-production`, OAuth2-CC, OIDC, mTLS, auth-scoped discovery, or replay-retention profiles, so `behaviorGate` correctly flips skip→fail under strict mode. Per the host's design these are not bugs; they are the price of advertising only what the host implements. **Strict-mode opt-out signaling landed 2026-05-13** via `OPENWOP_OPTED_OUT_PROFILES` env var consumed by `conformance/src/lib/behavior-gate.ts`. SQLite operators wanting strict-mode green run: `OPENWOP_REQUIRE_BEHAVIOR=true OPENWOP_OPTED_OUT_PROFILES=openwop-production,openwop-auth-oauth2-client-credentials,openwop-auth-oidc-user-bearer,openwop-auth-mtls,openwop-discovery-auth-scoped,openwop-replay-retention-expiry` — the 10 prior strict-fails skip with a "honest opt-out" log line instead of failing. Same mechanism applies to Python's 21 opt-outs. Advertise + opt-out conflict (host claims AND opts out) surfaces as a warning so typos can't mask real bugs. Python reference's strict-mode posture (re-measured 2026-05-12 on a clean host, prior reading inflated by test pollution): **679 pass / 21 strict-fail / 58 skip / 30 todo**. **All 21 strict-fails are honest opt-outs** — scenarios for profiles the Python host explicitly does NOT claim: audit-log-integrity ×2, api-key-rotation ×3, mTLS ×1, OAuth2-CC ×4, OIDC ×1, auth-scoped discovery ×3, production-backpressure ×3 + retention-expiry ×2, replay-retention-expiry ×2. **Zero real bugs inside CLAIMED profiles**: every `openwop-core` + `openwop-stream-sse` + `openwop-stream-poll` scenario passes both modes. The host's posture matches SQLite's "advertise only what you implement" pattern, with 21 strict-mode opt-outs (vs SQLite's 10) reflecting the smaller advertised surface of the stdlib-only port. Strict-mode green for Python now achievable via the same `OPENWOP_OPTED_OUT_PROFILES` mechanism (set to the 21 deliberately-unclaimed profiles; the suite will skip them with "honest opt-out" logs rather than failing). Implementing the underlying surfaces remains a future direction, but is no longer required to clear strict-mode green honestly.

Third-party hosts can append rows by opening a PR with their advertised profiles, suite version, and a public conformance result.

### Third-party host adoption — RFC 0021 external-validation gate (2026-05-18)

Per `RFCS/0001-rfc-process.md` §"Promotion to Accepted," some RFCs require a non-steward host to advertise the new surface before the RFC can flip `Active → Accepted`. RFC 0021 (AI Envelope) gated on this; closed by:

| Third-party host | URL | Advertised surface | Conformance evidence |
|---|---|---|---|
| **MyndHyve workflow-runtime** | `https://myndhyve-prod.web.app/.well-known/openwop` | `supportedEnvelopes: ['clarification.request', 'schema.request', 'schema.response', 'error']` + `schemaVersions[<each>] = 1` (RFC 0021 §C) | `OPENWOP_BASE_URL=https://myndhyve-prod.web.app npx vitest run src/scenarios/ai-envelope-shape.test.ts` → **18/18 pass** (schema-compile + positive/negative round-trip + advertisement-contract + 7 behavioral `acceptEnvelope` round-trips + universal-kind allowlist override). MyndHyve commit `4d475f0b`; Cloud Run revision `workflow-runtime-00169-5kl`. |

This row only tracks third-party adoption that closes an RFC external-validation gate — it is NOT a general-purpose conformance leaderboard. Hosts looking to publish full pass/fail/skip counts append a row to §"External conformance suite — pass rates" above.

### Third-party host adoption — RFC 0030/0031/0032/0033 envelope LLM-contract-hardening external-validation gate (2026-05-20)

Per `RFCS/0001-rfc-process.md` §"Promotion to Accepted," the four envelope LLM-contract-hardening RFCs (`Active 2026-05-20`) each require a non-steward host to advertise the new surface. MyndHyve workflow-runtime has integrated the surface; row pending the operator's URL/commit/revision fill-in for the formal Active → Accepted landing on 2026-05-27 (post-comment-window):

| Third-party host | URL | Advertised surfaces | Conformance evidence |
|---|---|---|---|
| **MyndHyve workflow-runtime** | `https://api.myndhyve.ai/.well-known/openwop` | RFC 0030: `capabilities.envelopes.reasoning.{supported: true, promptDirective: 'advisory'}` + `tierOneSubsetCompliance` (boolean — see §"Wire-shape drifts" below); RFC 0031: `capabilities.modelCapabilities.{supported: true, advertised: ['structured-output', 'discriminator-enum']}` (subset of spec-reserved set — only identifiers the host actually gates on; `substitutionSupported` not advertised — no host-wide swap facility); RFC 0032: `capabilities.envelopes.reliability.{events: true (boolean, see drifts), maxRetryAttempts: 2}`; RFC 0033: `capabilities.envelopes.reliability.completion.{distinguishesTruncation: true, truncationRetryMultiplier: 2}` (field name drift — see drifts) | `OPENWOP_BASE_URL=https://api.myndhyve.ai npx vitest run src/scenarios/envelope-reasoning-shape.test.ts src/scenarios/envelope-reasoning-secret-redaction.test.ts src/scenarios/envelope-tier-one-subset-static.test.ts src/scenarios/envelope-variant-discriminator-static.test.ts src/scenarios/model-capability-substituted.test.ts src/scenarios/model-capability-insufficient.test.ts src/scenarios/node-module-required-capabilities-shape.test.ts src/scenarios/envelope-refusal-shape.test.ts src/scenarios/envelope-retry-attempted.test.ts src/scenarios/envelope-retry-exhausted.test.ts src/scenarios/envelope-truncated.test.ts src/scenarios/envelope-truncation-cap-exhaustion.test.ts src/scenarios/envelope-completion-distinguishes-truncation.test.ts src/scenarios/envelope-recovery-applied.test.ts` → **87/87 pass on revision `workflow-runtime-00180-v8j`** (re-measured 2026-05-21 22:30 UTC with a dedicated conformance API key minted under an isolated workspace). Of the 87, 83 exercise host behavior end-to-end; 4 (the `node-module-required-capabilities-shape` block) remain on the honest-skip path because MyndHyve does not expose `GET /v1/host/sample/node-catalog` — the vendor-prefixed test seam the SHOULD-tier authoring-convention check requires to enumerate NodeModules. Net: **83 live + 4 host-seam-honest-skip = 95.4% MUST-tier coverage with 100% of MUST-tier wired tests passing.** **All five wire-shape drifts (a)/(b)/(c)/(d)/(e) RESOLVED 2026-05-21** — drift (e) was the discovery-location mismatch (top-level `modelCapabilities` vs spec-canonical `capabilities.modelCapabilities`); closed by MyndHyve in commit `386c9e7d` (in revision `00180-v8j`, migrating to the nested form rather than via dual-emit spec amendment). Verified post-deploy: `top: false / nested: true` on `https://api.myndhyve.ai/.well-known/openwop`. The cross-host validation criterion for all four `Active → Accepted` promotions is satisfied; no remaining wire-shape drifts on the MyndHyve target.<br><br>**Wire-shape drifts — RESOLVED 2026-05-21 (all 4 amendments landed before the 2026-05-27 promotion deadline):** (a) ✅ `envelopes.reliability.events` MUST be a JSON `string[]` of event names per RFC 0032 §C amendment; boolean form not permitted. MyndHyve renames `true` → `["envelope.retry.exhausted", "envelope.refusal"]`. (b) ✅ Canonical multiplier name is `truncationBudgetMultiplier` per RFC 0033 §B amendment; `truncationRetryMultiplier` not permitted. MyndHyve renames the field. (c) ✅ `tierOneSubsetCompliance` MUST be tri-state string `"strict" \| "warn" \| "off"` per RFC 0030 §C amendment; boolean form not permitted. MyndHyve migrates `true` → `"strict"` or `"warn"` per actual posture. (d) ✅ Spec accepted MyndHyve's shorter error-code forms: `envelope_payload_invalid` → `envelope_invalid` and `envelope_refused_by_provider` → `envelope_refusal` per RFC 0033 §F amendment; MyndHyve still renames `envelope_schema_violation` → `envelope_invalid`. See `docs/handoffs/MYNDHYVE-RFC-0030-0033-ADOPTION-FEEDBACK-2026-05-20.md` §"Summary of resolutions" for the full per-finding record. |

The 38+ assertions break down as: envelope-reasoning (shape + redaction, 12+5 live) · model-capability (substituted 6 + insufficient 4 + node-module-shape 4) · envelope-reliability (refusal-shape 8 + retry-attempted 5 + retry-exhausted 5 + truncated 4 + truncation-cap-exhaustion 4 + completion-distinguishes-truncation 5) · advertisement-shape (static rows across the four RFCs). The reference workflow-engine sample passes the same set under `OPENWOP_BASE_URL=http://localhost:<port>`; MyndHyve's row closes the cross-host validation criterion.

### Composition partners — real-impl interop evidence (2026-05-12)

Per `docs/PROTOCOL-GAP-CLOSURE-PLAN.md` §"Track 6: MCP And A2A Proof" T3.4. The openwop conformance suite's MCP/A2A probes can be pointed at live reference implementations via `OPENWOP_MCP_REAL_SERVER_URL` / `OPENWOP_A2A_REAL_PEER_URL` to verify wire-shape interop beyond the in-process fakes.

| Partner | Reference impl | Suite scenario | Result | Notes |
|---|---|---|---|---|
| **MCP** | `@modelcontextprotocol/sdk@1.29.0` — all three transports: `enableJsonResponse: true` streamable-http (`application/json`), default streamable-http (`text/event-stream` SSE-correlated by JSON-RPC id), and stdio (via `examples/mcp-stdio-bridge/`) | `mcp-tool-roundtrip.test.ts` | ✅ 2/2 pass against each transport (single-JSON verified 2026-05-12; SSE-streamed + stdio-bridged verified 2026-05-13) | Surfaced two probe gaps that the in-process fake had been hiding: (1) `initialize` requires `protocolVersion` + `capabilities` + `clientInfo` in `params` (official SDK returns 400 on empty params); (2) `mcp-session-id` from the initialize response MUST be threaded through `tools/list` + `tools/call`. Both closed in the probe; fake-mode compatibility preserved. The probe is HTTP-only by design; stdio MCP servers (the default for `modelcontextprotocol/servers` references) are exercised end-to-end via the bundled HTTP-to-stdio bridge at `examples/mcp-stdio-bridge/` — operators wrap any newline-delimited-JSON-RPC stdio server with the bridge and point `OPENWOP_MCP_REAL_SERVER_URL` at the bridge's port (default 4021). |
| **A2A** | `@a2a-js/sdk@0.3.13` reference peer (echo skill, JSON-RPC transport) | `a2a-task-roundtrip.test.ts` | ✅ 1/1 direct-probe pass against `http://localhost:4002` (drift-point subtests soft-skip without a host-advertised fixture, which is the documented stance) | Probe migrated from the v0.2-era simple-HTTP shape (`/agent.json` + `POST /tasks` + `GET /tasks/{id}`) to A2A v0.3 JSON-RPC: AgentCard at `/.well-known/agent-card.json`, `message/send` + `tasks/get` over the JSON-RPC endpoint discovered from `card.additionalInterfaces`. Internal `A2ATaskState` enum stays UPPERCASE (matches `a2a-integration.md`'s gRPC reference); wire responses translate to lowercase-hyphen (`completed`, `input-required`, `auth-required`) per the JSON-RPC enum. Probe accepts both Task and Message envelopes from `message/send` since A2A v0.3 leaves that choice to the peer. |

## Capability adoption — RFC 0027 + RFC 0028 prompt library

The prompt-library track introduces two independent capability axes on `capabilities.prompts`:

- **`supported`** (RFC 0027 Phase A) — node-execution PromptRef resolution + `prompt.composed` event emission. See `spec/v1/prompts.md` §"Capability advertisement".
- **`endpointsSupported`** (RFC 0028 Phase B) — the `/v1/prompts*` REST surface (`listPromptTemplates`, `getPromptTemplate`, `renderPromptTemplate`, plus mutating endpoints when `mutableLibrary: true`). Independent of `supported` — a host MAY implement either without the other.

Adoption status across tracked hosts (updated 2026-05-23 — MyndHyve RFC 0027 adoption):

| Host | `prompts.supported` | `prompts.endpointsSupported` | `prompts.mutableLibrary` | `prompts.agentBindings` | `prompts.observability` | Notes |
|---|---|---|---|---|---|---|
| In-memory reference | — | — | — | — | — | Not yet wired. |
| SQLite reference | — | — | — | — | — | Not yet wired. |
| Postgres reference | — | — | — | — | — | Not yet wired. |
| Python reference | — | — | — | — | — | Not yet wired. |
| Workflow-engine sample (`apps/workflow-engine/`) | **true** | **true** | **true** | **true** | **full** | RFC 0027 Phase A: composition pipeline (`host/promptCompose.ts`) + `/v1/host/sample/prompt/compose` test seam driving `prompt-composed-secret-redaction` + `prompt-composed-trust-marker` conformance. RFC 0028 Phase B: all six `/v1/prompts*` REST routes wired via `routes/prompts.ts` against the in-memory PromptStore (`host/promptStore.ts`); host-built-in templates loaded from `conformance-fixtures/prompt-templates/` at boot; user-source mutations accepted at run time. RFC 0029 Phase C: four-layer resolver (`host/promptResolve.ts`) + `/v1/host/sample/prompt/resolve` test seam driving the three `prompt-resolution-chain-*` scenarios. **RFC 0034 (Active 2026-05-21):** advertises `capabilities.observability.testSeams.{otelScrape, debugBundleExport}` when `OPENWOP_TEST_SEAM_ENABLED=true` (the two test-seam endpoints in `routes/testSeam.ts` predate the RFC; RFC 0034 formalized their wire contract). Conformance scenario `envelope-reasoning-secret-redaction.test.ts` tightens from soft-skip-on-404 to capability-gated MUST-serve when the env-gate is set. **RFC 0037 Phase 1 + RFC 0039 Phase 2 (Active 2026-05-22):** advertises `capabilities.multiAgent.executionModel.{supported: true, version: 1}` when `OPENWOP_MULTI_AGENT_EXECUTION_MODEL=true`; bumps to `version: 2` + `confidenceEscalationFloor: 0.5` (default; tunable via `OPENWOP_MULTI_AGENT_CONFIDENCE_FLOOR=<n>` ∈ [0.5, 1.0]) when `OPENWOP_MULTI_AGENT_EXECUTION_MODEL_PHASE_2=true`. At version: 2, `core.dispatch` gates each decision on the confidence floor BEFORE the per-worker spawn loop: a decision with stated `confidence` below the floor emits `core.workflowChain.confidence-escalated` (causationId chained to the matching `runOrchestrator.decided`) AND suspends the parent with `interrupt.kind: 'clarification'` carrying `originalDecision`, `confidence`, `floor`. Behavioral conformance `multi-agent-confidence-escalation.test.ts` passes end-to-end against this host. Memory-lifecycle half of RFC 0039 (MAE-2/3) remains follow-up (schema fields landed; host wiring deferred). Phase 1's per-decision wiring unchanged: `core.dispatch` execute() emits all 7 handoff state-machine transition events as `core.workflowChain.event` records with chained `causationId` per spec/v1/multi-agent-execution.md §"Transition events". Pre-creation failures throw `DispatchCreationError` (→ `dispatch.failed` phase); child-runtime exceptions are caught inside `dispatchSubWorkflow` and converted to `childStatus: 'failed' + childRuntimeError` (→ `child.failed` phase) so the state machine's `dispatching → failed` vs `running → failed` rows are correctly distinguished. Behavioral conformance `multi-agent-handoff-state-machine.test.ts` passes end-to-end against this host. **Open gap:** RFC 0028 §B pack-install flow (Ed25519 signature + SRI integrity + dependency-resolve) still deferred — `installPackTemplates()` seam exists but no install path invokes it. |
| MyndHyve workflow-runtime | **true** | — | — | — | **full** | **RFC 0027 adopted 2026-05-23.** MyndHyve commit `5385548c` lands the prompt-compose seam end-to-end with SR-1 redaction (BYOK secret-source variables → `[REDACTED:<credentialRef>]` markers in `prompt.composed` payloads, never plaintext) + RFC 0020 §D `<UNTRUSTED>...</UNTRUSTED>` wrapping (untrusted bindings propagate `contentTrust: "untrusted"` to the composed envelope). `prompts.observability` bumped `'hashed' → 'full'` and now backed by real emission. Verified live at `https://api.myndhyve.ai/.well-known/openwop` 2026-05-23. **Closes RFC 0027 path-to-Accepted** — RFC 0027 promotes Active → Accepted in the commit landing this row. RFC 0028 (`endpointsSupported` + `mutableLibrary` + `packsSupported`) + RFC 0029 (`agentBindings`) remain deferred per MyndHyve's adoption note (multi-day Firestore CRUD + RBAC tracks). |

Conformance gates:

- `prompt-template-shape.test.ts` — always runs (server-free schema-shape proof).
- `prompt-composed-secret-redaction.test.ts` — gated on `supported: true` + `observability: "full"`. Skips cleanly for hosts that advertise less.
- `prompt-composed-trust-marker.test.ts` — same gates.
- `prompt-resolution-chain-node-wins.test.ts` — gated on `supported: true`. RFC 0029 §A layer-1 precedence.
- `prompt-resolution-chain-agent-intrinsic.test.ts` — gated on `supported: true` + `agentBindings: true`. RFC 0029 §A layer-2 (system intrinsic).
- `prompt-resolution-chain-fallback-cascade.test.ts` — gated on `supported: true`. RFC 0029 §A layer-3/4 cascade.

SECURITY invariants `prompt-composed-secret-redaction` + `prompt-composed-trust-marker` are tracked in `SECURITY/invariants.yaml` (protocol-tier; verified via the above conformance scenarios). Under `OPENWOP_REQUIRE_BEHAVIOR=true`, the capability-gated scenarios FAIL instead of skip when the host advertises the gating capability but doesn't emit the asserted contract (per `conformance/coverage.md` §"Capability-gated scenarios").

## Capability adoption — RFC 0002 §B `agent.toolReturned` `causationId` pairing

`RFCS/0002-agent-identity-and-reasoning-events.md` §B requires every `agent.toolReturned` to carry `causationId === eventId` of the corresponding `agent.toolCalled`. The `agentReasoningEvents.test.ts` scenario was tightened on 2026-05-18 to assert this strict pairing (previously the scenario validated `callId` correlation only, which masked deviations on the event-log identity chain that `spec/v1/replay.md` §"Determinism with non-deterministic agents" depends on). The check is gated on hosts surfacing `eventId` in their `/events` projection — hosts that omit `eventId` from the projection soft-skip the assertion (and SHOULD add it).

Adoption status (2026-05-21):

| Host | `causationId` pairing | `eventId` surfaced on `/events` | Notes |
|---|---|---|---|
| Workflow-engine sample (`apps/workflow-engine/`) | **shipped** | **yes** | Reference impl; `conformance/src/scenarios/agentReasoningEvents.test.ts` is the regression coverage. |
| MyndHyve workflow-runtime | **shipped 2026-05-18** | **yes** | Executor: MyndHyve commit `a4270444` ("feat(mock-agent): full RFC 0023 §B impl + strict causationId pairing"). `EventLog.append()` returns `Promise<RunEventDoc>` synchronously (`packages/workflow-engine/src/protocol/EventLog.ts:189`); `core.conformance.mock-agent` threads the returned `eventId` into BOTH payload-level `causationId` (RFC 0002 §B) AND event-record-level `causationId`. `/v1/runs/{runId}/events` surfaces `eventId` so the strict assertion is NOT soft-skipped. Host-side regression: `mockAgent.node.test.ts:116-132`. **Conformance fixture seeded with pairs (2026-05-21):** MyndHyve commit `eb9a1858` extends the `conformance-agent-reasoning` fixture in `scripts/seed-conformance-fixtures.cjs` with two `mockToolCalls` entries (`demo:echo` n=1,2). `workflow_templates/conformance-agent-reasoning` reseeded against `myndhyve-prod` Firestore at 2026-05-21 ~14:10 UTC; no Cloud Run revision change needed (revision `workflow-runtime-00324-giy` already carries the `a4270444` executor). **Real-agent telemetry path closed (same commit `eb9a1858`):** the previously-dormant `src/core/agents/services/agentTelemetry.ts` → `WorkflowEngine.emitNodeEvent` → `teeToEventLog` pipeline now threads `EventLog.append`'s returned `eventId` back to the telemetry caller, mirrors `data.causationId` onto the EventLog record meta (not just the wire payload), and exposes `emitAgentToolReturned(ctx, payload, { causationId })` as the new pairing API. Six new tests in `emitNodeEvent.causation.test.ts` lock the return-value + meta-mirror + no-op + append-failure contracts. First real producer will be RFC 0002 §B compliant out of the gate. |
| In-memory / SQLite / Postgres / Python reference hosts | — | — | Not yet tracked here; assertion soft-skips on hosts that don't surface `eventId` on `/events`. |

**End-to-end conformance re-run (2026-05-21).** `agentReasoningEvents.test.ts` passes against `https://api.myndhyve.ai` (proxy: local tenant-injecting forwarder per MyndHyve's `/v1/runs` tenantId convention; conformance API key minted with scope `wop:runs:create,read,cancel` + `wop:approvals:respond` + `wop:artifacts:read` under an isolated workspace). Run against revision `workflow-runtime-00324-giy` emitted 11 events total: 1 `agent.reasoned`, 2 `agent.toolCalled`, 2 `agent.toolReturned`. **Strict pairing chain validated 2/2:** each `agent.toolReturned.causationId` exactly equals its paired `agent.toolCalled.eventId` across both `demo:echo` invocations from the fixture. RFC 0002 §B verified-on-MyndHyve at the protocol level.

## Capability adoption — RFC 0022 §D `node.dispatched` event + shared-bag cross-worker handoff

`RFCS/0022-host-variable-mapping.md` §D normates `node.dispatched` — the event emitted after each successful `core.dispatch` child invocation, carrying `childRunId` + `childWorkflowId` + `parentNodeId` + `status` — and §A defines the `perWorkerInputMappings` / `perWorkerOutputMappings` precedence over default `inputMapping` / `outputMapping`, with the shared parent variable bag as the cross-worker handoff channel. `dispatch-cross-worker-handoff.test.ts` (HVMAP-1c + HVMAP-1c-override, 2 subtests) exercises both surfaces end-to-end.

Adoption status (2026-05-21):

| Host | `node.dispatched` emission | `perWorkerInputMappings` / `perWorkerOutputMappings` | HVMAP-1c end-to-end | Notes |
|---|---|---|---|---|
| Workflow-engine sample (`apps/workflow-engine/`) | **shipped** | **shipped** | **passes** | Reference impl. |
| Postgres reference (`examples/hosts/postgres/`) | shipped | shipped | `it.todo()` host-side (supervisor-mock seam pending) | Per the Postgres row above. `core.dispatch` + `core.subWorkflow` executors carry §A/§D mappings; HVMAP-1a/1b/1c host-side specifically depend on a supervisor-mock extension that emits a multi-decision `OrchestratorDecision` sequence — the existing pinned-single-decision reference supervisor doesn't yet produce it. |
| MyndHyve workflow-runtime | **shipped 2026-05-21** | **shipped 2026-05-21** | **passes — first cross-host validation** | MyndHyve commit `f974f9a4` ("feat(dispatch): emit node.dispatched + perWorkerInput/OutputMappings") deployed in Cloud Run revision `workflow-runtime-00177-tkz` (promoted 2026-05-21 ~15:58 UTC; revision also extends `RunEventType` union with `node.dispatched`). `dispatch-cross-worker-handoff.test.ts` (HVMAP-1c + HVMAP-1c-override, 2/2) passes against `https://api.myndhyve.ai` measured 2026-05-21 with a dedicated conformance API key and a local tenant-injecting forwarder (MyndHyve's `/v1/runs` requires `tenantId` in body; the conformance driver v1.2.0 doesn't inject it). Cold-start required `OPENWOP_LIFECYCLE_TIMEOUT_MS=60000`; warm runs settle under the 10s default. |
| In-memory / SQLite / Python reference hosts | — | — | — | Not tracked here. SQLite's Soak run opts out of the RFC 0022 §A/§D fixture set (`conformance-dispatch-input-mapping*`, `conformance-dispatch-output-mapping*`, `conformance-dispatch-cross-worker-handoff*`, `conformance-subworkflow-input-mapping*`) per `.github/workflows/conformance-soak.yml` — the reference SQLite executor doesn't implement variable projection between parent and dispatched child runs. |

This is the **first cross-host end-to-end validation of RFC 0022 §D** — closes the cross-host criterion if/when RFC 0022 is promoted Active → Accepted.

## Third-party host adoption — RFC 0037 Phase 1 + RFC 0039 Half A + RFC 0044 (multi-agent execution model — handoff state machine + confidence escalation + vendor-kind advertisement) external-validation gate (2026-05-22)

Per `RFCS/0001-rfc-process.md` §3 (Status states): `Accepted` = "Implementation landed; conformance updated; Maintainers flip when acceptance criteria met."

**Three RFCs accepted from one Cloud Run conformance run** (MyndHyve revision `workflow-runtime-00353-rab`, suite `@openwop/openwop-conformance@1.5.0`):

| Host | Discovery doc | Capability advertisement | Conformance verification | Notes |
|---|---|---|---|---|
| **MyndHyve workflow-runtime** | `https://rfc0037---workflow-runtime-gjw5bcse7a-uc.a.run.app/.well-known/openwop` (tagged preview revision; production stays pinned at `00187-k2z`) | `capabilities.multiAgent.executionModel.{supported: true, version: 2, confidenceEscalationFloor: 0.5, confidenceEscalationInterruptKind: 'x-host-myndhyve-low-confidence'}` per RFC 0037 §C + RFC 0039 §A + RFC 0044 §A | `OPENWOP_BASE_URL=https://rfc0037---workflow-runtime-gjw5bcse7a-uc.a.run.app npx vitest run src/scenarios/multi-agent-handoff-state-machine.test.ts src/scenarios/multi-agent-confidence-escalation.test.ts` → **2 passed / 2 skipped / 0 failed (exit 0)** against `@openwop/openwop-conformance@1.5.0`. **Passing assertions:** (1) RFC 0037 §C advertisement-shape probe (boolean / integer / range checks against `multiAgent.executionModel`); (2) RFC 0039 Half A + RFC 0044 vendor-kind routing — the scenario reads `confidenceEscalationInterruptKind: 'x-host-myndhyve-low-confidence'` (matches `^x-host-[a-z][a-z0-9-]*-[a-z][a-z0-9-]*$`) and asserts `terminal.status.startsWith('waiting-')`, which matches MyndHyve's `waiting-approval` mapping. **Skipped assertions:** (1) RFC 0037 behavioral 4-event causation-chain leg, gated on `isFixtureAdvertised('conformance-multi-agent-handoff') AND isFixtureAdvertised('conformance-multi-agent-handoff-child')`; (2) RFC 0039 behavioral confidence-escalated event chain (same fixture-gating pattern). Both fixture-gated skips are separate strengthening passes on MyndHyve's roadmap. Cloud Run revision `workflow-runtime-00353-rab`, deploy commit `c4342b5b` (includes RFC 0037 Phase 1 emission + RFC 0039 Half A escalation + RFC 0044 vendor-kind advertisement). Parallel-session safety verified — production traffic unchanged at 100% on `workflow-runtime-00187-k2z` pre + post deploy; the new revision sits at 0% with `--no-traffic --tag rfc0037`. Adoption notes at MyndHyve `docs/openwop-adoption/0037-multi-agent-phase-1.md` (Re-run #2 section, commit `89cd564b`) + `docs/openwop-adoption/0044-vendor-kind-mapping.md` (commit `c4342b5b`, satisfying RFC 0044 §C with the clarify-semantic correspondence + `waiting-approval` status + 5 downstream consumers enumerated). |

This is the **first vendor-neutral validation tripwire firing** per RFC 0001 §"Promotion to Accepted" — and the first time three RFCs graduate from a single conformance run. The combination demonstrates the `x-host-<host>-<kind>` vendor-extension pattern (RFC 0044) working end-to-end: a host with entrenched vendor semantics (`interrupt.kind: 'low-confidence'` cross-cutting `LOW_CONFIDENCE_SUSPEND_REASON` + downstream UI consumers) advertises its kind via the canonical namespace + passes conformance without forcing a cross-cutting rename.

**Promotion summary:**
- RFC 0037 Phase 1 — already promoted Active → Accepted 2026-05-22 (prior signal, version: 1 advertisement)
- RFC 0039 Half A — promoted Active → Accepted in the commit landing this row (Half B memory-lifecycle remains a follow-up strengthening tier)
- RFC 0044 — promoted Active → Accepted in the commit landing this row

The advertisement-and-pass-modulo-honest-skip pattern matches the precedent from the RFC 0030–0033 envelope LLM-contract-hardening track promotion (2026-05-21).

### Honest correction — MyndHyve `registerHostSampleRoutes` was not called pre-`60b569de` (2026-05-23)

MyndHyve's session reported on 2026-05-23 that the `/v1/host/sample/*` host-extension routes had been deployed but the `registerHostSampleRoutes(app)` call was never invoked from `services/workflow-runtime/src/index.ts`. Effect: every `/v1/host/sample/*` request returned `404 Not Found` in production from each seam's initial deploy until commit `60b569de` (revision `workflow-runtime-00196-7mm`, 2026-05-23). The affected seams:

- `POST /v1/host/sample/prompt/compose` — RFC 0027 §E compose-seam (404 from 2026-05-23 landing until `60b569de`)
- `POST /v1/host/sample/test/llm-cache-key` — RFC 0041 §A cache-key seam (404 from 2026-05-22 landing until `60b569de`)
- `POST /v1/host/sample/envelope/accept` — RFC 0021 envelope-accept (404 from initial deploy until `60b569de`)
- `GET /v1/host/sample/test/otel/spans` — RFC 0034 OTel scrape (404 from initial deploy until `60b569de`)

Every conformance scenario that hit these paths returned the 404 soft-skip path instead of exercising the host's actual implementation. The shape probes (advertisement-only) passed and appeared correct in this matrix; the **behavioral** assertions never ran.

This affects the framing of earlier promotion entries — RFC 0027 graduated Active → Accepted on 2026-05-23 (commit `8f65168` upstream) under MyndHyve's "71/71 green on targeted local suites" claim. Those local suites exercised the compose-seam code directly without going through the host's REST surface, so they were valid evidence that the compose-seam logic works. But the **conformance-suite scenarios** that test the seam through the `/v1/host/sample/prompt/compose` HTTP endpoint were silently skipping on 404. With `60b569de` deployed, the conformance scenarios now run and pass against the live wire — verified 2026-05-23 via direct curl probes (POST → 200 OK or 400-not-404 across all four seams; the 400s indicate the routes are registered and validating input, which is what we want from an empty-body probe).

**RFC 0027 status stays Accepted.** The advertisement was real, the local-tests-green claim was real, and the wire surface is now exercisable end-to-end. The honest correction is documentation of the gap between earlier reported PASS and actual SKIP-on-404 — not a Status reversal. The same SECURITY-tier-graduation honesty principle that motivated the sandbox revert (`5864a2f`) applies here: surface the gap clearly so the conformance-pass claim's load-bearing meaning is preserved.

**RFC 0034 promotes Active → Accepted on the post-`60b569de` evidence.** Now that the `/v1/host/sample/test/otel/spans` route is registered and serving `200 OK { spans: [] }` (verified live 2026-05-23), the seam-shape probe passes. Per RFC 0034 §B's "Upstream dependency on RFC 0021 envelope-accept" clause, the empty-buffer Tier-1 boundary is explicitly allowed — the SR-1 behavioral assertion is vacuously safe (no spans means no canary leaks). MyndHyve's Tier-2 (SDK provider + BatchSpanProcessor + InMemorySpanExporter wiring) is a separate strengthening pass.

### MyndHyve adoption status — RFC 0021 + RFC 0028 Tier-1 + RFC 0029 Tier-1 + RFC 0034 (2026-05-23, all `60b569de`-and-later)

Per MyndHyve commit `60b569de` (revision `workflow-runtime-00196-7mm`):

| RFC | MyndHyve adoption | Promotion impact |
|---|---|---|
| **0021** (AI envelope primitive) | `POST /v1/host/sample/envelope/accept` live with full §A outcome decision tree (`accepted` / `invalid` / `gated` / `breached`) + BYOK-canary recursive SR-1 redactor + contentTrust propagation. Wire-shape verified live: `200 OK {status: 'accepted', envelopeId, contentTrust}`. | RFC 0021 was Accepted long ago; this is continued adoption, no Status change. |
| **0028** Tier-1 (Prompt Library Endpoints) | `GET /v1/prompts` + `GET /v1/prompts/{templateId}` + `POST /v1/prompts:render` live on production. `POST /v1/prompts` + `PUT/DELETE /v1/prompts/{templateId}` return `501` + `Allow: GET, HEAD, OPTIONS` per §C immutable-library posture. Advertises `prompts.{packsSupported: false, mutableLibrary: false, library: {id: 'myndhyve-system', renderEndpoint, maxRenderRequestBytes: 65536}}`. | **Stays Active.** Path-to-Accepted requires `packsSupported: true` AND `mutableLibrary: true` (per RFC 0028 §B). MyndHyve's honest Tier-1 advertises both `false` — Tier-2 (pack distribution + RBAC-scoped mutating endpoints) is a multi-day Firestore/RBAC session per their adoption note. |
| **0029** Tier-1 (Prompt Override Hierarchy) | `agent.promptResolved` RunEventType + payload (strict, `.refine()`-enforced invariant: `chain[applied=true].source === resolved`, or all `applied: false` when `resolved: null`). `emitMasEvent` helper + `callPrompt.node.ts` emission point fires once per `(nodeId, kind='system')` BEFORE the LLM call. Honest Tier-1 chain: node layer with synthetic `prompt:inline-<nodeId>` ref when `config.systemPrompt` set; `resolved: null` with both node + host-defaults layers `applied: false` when unset. | **Stays Active.** Path-to-Accepted requires `agentBindings: true` advertisement (RFC 0029 §D). MyndHyve's Tier-1 explicitly defers the agent-* layers (agent-intrinsic / agent-overrides / agent-library-default — dependent on AutomationAgent formal sub-layer modeling). |
| **0034** (OTel collector test seam) | `GET /v1/host/sample/test/otel/spans?runId=...` live, returns `200 OK { spans: [] }`. `runId` scope (MUST) + `envelopeId` + `name` filters (SHOULD) all honored. Advertises `observability.testSeams.otelScrape: true`. Tier-1 honest boundary: empty buffer because MyndHyve uses `@opentelemetry/api` only (no SDK provider). Per RFC 0034 §B explicit allowance — seam-shape probe passes; SR-1 behavioral assertion against span attrs vacuously safe. | **Active → Accepted (2026-05-23).** First non-steward OTel-seam adoption. Tier-2 SDK wiring is strengthening, not gating. |

### Forward-reference — MyndHyve vendor-extension RunEventTypes

For consumers writing strict RunEventType-enum validators, this table lists the vendor-extension event types MyndHyve emits to its event log. Per `host-extensions.md` §"Canonical prefixes", strict validators MUST tolerate vendor types matching `^x-host-<host>-<key>$` as additive — they're host-private extensions, not protocol additions.

| Vendor event type | Emitted by | Wire shape (host-private, NOT normative) | Spec context |
|---|---|---|---|
| `x-host-myndhyve-memory-written` | MyndHyve SR-1 redaction chokepoint, post-`MemoryAdapter` write | `{ memoryRef: string (collection path), memoryId: string, content: string (post-SR-1 redaction), source: 'session' \| 'manual' \| 'shared' \| 'feedback', tags?: string[], expiresAt?: string (ISO 8601, writer-clock per RFC 0039 MAE-2), writtenAt: string (ISO 8601, write-time wall clock) }` | RFC 0039 Half B internal SR-1 audit trail. Locked in MyndHyve commit `21fb2387` (2026-05-23) — chose Option 1 (vendor namespace) over Option 2 (canonicalize via RFC) for now. Rationale at MyndHyve `packages/workflow-engine/src/protocol/RunEvent.ts:111-121` parallels the `confidenceEscalationInterruptKind: 'x-host-myndhyve-low-confidence'` shape. If/when a multi-host SR-1-audit canonical event type emerges via additive RFC adding `memory.written` to the RunEventType enum + payload schema, MyndHyve's rename to the canonical form is one-line (event-log append site + enum entry + resolver filter). |

These rows are documentation, not protocol-tier wire-shape commitments. Future additive RFCs MAY canonicalize an event family currently rendered as a vendor extension; when that happens, the entry's "Spec context" cell links to the new RFC + the host's migration commit.

## Experimental capability advertisements (RFC 0042 §B)

Per RFC 0042 (Draft 2026-05-22, response to the standards-readiness review's "Active RFC → explicit carve-out" recommendation), hosts that advertise capability sub-blocks backed by RFCs that have not yet promoted to `Accepted` SHOULD set `tier: "experimental"` + `experimentalUntil` (ISO-8601, ≤ 12 months) on the sub-block. This makes the stability claim machine-readable: clients and conformance suites can route around experimental surfaces, and external auditors can read the discovery payload to form an opinion in 30 seconds.

The conformance suite's `experimentalGate()` helper (`conformance/src/lib/behavior-gate.ts`) honors the field: under default mode, scenarios consuming an `experimental` capability soft-skip with a dedicated log line; under `OPENWOP_REQUIRE_EXPERIMENTAL=true`, the same scenarios run as hard assertions. Hosts ready to claim full coverage in `INTEROP-MATRIX.md` set both env vars (`OPENWOP_REQUIRE_BEHAVIOR=true` + `OPENWOP_REQUIRE_EXPERIMENTAL=true`).

| Host | Experimental capabilities advertised | `experimentalUntil` | Active RFCs this carve-out covers | Tracked-out conformance scenarios |
|---|---|---|---|---|
| Workflow-engine sample (`apps/workflow-engine/`) | *Not yet wired into discovery — repo-side RFC 0042 schema landed; host wire-up tracked in `docs/PHASE-4-PROGRESS.md` as the Phase C close-out follow-up.* | — | RFC 0035 (sandbox MVP behaviorally covered; tier-shape claim is optional), RFC 0036 (multi-region + cross-engine harnesses live), RFC 0041 (§B replay-divergence-at-refusal live, §C observable-sequence-determinism deferred) | `replay-observable-sequence-determinism.test.ts` (2 `it.skip` blocks carrying the RFC 0042 marking) |
| MyndHyve workflow-runtime | *Currently advertises `multiAgent.executionModel.{version: 2, …}` as stable in production (revision `workflow-runtime-00353-rab`). RFC 0042 §B opt-in would be additive — recommended once Phase 3 cross-host causation lands on MyndHyve and pre-graduation evidence is in flight.* | — | RFC 0039 §B Half B (memory lifecycle) wired (commit `a51f7bbd`); RFC 0040 (cross-host causation) + RFC 0041 (replay determinism) pending. | `multi-agent-memory-lifecycle.test.ts` (MAE-2 + MAE-3 `it.skip`), `cross-host-traceparent-propagation.test.ts` (MCP + A2A `it.skip`) |
| Postgres / SQLite / In-memory / Python reference hosts | *Not applicable — these hosts don't advertise the Active-RFC sub-blocks gated for experimental marking.* | — | — | — |

**Six `it.skip` blocks across three scenario files** carry an explicit `out of stable profile via RFC 0042` marker (replacing the earlier `it.todo` form). These mark the scenarios as "tracked-but-deferred" rather than "unfinished," satisfying the 2026-05-23 audit's "Replace remaining `it.todo` assertions with runnable behavior tests or mark them out of the stable profile" acceptance-bar item.

## Reading Rows

- **Compatibility profile claim** is derived from `/.well-known/openwop` according to `spec/v1/profiles.md`.
- **Scale claim** follows `spec/v1/scale-profiles.md`.
- **Production profile claim** follows `spec/v1/production-profile.md` and is recorded separately because durability, retention, backpressure, and observability are operational evidence, not discovery-payload predicates.
- **Conformance evidence** should name the suite version, command used, target URL class, and pass/fail/skip counts. Do not include private deployment identifiers, secrets, or internal result paths.

## Add A Host

1. Implement the openwop v1 wire contract.
2. Run `@openwop/openwop-conformance` against the host.
3. Publish a result file or Markdown summary in a public repository.
4. Add a row above with compatibility, scale, production-profile, and evidence claims.

## See Also

- `conformance/README.md` — how to run the suite.
- `spec/v1/profiles.md` — compatibility profile predicates.
- `spec/v1/scale-profiles.md` — scale tier definitions.
- `spec/v1/production-profile.md` — public-release operational profile.
