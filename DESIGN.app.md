# OpenWOP Reference App — Design Standards (moved)

> **This document moved with the reference application to [`openwop/openwop-app`](https://github.com/openwop/openwop-app)** (as `DESIGN.md` there — the `.app` suffix was only needed here to distinguish it from the corpus `DESIGN.md`). It governs the app's React frontend (`frontend/react/`), which is no longer in this repo.
>
> The shared corpus design doc `DESIGN.md` moved to [`openwop/openwop-site`](https://github.com/openwop/openwop-site) with the public site. The app-specific standards live with the app they govern.
