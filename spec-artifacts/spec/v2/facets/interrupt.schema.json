{
  "$comment": "RFC 0173 §B — approver enforcement binds with the fields; refKinds stays a facet",
  "properties": {
    "refKinds": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "principal",
          "group",
          "role"
        ]
      }
    },
    "tokenAlgs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": [
          "hs256"
        ]
      }
    }
  },
  "required": [
    "tokenAlgs"
  ]
}
